# 第 81—125 章源码复审记录

> 权威基线：本地 `.repo/manifests/default.xml` 的 `android-11.0.0_r48`。  
> 审查原则：逐篇完整阅读；路径、方法、调用链、进程/线程、常量/超时与版本边界以本地可执行源码为准；
> 练习保持 macOS 只读，不要求编译、刷机或改设备状态。  
> 状态说明：“通过”表示本轮未发现需要落盘的实质错误；“修订”列出本轮实际改动。此文件可分批更新，便于中断恢复。

| 章 | 结果与证据 / 修改摘要 |
|---:|---|
| 81 | **修订。**逐段对照 `system/vold/Checkpoint.cpp`、`system/core/fs_mgr/fs_mgr.cpp`、`system/core/rootdir/init.rc`：`retry+1`、`needsCheckpoint/needsRollback`、F2FS 与 dm-bow、commit/abort、密钥延迟删除边界成立；补一张控制面→两种数据保护分支的最小 Mermaid 主链图，并把省略目录的 dm-bow target 路径补全为实存的 `system/core/fs_mgr/libdm/include/libdm/dm_target.h`。 |
| 82 | **修订。**逐段对照 `update_attempter_android.cc`、payload consumer action、BootControl 适配层和 `snapshot.proto`：修全 3 个被截短的源码路径；把 Virtual A/B 的 `UpdateState` 与底层 `SnapshotState` 分开，补全 `MergeNeedsReboot/MergeFailed/Cancelled` 等边界；明确 `IBootControl.hal/types.hal` 是仓库接口源，常见 `I*.h/types.h` 是构建生成头，不能当源码树路径。 |
| 83 | **修订。**逐段对照 `external/avb/libavb`、`fs_mgr_avb.cpp` 与 first-stage mount：信任根、descriptor、rollback location、A/B 延迟提交和 verity 层次成立；细化两个 disabled flag，明确 `VERIFICATION_DISABLED` 仍验证顶层 vbmeta、跳过 descriptors 并装载 requested partitions，是否容错由调用方策略决定。 |
| 84 | **通过。**逐段对照 `first_stage_main.cpp`、`first_stage_mount.cpp`、`fs_mgr_fstab.cpp` 和 `liblp`：DT/default fstab 选择、先挂 `/metadata`、创建 logical/snapshot view、再叠 dm-verity、同 mount point 候选项和 switch-root 顺序均与 r48 一致；userdata 仅先初始化块设备、不等于解锁 CE 的说明准确。 |
| 85 | **修订。**逐段对照 `init/main.cpp`、`init/selinux.cpp`、libselinux Android glue 与 sepolicy 宏：split/precompiled identity、CIL fallback、enforcing、restorecon+re-exec、daemon/App transition 和 contexts 边界成立；修正开篇“system/vendor/product 已全部挂载”的过度概括，补上 `MountMissingSystemPartitions()` 对 R system + 旧 vendor 布局的兼容分支。 |
| 86 | **通过。**逐段对照 Android 11 sepolicy 的 daemon/Binder/vndbinder/HAL/property 宏、vendor 示例 type 与 contexts：本章已明确 `mower_diag` 全为虚构教学设计，三种 IPC 为互斥架构选项，且专用设备/sysfs/data type、callback 反向 call、public policy API 与 neverallow 边界准确；未改正文。 |
| 87 | **修订。**逐段对照 `UEventObserver.java`、core JNI 注册表、uevent native 封装、services JNI 与 Android 11 Vibrator AIDL/HIDL fallback：共享线程、native/Java 两级 match、事件恢复、JNI/HAL 边界和线程锁建议成立；把源码地图中的 `.../SystemServer.java` 与 `vibrator/1.x` 改成 r48 可定位的真实路径/版本目录。 |
| 88 | **通过。**逐段对照 `ueventd.cpp`、`uevent_listener.cpp`、`devices.cpp`、配置 parser、firmware/modalias handler 与 Framework observer：r48 实际只从 `/sys/devices` regenerate、coldboot fork/stride、可选并行 restorecon、SEQNUM 被忽略、overflow 丢弃、路径/label/mknod/add-change-remove 和跨 listener 竞态均有源码证据；正文也已主动标出 README 与执行代码不一致，未改正文。 |
| 89 | **修订。**逐段对照 vibrator HIDL 1.0—1.3、stable AIDL v1、libhidl service management、hwservicemanager、VINTF 合并和 Framework JNI：两种 manager/driver domain、VINTF enforcement、lazy start、notification、AIDL eager rc、capability/fallback 与死亡恢复边界均准确；补充 `.hal` 是仓库接口源、`IVibrator.h/IServiceManager.h/types.h` 通常是构建输出，防止 Mac 只读练习去打开不存在的生成头。 |
| 90 | **通过。**完整阅读后对照 r48 `ProcessState`、`IPCThreadState`、`BpBinder`、`Parcel` 与 Binder UAPI；因本 checkout 无匹配 kernel driver，正文已明确把内核字段/锁限定为稳定对象模型而非伪装逐行结论。15-thread 配置、约 1 MiB mmap、BC/BR、ptr/cookie 强引用提升、reply/oneway、death cookie、calling identity 与 freezer 返回均准确；未改正文。 |
| 91 | **修订。**完整对照 native `Parcel.cpp/Parcel.h`、Java `Parcel.java`、`Status`、Binder UAPI 与 JNI 错误映射：将核心布局改成 Mermaid，明确 `mObjects` 是特殊对象 offset 表而非第二份 payload；纠正 `appendFrom()` 会自动拒绝任意“切到 object 中间”的过度承诺——r48 仅复制完整落入范围的 objects，调用者仍须验证切片协议边界。FD/Blob/owner、reply headers 与 TLE 歧义其余成立。 |
| 92 | **修订。**完整对照 Java/CPP/NDK generator、aidl unit tests、真实 light/vibrator/power/identity 接口与 frozen API 目录：Proxy/Stub 镜像顺序、CPP pure-out array length、oneway、UNKNOWN_TRANSACTION default、local fast path、version/hash meta transaction、size-prefix 与 backend 边界均准确；把源码地图中 5 个带 `...` 的占位路径展开为 r48 实存的 ILights、IVibrator、IVibratorCallback、IIdentityCredential 和 `tests_1/some_package/IFoo.aidl`，避免读者把省略号当目录。 |
| 93 | **修订。**完整对照 servicemanager `main/ServiceManager/Access`、AIDL contract、Java facade 与 libbinder shim：handle 0、单线程 Looper+5 秒 timer、三种查询、两类 callback、lazy start/guarantee/refcount 边界成立；修正 `canFind` 被拒时并非返回虚构 security error，而是兼容性地给调用者 null；删除 r48 同名覆盖会“记录警告”的错误说法，并补一张 lazy 注册/业务直连 Mermaid 时序图；一级标题统一。 |
| 94 | **修订。**完整对照 `Context/ContextImpl/SystemServiceRegistry/SystemService/LocalServices` 与 PowerManager 注册、模块初始化：class→name→Fetcher、per-ContextImpl gate、三类 cache、Binder/LocalService 发布和缺失服务边界成立；统一一级标题，并纠正 `StaticApplicationContextServiceFetcher` 只有 ConnectivityManager 一个使用者的说法——r48 的 TestNetworkManager 也使用，且静态实例是每进程缓存。 |
| 95 | **修订。**完整对照 `SystemServiceManager/SystemService/SystemServer/UserController`：反射构造、先入 `mServices` 再 `onStart`、phase 严格递增且只遍历轮次起始长度、50ms 慢回调、phase 1000 关闭 init pool、用户回调过滤/异常继续与 CE 最后窗口均准确；补清 phase 1000 只是 AMS 推进的 Framework 里程碑，不等价于 Home 首帧/可交互或所有 BOOT_COMPLETED Receiver 完成；标题统一。 |
| 96 | **通过。**完整对照 `TimingsTraceAndSlog/TimingsTraceLog/SystemServerInitThreadPool/SystemServer`：同线程 trace 栈、非 user build duration、10 层记录、CPU 数固定池、pending/Future 异常、Sensor/WebView/Zygote/BlobStore 汇合、20 秒 shutdown 抓栈与 phase 1000 边界准确；只读命令可执行，一级标题统一。 |
| 97 | **通过。**完整对照 `SystemServer`、`ServiceThread`、Fg/Ui/Io/Display/Animation/SurfaceAnimation/BackgroundThread 与 `Watchdog`：main 两阶段、Binder 31 上限语义、调度继承、StrictMode I/O、共享线程属性、calling identity、oneway、polling 快路径、Monitor 与 Binder 可用性检查均准确；练习路径可执行，一级标题统一。 |
| 98 | **修订。**完整对照 libbinder `ProcessState/IPCThreadState`、Java Binder/JNI、`BinderCallsStats` 与 Watchdog：同步/oneway、异步队列、BR_SPAWN_LOOPER、31/15 上限、池饥饿、WorkSource、death generation 和等待图均成立；补上 r48 `FAILED_TRANSACTION` 的真实 JNI 启发式——可抛 RemoteException 且 data Parcel >200 KiB 才映射 TTLE，小 Parcel通常映射 DeadObject，而非笼统“都映射 TTLE”；标题统一。 |
| 99 | **修订。**全篇核对 init/AMS/UserController/bootstat 路径与时钟边界；把不存在的 `frameworks/proto_logging/stats/atoms.proto` 改为 r48 真实文件 `frameworks/base/cmds/statsd/src/atoms.proto`。 |
| 100 | **修订。**确认本章是教学设计而非现有服务；将 AIDL/Manager/server 目录全部醒目标为“教学拟新增、当前树不存在”，并把过期的“到 100 停止”改为“100 是阶段总结，101 起真实服务精读，路线持续到 200”。 |
| 101 | **修订。**完整对照 `DropBoxManagerService/DropBoxManager/IDropBoxManagerService`：lazy init、两级索引、配额、公平裁剪、低优先级广播、permission+AppOps、FD Entry 与 dump 边界成立；补 Mermaid 主链；明确指定 tag 索引看不到 lost/零 block 条目，文件 Entry 在服务端返回前已打开 FD；纠正持久化顺序，r48 是先 `sync(foutput)` 再 close/flush/gzip 尾部，不能宣称最终字节均被该次 fsync 覆盖；标题统一。 |
| 102 | **修订。**完整对照 `BootReceiver`、Manifest、ShutdownThread/init reboot、fsck 解析和 r48 tombstoned：header、firstboot、last_kmsg/pstore 截断、mtime+AtomicFile、mount/shutdown/fs_stat、DropBox/Atom 与 observer 均成立；沿生产者纠正“CREATE 可能读半文件”的悬置结论——tombstoned 完成后才 `linkat` 出 `tombstone_XX`，fallback `.temporary*` 又被前缀过滤；补充真正残留的是扫描到 watch 生效之间的窗口；标题统一。 |
| 103 | **修订。**完整对照 `debuggerd_handler.cpp`、`crash_dump.cpp`、`tombstoned/`、libdebuggerd 和 BootReceiver：补出 handler、crash_dump、unwind/report 库、tombstoned、Framework 消费者的责任表与 Mermaid 主链，明确仓库真实目录为 `system/core/debuggerd/tombstoned/`；细化 detach 注入 signal 与 handler 恢复默认处置/重排信号的协作；把 system_server 特例纠正为检查 PID 对应主线程名，而非必然检查崩溃线程；收紧 completed 后 linkat 与 `.temporaryN` 过滤的文件完整性结论；标题统一。 |
| 104 | **修订。**完整对照 `NativeCrashListener`、AMS `handleApplicationCrashInner`、`AppErrors`、`AppErrorDialog` 与 crash_dump：补 Mermaid 主链并明确 ACK 与 reporter 并行；纠正时序，r48 是先尝试通知 AMS、再 close 输出并向 tombstoned completed，故正式路径发布与 Framework 处置可交错；纠正文末无 NUL 降级，EOF 可带部分文本继续，而 socket timeout 通常抛异常并放弃本次 reporter；其余 PID 关联、persistent 跳过、crash-loop/bad process、Service retry、UI 与退出归因边界成立；标题统一。 |
| 105 | **修订。**完整对照 ART `HandleUncaughtExceptions`、libcore Thread/ThreadGroup、`RuntimeInit`、`ApplicationErrorReport`、ActivityThread token、Binder server dispatch 和 AMS：pre-handler/default handler、CrashInfo 首尾各 10 KiB、同步报告与 finally 退出等主链成立；细化 Binder 线程异常边界——`RemoteException/RuntimeException` 由 Binder Java 层回包或记录，`Error` 才由 native 桥尝试送入 uncaught machinery；说明 `mCrashing` 分支中的 return 仍会执行 finally 杀进程，只跳过重复报告；标题统一。 |
| 106 | **修订。**完整对照 `AppExitInfoTracker`、`ApplicationExitInfo`、ProcessList zygote/LMKD 接口与 AMS 查询鉴权：四来源合并、5 分钟 freshness、LMKD 优先、三种 UID、128-byte summary、ANR trace claim、30 分钟 AtomicFile 持久化和查询边界成立；纠正 loaded 窗口的过度概括——被 gate 的是 ProcessRecord death 与 AMS app-kill record，LMKD/zygote 外部消息仍可进入 Handler/cache，但可能因缺 raw record 而无法形成完整历史；标题统一。 |
| 107 | **修订。**完整对照 lmkd 协议、`cmd_procprio`、PSI/vmpressure、新策略、victim/kill/pidfd、stats 与 ProcessList socket：PSI 只触发评估、adj 优先于档内 LRU/RSS、LMK_PROCKILL 与退出历史的边界成立；补出同仓库文档与执行代码差异——r48 README 称 `kill_timeout_ms` 默认 0，但 `update_props()` 的真实默认是 100 ms，运行时还可被产品属性覆盖；标题统一。 |
| 108 | **修订。**完整对照 `OomAdjuster` quick/full 入口、四维基础证据、Service/Provider flags、cycle 固定点、cached/service 分档和 apply：主链成立；收紧 quick 图对 WPRI edge 的掩码描述，明确“只命中”限定于 WAIVE/TREAT/ADJUST 三个位；补上 r48 `BIND_WAIVE_PRIORITY` 虽不传播主要 importance，却可能以 `shouldNotFreeze` 保护未冻结 client 的 host；进一步说明 `setOomAdj()` 返回失败也不阻止本地 `setAdj=curAdj`，所以它不是 lmkd `/proc` 已生效确认；标题统一。 |
| 109 | **修订。**完整对照 `ProcessList` 的两段 LRU、依赖联动与 connection group，及 `OomAdjuster.assignCachedAdjIfNecessary/updateAndTrimProcessLocked`、`ActivityManagerConstants`：列表方向、r48 `hasService=false`、900～999 分档、默认 32、empty aging 和四类退出 subreason 均成立；统一标题，并纠正“只有全量 OOM adj 才做缓存 trim”的表述——局部 reachable 更新结束后同样调用 `updateAndTrimProcessLocked()`，且该方法扫描完整 `mLruProcesses`。 |
| 110 | **修订。**完整对照 `AppDeathRecipient/appDiedLocked/cleanUpApplicationRecordLocked`、`ActiveServices.killServicesLocked`、Provider death、ProcessList name/UID/LRU 清理和 ATMS Activity 恢复：三重实例校验、stable/unstable、bounded retry、persistent/isolated 与退出证据边界成立；统一标题，修正 Service crash 条件为 `>=`；明确 `ProcessList.remove(pid)` 只撤销 lmkd 登记；补出 r48 后半段会把入口 `allowRestart` 重置为 true、再由 successor 等条件约束的真实边界；并按源码重排总图，立即 `startProcessLocked()` 发生在 cleanup 内、早于 ATMS `handleAppDied()`。 |
| 111 | **修订。**完整对照 `scheduleServiceRestartLocked/performServiceRestartLocked/unscheduleServiceRestartLocked`、StartItem 3/6 次上限、AppErrors crashCount、默认退避常量和 `sendServiceArgsLocked`：普通指数退避、重复 crash 长退避、错峰、双重取消、RETRY/REDELIVERY 与 URI grant 边界成立；统一标题；重点纠正 FGS 重建说明——schedule 阶段 cancel 通知却保留 `isForeground/foregroundId/foregroundNoti`，已兑现过转换义务的 FGS 会在 realStart 时重发通知，通常不会仅因换进程重新启动 10 秒 timeout；只有仍为 `fgRequired` 的未完成转换继续受限。 |
| 112 | **修订。**完整对照 `bringUpServiceLocked`、ProcessList PID/startSeq、`attachApplicationLocked`、`realStartServiceLocked`、ActivityThread create 与三类 timeout：fork/attach/create/done 完成点、10s/20s/200s/10s 起点和进程级 execution 聚合均成立；统一标题；纠正 restart guard 的范围，新 start/bind 可先通过 `unscheduleServiceRestartLocked()` 合法改变状态；纠正 attach timeout 后 pending Service 的 r48 处置为直接 `bringDownServiceLocked()`，不是自动转入 Service restart；并按真实语句顺序拆开 FGS“清 deadline 义务”与“真正获得 foreground 待遇”，AppOps/后台限制可在 timeout 已取消后仍忽略转换。 |
| 113 | **修订。**逐段对照 `AnrHelper`、`ProcessRecord.appNotResponding`、AMS trace/prune、WPC controller、AppErrors/Dialog 与 AppExitInfo：单消费者、1 分钟降级、PID 分层、20 秒共享预算、first-PID offsets、silent kill、Wait 重排等成立；统一 Mermaid label 引号；纠正“DropBox 已先落盘”的同步错觉，普通 App 的 `addErrorToDropBox()` 只是先启动 worker；并把第二次 WPC hook 收紧为实际的 `IActivityController` 协议，非零结果才在锁外 kill/重排 timeout 并接管默认 UI。 |
| 114 | **修订。**完整对照 AMS queue 选择、`BroadcastQueue.processNextBroadcastLocked/finishReceiverLocked/broadcastTimeoutLocked`、ActivityThread receiver 与 PendingResult：dynamic parallel、manifest serialized、pending attach、goAsync、QueuedWork、滚动 timer、WAITING_SERVICES、hung 兜底和先推进后 ANR 均成立；统一 Mermaid 标点节点引号；收紧 foreground queue 表述，不用 queue 名推断固定 adj/sched group；明确 r48 仅 background/offload `mDelayBehindServices=true`；补出 offload flag 是隐藏 API且还受开机只读属性开关约束。 |
| 115 | **修订。**完整对照 `BroadcastDispatcher/BroadcastQueue/BroadcastRecord/BroadcastConstants`：UID 稳定拆分、split refcount、普通 deferred 递减、Alarm in-flight fast lane、current 非抢占和跨 queue 边界均成立；重点纠正 receiver timeout 分支会在 `finishReceiverLocked()` 前先将 `receiverTime=now`，所以此次 elapsed 通常只有几毫秒，不会如原文所说自动叠加 `startDeferring()`；同时收紧 cancel 仅清时间、不删账户的语义。 |
| 116 | **修订。**完整对照 AMS Provider 获取/发布/死亡、`ActivityThread` 引用协议和 `ContentResolver` 常量：10 秒 publish、20 秒 ready、1 秒客户端 retain、stable/unstable、external handle 与死亡分叉均成立；纠正 ready deadline 是 AMS 完成 resolve/启动准备、进入 CPR 等待阶段才起算，不是整个最外层 API 的硬上限；并按 r48 真实分支说明普通虚假/无关唤醒后会立即判等待失败，不能套用教科书式“继续等剩余 deadline”。 |
| 117 | **修订。**完整对照 `ContentProvider.Transport/ContentProviderNative`、CancellationSignal 和 BulkCursor/CursorWindow：authority/user/path、permission/AppOps/URI grant、`call()` 自行鉴权、跨 Binder 取消和窗口化 Cursor 边界均成立；明确 same-app 快路比较的是忽略 userId 的 appId，并把 AppOps 拒绝 insert 默认结果纠正为在基 URI 后追加 `/0`，而非“可能返原 URI”。 |
| 118 | **通过。**完整对照 `ContentResolver/ContentObserver/ContentService`：authority+path segment 树、false 仍收祖先通知、SKIP 只跳 leaf 上 descendants entry、Binder self identity、user 匹配、锁内收集锁外 oneway 分发、后台 10 秒延迟、通知聚合、SyncManager 调度与字符串前缀 cache 失效均与 r48 一致；未改正文。 |
| 119 | **修订。**完整对照 `SyncManager/SyncOperation/SyncJobService/SyncManagerConstants/AbstractThreadedSyncAdapter`：Adapter 发现、账户展开、Job 持久化、duplicate/conflict、绑定与 oneway 完成、周期失败派生 one-off 均成立；补全 `AuthorityInfo` 六个值及 2 的恢复语义；明确 r48 `IGNORE_BACKOFF` 会跳过 backoff 与 `delayUntil` 整块；重点纠正旧 Javadoc 的“SyncManager 30 分钟 timeout”不存在于当前实现，r48 实际可见的是 JobServiceContext 默认 10 分钟 time slice 与 SyncManager 每 60 秒按 Adapter **UID** 总流量的 10-byte 无进展检查。 |
| 120 | **修订。**完整对照 `SyncStorageEngine`、`SyncStatusInfo` 与 SyncManager Job 恢复：accounts/status/stats 三文件、authorityId 双索引、syncable 多值状态、backoff/delayUntil 不落 accounts、pending 启动清零后从一次性 persisted Job 重建、active 与 100 条 history 内存化、status/stats 节流、RemoteCallbackList 按 user+mask 锁外回调与 `clearAllBackoffsLocked` 漏 index 0 边界均成立；重点纠正旧 periodicSync 迁移的过度承诺：r48 保留 XML parser 和 `restoreAllPeriodicSyncs()` helper，但全树中 helper 只有定义、无生产调用点，不能说当前开机主链必然执行迁移；当前周期权威仍是 JobScheduler persisted Jobs。 |
| 121 | **修订。**逐段对照 APEX `JobSchedulerService`、`JobStore`、`JobServiceContext`、`JobStatus`：ready 多层门、2 秒快照写盘、18 秒 bind、8 秒 start/stop ack、固定 10 分钟 context time slice 均成立；将源码路线残留的旧 Job API 目录改为 `frameworks/base/apex/jobscheduler/framework/java/android/app/job/`。特别确认 `getMaxJobExecutionTimeMs()` 没有替换 `JobServiceContext` 固定 timeout。 |
| 122 | **修订。**逐段对照 `QuotaController.java`：source user+package、四类门槛、1 分钟短期 rate window、5 秒仅合并 bucket session、短期计数跨 bucket cache、内存账本、无独立 parole 字段、restricted 充电例外等均有代码证据；为 Mermaid 中含 `?`、`+`、`/` 的节点标签统一加引号，消除渲染歧义。 |
| 123 | **修订。**逐段对照 `ConnectivityController.java`、APEX `JobInfo.java` 与 service flag 校验：active network/UID connected/capability、insane、拥塞、prefetch、standby exception 引用语义成立；修正 JobInfo 旧路径，并补充 `FLAG_WILL_BE_FOREGROUND` 是隐藏且需 `CONNECTIVITY_INTERNAL`、仍须真正调用 `startForeground()`，避免初学者误以为普通应用可用 flag 免费联网。 |
| 124 | **通过。**逐段对照 `TimeController.java`、`JobStatus.java`、APEX `JobInfo.java`：elapsed 时间、两个 heuristic Alarm、delay non-wakeup、deadline wakeup、一次性 override 与 periodic latest 边界、WorkSource 和锁/回调边界成立；未改正文。 |
| 125 | **通过。**逐段对照 `ContentObserverController.java`、`JobStatus`、`JobInfo/JobParameters`：按 user+URI+flags 共享 observer、每 Job 双延迟、40/50 阈值、Handler 非 wakeup、prepare 所有权转移、replacement/failure 交接和权限边界成立；未改正文。 |
