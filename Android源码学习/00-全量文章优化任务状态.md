# Android 源码学习文章全量优化任务状态

- 任务状态：进行中
- 优化范围：001—586 全量复查与可读性重构
- 最近完成章节：283
- 当前章节：284
- 下一章节：284（完成当前章后更新为 285）
- 最近更新时间：2026-09-15 23:24（Asia/Shanghai）
- 暂停或受阻原因：无
- 恢复方法：先读取本文件、`00-学习进度.md` 与 `00-文章可读性重构计划.md`，复核最近完成章后从“下一章节”继续；保持第 587 章新章生成恢复点不变。

## 当前批次

- P2 固定 120 节模板章。
- 第 136 章已完成：由 125 个二级标题收束为一条“历史负载如何修正 evaluated priority”的主线，并按 r48 源码复核 note 状态边界、nesting、两批窗口、dump 与 100 条事件历史。
- 技术修正：明确负载降权不会改变 TOP/非 TOP 分类，也不会按 priority 全局重排 pending queue；同 UID 抢占应限定为 `JobStatus.getUid()` 的 scheduling UID；删除旧稿关于文本 dump 多打印左括号的错误结论。
- 第 137 章已完成：由 125 个二级标题收束为一条“constraint 已 ready 后，thermal restriction 如何成为额外运行门”的主线，并核准 TMS 聚合、异步 listener、priority 豁免、pending/active 分流、停止原因优先级及 raw/overall dump 差异。
- 技术修正：明确同进程 Binder 接口不等于必然切 Binder 线程；Android 11 的 thermal stop reason getter/常量属于 `@hide`，不能写成普通 SDK 应用可依赖的公开契约。
- 第 138 章已完成：由 125 个二级标题收束为一条“constraint ready 后怎样通过完整候选门”的主线，拆清 registered/pending/active、多用户、backup、restriction、组件、batching、UID priority 与 shell override。
- 技术修正：指出 UID 状态消息只重新分配既有 pending，并不单独保证重建候选；核准 backup gate 按 source UID、恢复计数却只认自调度 scheduling UID 的 r48 不对称；明确三类 ready/pending 诊断口径不同。
- 第 139 章已完成：由 125 个二级标题收束为一条“同 jobId 再次 schedule 时，旧 Job 在哪个失败点开始受影响”的主线，拆清 Binder 验权、persisted API quota、enqueue 快路径、replacement、URI grant、WorkItem 迁移与异步落盘。
- 技术修正：核准默认第 251 次 persisted schedule 只对 debuggable 应用抛异常，非 debug 应用标记 buggy 后仍继续；指出普通入口共享 `mDebuggableApps` 的 null key；确认 `> 100` 比较允许第 101 个 Job，随后连 replacement 也会先被挡；限定 `RESULT_SUCCESS` 只代表服务端内存流程已接受完成。
- 第 140 章已完成：由 123 个二级标题收束为一条“不同取消入口按哪套身份选 Job，并在何处完成授权、运行态与磁盘清理”的主线，拆清单项、整 UID、package、source user、UID observer 与 shell 六类选择器。
- 技术修正：明确 URI revoke 在 r48 经 `UriGrantsManagerInternal` 本地接口，不应写成必然 Binder 往返；以实现而非漂移注释为准确认 package update 跳过清理；指出 package 入口漏选 system 代包 Job、shared UID/Controller 粗粒度边界及启动期孤儿删除不主动写回 jobs.xml。
- 第 141 章已完成：由 125 个二级标题收束为一条“Controller 事实变化如何经 Handler 扫描与批处理改变 pending”的主线，拆清 specific/null run-now、CHECK 合并、pending 全量重建、5 个/31 分钟聚批与 JCM 分槽边界。
- 技术修正：明确 31 分钟没有专用 Alarm，只在后续扫描比较；`mReportedActive` 可由 pending 而非 running 置真；active 停止门窄于完整候选门；stale run-now 会退化为 greedy 扫描，stale STOP 通常删不到新对象却可能按 UID+jobId 停止新 active 代际。
- 第 142 章已完成：由 123 个二级标题收束为一条“JSS jobs-active 如何参与 DeviceIdle maintenance 提前退出”的主线，拆清 pending/running 聚合、乐观占位、3 秒后台恢复、minimum active-op、WakeLock 与 deep/light budget Alarm。
- 技术修正：明确 jobs-active 为全局布尔而非运行计数，只阻止 early exit，不主动开窗、续约 Alarm 或持有 DIC WakeLock；指出进入 Doze 的 active 停止口径比报告公式更粗；默认 5/30 秒 minimum 与 3 秒延迟只是可配置关系，不是不可破坏的不变量。
- 第 143 章已完成：由 55 个二级标题收束为一条“配置字段更新后，各消费者何时真正收敛”的主线，拆清完整快照解析、单字段回退、并发矩阵、在途 screen-off Runnable、backoff 与 API quota Tracker。
- 技术修正：明确 Controller 常量回调在 r48 没有 override，热更新本身不触发 Job 扫描；错误 boolean 会变 false 而非回默认；并发下降不主动停止 active；screen-off 延迟变长可能令旧 Runnable 直接返回且不补发；负 quota window 可在 disable 清账后抛错，外层 catch 不回滚部分提交。
- 第 144 章已完成：由 68 个二级标题收束为一条“Job 不运行时怎样跨七层建立诊断证据链”的主线，拆清 Registered/Pending/Active、JobInfo/JobStatus、overall Ready、Controller、JCM、三类时钟与短命令边界。
- 技术修正：明确 package 参数按 appId 匹配 calling/source 且只过滤部分段落，Pending/Active/JCM 仍为全局输出；区分 dump 现场 priority 与分配快照；确认 `get-job-state ready` 只调用 `JobStatus.isReady()`；记录 r48 Proto 非匹配 Registered 分支 `start` 后 `continue`、未显式 `end` 的 token 配对缺口。
- 第 145 章已完成：由 76 个二级标题收束为一条“Alarm 从 API 时间窗口到 timerfd 再到 InFlight 完成”的主线，拆清 Binder flag 重写、elapsed 归一化、Batch 交集、政策延期、kernel 两个近期截止点、repeat 相位与 WakeLock 引用。
- 技术修正：确认 exact 自动 standalone；每 UID 500 上限早于旧目标移除，达到上限时 replacement 也先失败；kernel 到点后 allow-while-idle、background 与 non-wakeup 仍可延期；listener 是 oneway + complete/timeout 协议；无 driver 的 Handler fallback 不具备等价 wakeup/elapsed 语义。
- 第 146 章已完成：由 73 个二级标题收束为一条“deep Doze 建立挂起门后，普通 exact 与 AWI exact 为什么走向不同”的主线，拆清 IDLE_UNTIL 容器分流、AlarmClock 边界协商、creator UID 节流、临时白名单与 maintenance 反馈。
- 技术修正：明确 exact/standalone 不授予 idle 穿透；r48 普通 AWI 默认 short/long 为 5 秒/9 分钟且仍受 App Standby 配额；十秒 BroadcastOptions 只用于普通 AWI PendingIntent，不等于 AMS WakeLock；alarms-active 仅阻止 maintenance early exit。
- 第 147 章已完成：由 72 个二级标题收束为一条“App Standby 如何用 source 账户历史改写实际 Alarm 时间”的主线，拆清默认 bucket 配额、同毫秒去重、q-th-last 公式、expected/actual 分账、定向/全量重排与容器收敛时机。
- 技术修正：明确 history 统计所有非豁免成功发起的交付而非仅 wakeup；普通 AWI/exact 不豁免；pending-background 释放后会新增 history 却不就地触发 standby 重排；记录 bucket 消息全局合并、固定一小时 history 窗口与热更新不主动重排的 r48 边界。
- 第 148 章已完成：由 75 个二级标题收束为一条“RTC 需求怎样投影到 elapsed，并在 wall-clock 跳变后重新收敛”的主线，拆清三根时钟、orig/expected/actual、cancel-on-set bitmask、改钟/改时区、TIME_TICK 与 DATE_CHANGED 自续排。
- 技术修正：明确 `setTimeImpl()` 不直接 rebatch 或发 TIME_SET，而由 kernel 时间变化事件统一驱动；服务端 true 不能证明硬件 RTC 写入成功；严格 ±1000 ms 边界与注释不完全一致；main rebatch 不覆盖所有 pending 容器；修复旧稿 DATE_CHANGED 段落多余代码围栏。
- 第 149 章已完成：由 80 个二级标题收束为一条“直接 token 与生命周期身份怎样决定 Alarm 清理范围”的主线，拆清 PendingIntent Key、extras/flags、replacement、四容器精确取消、listener death、异步 token 作废与 bulk remove 矩阵。
- 技术修正：确认 background pending 外层按 creator UID 分组，而 stopped 清理将该 key 当 uid 整组删除并按它扣登记计数，代理场景存在身份不对称；补出 user stop 不清 `mUseAllowWhileIdleShortTime`；限定 bulk remove 漏 non-wakeup 与 stopped/user stale next-wake 的后果仍受 PI cancel/send 兜底约束。
- 第 150 章已完成：由 76 个二级标题收束为一条“成功投递怎样借 InFlight、共享 WakeLock 与目标类型完成信号闭合执行账”的主线，拆清等待/执行/统计三类账、PendingIntent type 完成点、listener completion/timeout、WorkSource 归因与广播 UID 边沿通知。
- 技术修正：明确 activity/service PendingIntent 的 finished 只代表请求交接，listener timeout 不停止 App 代码且 r48 不直接触发 ANR；确认广播 pending 接口虽名为 recipient UID，实际传 `alarm.uid`；补出 unmatched PI callback 仍会无条件 ref--、同 listener 重叠可丢 timeout，以及同步发送失败时 noteAlarmStart/Finish 可能不对称的边界。
- 第 151 章已完成：由 66 个二级标题收束为一条“只凭一份 dump 怎样定位 Alarm 所处生命周期”的主线，按等待容器、三根时钟、政策时间、Java/kernel deadline、InFlight 与累计统计建立诊断树。
- 技术修正：明确 expected 字段主要是 App Standby 恢复基线，AWI 节流会连 expected 一起重置而 Doze/后台限制多换容器；补出无下一项时 Java next 不清零且旧 timerfd 不显式 disarm；收紧 Last wakeup/trigger/dispatch 的阶段入口语义，并核准 Proto time-until 符号相反、schema whitelist 未写及 interactive 条件差异。
- 第 152 章已完成：由 77 个二级标题收束为一条“客户端声明怎样变成服务端有效电源约束”的主线，拆清本地双计数、Binder token 记录、owner/WorkSource、disabled、summary、共享 SuspendBlocker 与 release/death。
- 技术修正：明确 timed release 在客户端主 Handler 且基于 uptime，多次引用模式可保留多个 timeout；同 token 不新增记录却仍会重放 acquire flag 与状态重算；补出 package/historyTag 未纳入 same-properties 比较、UID gone 只移除 UidState 而不删 WakeLock、NONEXISTENT 又不触发 disabled，以及 long-partial 只统计有效段而不自动释放。
- 第 153 章已完成：由 85 个二级标题收束为一条“设备级状态提交怎样经多层异步协议完成亮灭屏”的主线，拆清四态与 interactive 二值折叠、uptime 防旧事件门、七阶段 dirty-bit 重算、用户活动时间线、Sandman、Doze 接棒、Display ready、Notifier early/late 与广播完成账。
- 技术修正：明确 raw wakefulness 先变不等于 transition 完成；`NO_DOZE` 仍真实提交中间 `DOZING`；screen WakeLock 只在 summary 中失效而非被删除；拒绝的 user activity 只有部分路径先产生统计副作用；attentive 自动睡眠仍记 `TIMEOUT`，且 attention 回注也会推进 attentive deadline；补出 DOZING 无 DOZE bit 时显示 policy 的暂留分支、`QUIESCENT` 字符串缺口，以及 `SCREEN_ON` early、`SCREEN_OFF` late 和专用广播 blocker 的精确顺序。
- 第 154 章已完成：由 66 个二级标题收束为一条“最新显示目标怎样经 pending/active 快照与分层完成点收敛”的主线，拆清同步提交、消息合并、一次性近距命令、开屏代际 token、DPS clean、local/latest ready、callback 重算与诊断字段。
- 技术修正：明确 DPC Handler 与 PMS power Handler 是不同对象但共享同一 ServiceThread Looper；`blockScreenOn` 和自动亮度 adjustment override 在 r48 只有数据类引用；关屏 blocker 主路径立即解除；DPS clean 不等于硬件 fence，brightness-only 下发也不在 `finished` 公式内；并修正 PMS ready、DPC unfinished、callback 与近距 debounce 是同一 `PowerManagerService.Display` blocker 上的多份引用，而非多把独立 blocker。
- 第 155 章已完成：由 67 个二级标题收束为一条“软件显示目标下发到底层时每个完成点能证明什么”的主线，拆清 DPS 失效、Photonic pending/actual、state-only ready、DisplayBlanker 顺序、DMS 锁内收集/锁外执行、LocalDisplay suspended 转换、power mode 与两条 brightness 路径。
- 技术修正：明确 Photonic `actual` 在 blanker 调用前写入且不是硬件回读；brightness-only 在途不阻止 `mScreenReady` 或 DPC `finished`；LocalDisplayDevice 软件 state 早于 Runnable；只有默认 local display 持有 Backlight；HWC 不支持 Doze 时 vendor mode 可退化为 ON；power-mode 错误与 SurfaceControl brightness false 均可能只留下日志，整条 r48 链没有面板完成 fence。
- 第 156 章已完成：由 72 个二级标题收束为一条“单笔 ALS 变化怎样穿过全部门后改变 DPC 目标亮度”的主线，拆清 enable 覆盖顺序、Doze 复用、sensor 生命周期、ring 时间权重、fast/slow、ambient debounce、mapping、用户点、短期模型、App correction 与输出迟滞。
- 技术修正：明确 temporary brightness/boost 在 enable 计算后覆盖，因而不一定停 ALS；找不到 sensor 或注册失败时内部 enabled 仍可能为 true；initial rate 基线默认并不快于 normal；短期模型 invalid 不停用用户点，只等待 accepted ambient 复核；brightness 与 adjustment 两类用户变更都绕过输出迟滞；并补出方向日志总落到 `Darkened` 且打印新阈值、前台查询失败保留旧 context 等诊断边界。
- 第 157 章已完成：由 79 个二级标题、2418 行收束为 16 个二级标题、598 行，以“用户调整为何会或不会形成可查询历史”为主线，拆清 nits 启动门、独立默认 ALS、DPC 候选、后台上下文、事件/日统计、两份 XML 与权限过滤。
- 技术修正：明确事件亮度是 DIM/low-power 后目标经 int 背光量化再映射的 nits；Tracker 停止监听不清最近 lux，首个新回调前可能读到旧队列；无 lux/焦点而丢事件时 `mLastBrightness` 仍已前移；30 天只在读写时裁剪；颜色 bucket 在 r48 Java 层直接转交采样数组；周期 Job 与关机入口只排后台写，不提供双文件耐久完成确认。
- 第 158 章已完成：由 74 个二级标题、1997 行收束为 16 个二级标题、644 行，以“环境 CCT 怎样成为最终白点矩阵”为主线，拆清四层 enable 门、双传感器/滤波、映射与 bias、Throttler、pending 提交、Bradford/RGB 矩阵、3 秒动画与 level 125 合成。
- 技术修正：明确 available 不是 DPC Factory 创建门，DWB lux 是区别于 ABC/Tracker 的独立监听；r48 两方向 debounce 资源交叉读取、档位取 ceiling 且无到期重试；默认 `[10,10]→[0,1]` 是 10 lux 台阶；NaN/Infinity 防御不完整；debug override 的 DPC 重跑不重算候选；`setMatrix()` 失败可留下 identity 且无上行错误，SurfaceFlinger transact 返回也不是显示 fence。
- 第 159 章已完成：由 32 个二级标题、1752 行收束为 16 个二级标题、660 行，以“Night Display 策略怎样成为最终颜色矩阵请求”为主线，拆清 setup 双门、手动覆盖、custom/Twilight、色温多项式、color mode 重建、DWB/无障碍仲裁、全局/单应用饱和度与 level 合成。
- 技术修正：明确 tearDown 不清 DTM 矩阵或 Night 温度缓存；custom exact Alarm 是非唤醒 RTC，秒级设置按分钟计算且回调未统一到 DisplayThread；Night 温度与单应用饱和度缺服务端范围校验；auto mode 原值与有效值可分离；LAST_ACTIVATED_TIME 读写 userId 不对称、温度缓存可跨用户/外部写残留；Binder 返回与 SF transact 均不是显示完成确认。
- 第 160 章已完成：由 32 个二级标题、1383 行收束为 16 个二级标题、696 行，以“Layer 颜色事实怎样成为设备可执行的输出 profile”为主线，拆清 Dataspace/ColorMode/RenderIntent、能力初始化、四个私有事务、矩阵合成、HDR 选择、HWC fallback、GPU/HWC 分工与完成点。
- 技术修正：明确 WCG 下补入对外 HDR types 不等于原生 PQ/HLG 支持；force mode 只覆盖 sRGB/P3 且随后仍可被 HDR 改写；1023 省略第二参数会保留旧 force mode；intent 候选虽按 HDR/SDR 分组但最终硬回退到 COLORIMETRIC；PQ client 标志只跟最后扫描到的 PQ Layer；全局饱和度首次低于 100 的齐次尾值 0 与 SF/HWC affine 契约冲突；HWC mode 失败无 Framework 回滚，transact 也不是 present。
- 第 161 章已完成：由 31 个二级标题、1515 行收束为 16 个二级标题、661 行，以“CLIENT/DEVICE 怎样协商并借 client target 汇合”为主线，拆清 present 骨架、validate/presentOrValidate、HWC requests、LayerSettings、透明清理、安全遮蔽、RenderEngine、request cache、target BufferQueue 与四类 fence。
- 技术修正：明确 HWComposer accept 早于 Display 本地应用 changed types；validate 出错只保留默认 output flags，不保证 per-layer 全 GPU 回退；`applyClientTargetRequests()` 因 `auto` 副本未把 dataspace 写回 Output state；flip-only 会 dequeue/queue 但不绘制；NO_BUFFER_AVAILABLE 沿用既有 target 且不重发 setClientTarget；draw/advance 失败无帧级回滚；Layer release 合并当前 target fence 是缺少上一帧代际追踪的保守近似。
- 第 162 章已完成：由 30 个二级标题、1458 行收束为 16 个二级标题、919 行，以“共享分配怎样借 slot 与 fence 安全复用”为主线，拆清 GraphicBuffer/native handle 身份、allocator/mapper、flatten/import、CPU 映射、数量模型、状态计数、双端 slot cache、丢帧和连接生命周期。
- 技术修正：明确 64 只是 slot 硬上限，默认同步公式当前为 2；max-dequeued 检查只在首帧 queue 后启用；分配在 Core 锁外且失败会清 slot；`requestBuffer()` 不是分配入口；FREE 与 fence signal 是两个维度；acquire 可临时多持有 1 张，按时间戳丢帧不先检查候选 fence；queue replacement 会保留旧 slot fence 并合并 damage；旧 EGL 等待与 Surface fence `dup()` 失败均只记录后继续。
- 第 163 章已完成：由 33 个二级标题、1343 行收束为 16 个二级标题、876 行，以“一次 View 更新怎样跨五个完成点走到 present”为主线，拆清 traversal、RenderNode 录制、UI/RT 交接、damage、GL/Vulkan、ReliableSurface、BLAST、两类 SF Layer 与窗口绘制回执。
- 技术修正：明确 UI 提前放行只取决于 `prepareTextures`；r48 `reserveNext()` 实为 no-op；ViewRoot dirty 的纵向 offset 使用 `surfaceInsets.right` 而非 top；空 damage 会绕过 CommonPool future 等待与常规 FrameCompleted 却仍触发 callback；ReliableSurface 的 scratch fence 仅在 fd>0 时 close；BLAST 用本地阻塞式 BufferQueue 且 release 落后一张；传统可丢头帧可绕过未 signal fence，SF 又用连续已 signal 的 maxFrameNumber 限制后帧 drop；BLAST 的 desired-time/acquire-fence gate 前移到 Transaction apply；`RenderProxy::fence()` 只清 RT 队列，不是 present fence。
- 第 164 章已完成：由 34 个二级标题、1213 行收束为 16 个二级标题、899 行，以“一组 Layer patch 怎样形成有边界的原子提交”为主线，拆清 `what` 增量协议、merge、复用、Parcel/cache、applyToken FIFO、current/drawing、BufferStateLayer latch 与 callback/release fence。
- 技术修正：明确原子性不是字段失败全量回滚；r48 `merge()` 不迁移 desired time、sync 与 animation 等全部事务级语义，`apply()` 又保留 `mDesiredPresentTime` 和 `mContainsBuffer`；queued `apply(true)` 不等待未来 commit；同 token 顺序不保证每笔产生可见帧；queued `TransactionState` 不保存原事务的 `InputWindowCommands`；transaction callback 携带 present fence 而不等待其 signal，BLAST 依 previous release fence 落后一项释放本地 BufferItem。
- 第 165 章已完成：由 45 个二级标题、1510 行收束为 16 个二级标题、791 行，以“一套主显示节拍怎样派生 App/SF 两路软件 VSync”为主线，拆清五层 VSync、Reactor/legacy、phase 数学、EventThread、BitTube、两端消费差异、Modulator 与刷新率/采样反馈。
- 技术修正：明确 Predictor 的 20% 是周期格点容差而非裁掉 20% 样本；负 phase 在 r48 代表跨周期预算，callback 位于 V1 前却以 V2 为 expected target，SF 的辅助重算还把零 offset 纳入加一周期分支；`onTransactionHandled()` 实际先于 `handleTransactionLocked()`，不是 commit 证明；App drain 保留最后一条 VSync，而 SF pending INVALIDATE 保留第一份 expected time；`requestNextVsync()` 是 oneway Binder，BitTube `EAGAIN` 不重试；hardware VSync 开关还跨 EventControlThread 与 SF 主线程，逻辑状态不等于 HAL 已完成。
- 第 166 章已完成：由 48 个二级标题、1316 行收束为 16 个二级标题、1051 行，以“Layer 证据与全局信号怎样在 Policy 内选出 config，并沿 active/period 两条确认链推进”为主线，拆清候选过滤、V2 history、显式 vote/tree、面积/焦点、cadence 评分、timer、desired/upcoming、HWC timeline、present fence 与 period sample。
- 技术修正：明确正常成功路径会在 present fence 推进 active 后，于同一次 invalidate 后段再次进入 `performSetActiveConfig()` 清理 desired pending；r48 的 `newVsyncAppliedTimeNanos` 只被缓存并钳到当前时间加 200ms，没有成为 active 更新或 period 确认的等待门；主显示 `getActiveConfig()` 在有 pending 目标时返回 desired id；单档 primary 下非 focused ExplicitDefault 不参与常规 score；稳定 rate 队列因 `size >= 90` 淘汰实际不保留满 90 项，并记录 `maxExplicitWeight`、`mUseFrameRatePriority` 的本版未使用边界。
- 第 167 章已完成：由 42 个二级标题、1092 行收束为 16 个二级标题、910 行，以“多套局部帧账本各自能证明什么”为主线，拆清 FrameInfo 槽位、JankTracker deadline、FrameMetrics 交付、SF missed、TimeStats 与 FrameTracker。
- 技术修正：明确 r48 尚无后续统一 FrameTimeline/token；C++ `duration()` 扣 UI→RT stall 而 Java TOTAL 直接相减；`FrameCompleted` 不是 GPU/present 完成，延迟补写的 `GpuCompleted` 不回填当前回调；HighInputLatency 计数不必从属于 jankyFrames；既有 JankTracker 没有刷新率热更新调用链；SF GPU/HWC miss 只是合成路径投影，短 jank atom 还存在 count 先清后用；TimeStats 的首条基线、队头阻塞、容量与尾桶策略会改变统计口径；FrameTracker 的 128 槽三时间环不是统一 timeline。
- 第 168 章已完成：由 59 个二级标题、1514 行收束为 16 个二级标题、1028 行，以“画面异常时三类 trace 各能证明什么”为主线，拆清 drawing-state 快照、ATRACE 执行区间、FrameTracer buffer 事件、fence 补写与 Perfetto phase 重建。
- 技术修正：精确限定 `missed_entries` 为 worker 获锁前合并的额外通知；修复旧稿 Perfetto parser `.cpp` 错路径和过期行号；补出无 predicate wait、5 MiB 超大 entry 静默丢弃、1029 KB/byte 校验及写错误被 `NO_ERROR` 覆盖；明确 FrameTracer 64 位 bufferId 写成 32 位、BufferStateLayer 未建 tracker、pending 跨 session、PRESENT refresh timestamp 替代；记录 parser 按 buffer/name 弱键建图、Display 名前 10 字符 track 冲突及 raw timestamp map 不清代的边界。
- 第 169 章已完成：由 50 个二级标题、1188 行收束为 16 个二级标题、1001 行，以“queue 返回以后，应用怎样追回 buffer 后半生”为主线，拆清 producer/consumer 双副本、十类事件、dirty delta、8 槽 ring、connectId、BLAST callback、CompositorTiming 及 EGL/Vulkan 翻译。
- 技术修正：明确 PENDING、INVALID 与 NOT_FOUND 的不同语义；支持能力首次 Binder 失败会在 Surface 生命周期内负缓存；void Binder 查询错误无法直接上传；dirty bit 选槽但 delta 仍携带整组标量，失败 apply 不回滚已更新 timing；默认关闭只停 producer 回传而非普通 consumer 建账；补出零/负 history 配置、latest-frame release 口径、BLAST previous-release 代际不对称、Vulkan 延迟 5 帧/最多 10 条及负 margin 转无符号的边界。
- 第 170 章已完成：由 50 个二级标题、1394 行收束为 16 个二级标题、848 行，以“一份多阶段 dump 能证明什么”为主线，拆清 priority/权限/TimedLock/单次 write、current/drawing/output 四层、Proto writer/parser、每显示可见性、buffer/region/HWC 及跨系统诊断。
- 技术修正：明确 CRITICAL 丢弃剩余参数而 HIGH/NORMAL 为空实现；锁超时后仍无锁读取且最终 write 错误不上传；parser 保留 id 连树但文本不打印，Region this 来自未初始化值；requested/effective 差异可由父变换造成；缺失 active_buffer 在文本中退化为全零；queued_frames 只由传统 BufferQueueLayer 覆盖，BufferStateLayer 的 0 不能排除待处理 buffer；Proto HWC type 仅为默认 Display，普通 proto 又不含 offscreen tree。
- 第 171 章已完成：由 50 个二级标题、1107 行收束为 16 个二级标题、681 行，以“同步截图完成到哪里”为主线，拆清 Java/JNI/Binder、Display/Layer RenderArea、drawing-state 再合成、draw fence、secure/protected 策略与截图/显示双证据轴。
- 技术修正：明确 Display 截图会排除 DONT_SCREENSHOT/mPrimaryDisplayOnly Layer；ColorMode 查询与 capture 非原子；任一尺寸为零会重置两维且 EAGAIN 重试无上限；drawLayers 与 sync_wait 错误不上传；system/unsafe 仍不保证 secure/protected 像素导出；Java captureLayers 实际跳过根层；补出 current secure/crop、drawing layerStack/遍历与 current parent exclude 的混合代际，以及 frameScale 非有限值、format 忽略、JNI release 和负 exclude count 缺口。
- 第 172 章已完成：由 50 个二级标题、1103 行收束为 16 个二级标题、667 行，以“WMS 状态怎样成为可对齐但非原子的窗口时间线”为主线，拆清 shell session、transaction/frame 两种触发、DisplayThread callback、global-lock 快照、Proto 树、按字节 FIFO、落盘与 Winscope 对齐。
- 技术修正：明确 transaction 只覆盖 WMS 自己的 close 点且由触发线程序列化；entry timestamp 早于等待 global lock；frequency/level/size 都会清历史；size 缺非正与溢出校验；单条超限可令 frame 的 mScheduled 不复位；切模式与 stop 不取消旧 callback，也没有 in-flight flush 屏障；stop 的成功文案不能证明文件写成；CRITICAL 可留下空 child 包装；Winscope 在另一轨首样本晚于所选时刻时仍可能选 index 0 的未来样本。
- 第 173 章已完成：由 50 个二级标题、1069 行收束为 16 个二级标题、730 行，以“屏幕点怎样从 SF drawing tree 的输入快照选中窗口并成为局部坐标”为主线，拆清 handle/channel/快照三完成点、WMS AnimationHandler 合并、Layer transaction、SF fillInputInfo、Dispatcher 焦点与 TouchState。
- 技术修正：明确 updateInputWindowsImmediately 只立即重建并 merge；null-token invalid overlay 在 SF 会重新走无真实 InputInfo 默认填充；SF 到 InputManager 的 setInputWindows 与反向 listener 都是 oneway，正常同步靠独立回调且 Binder 发送失败只能等超时；只有本次 map 出现的 display 被替换，移除另走显式空列表；窗口 handle 复用要求 id+token；三类焦点互相独立；modal 空 region 仍可截触摸、跨 UID OUTSIDE 会清坐标；普通 MOVE 粘住 DOWN 目标；复杂旋转的 region/scale 不是完整逆矩阵，global scale 不重复乘 X/Y；sync 另有五秒超时、SF 主线程不等和服务缺失边界。
- 第 174 章已完成：由 50 个二级标题、1143 行收束为 16 个二级标题、662 行，以“一笔已选中目标的输入怎样跨 socket 并靠 FINISHED 结账”为主线，拆清 channel 所有权、seqpacket ABI、三类编号、outbound/wait、App Looper、InputStage、batch seq chain、ANR 与连接清理。
- 技术修正：明确四次 32 KiB setsockopt 都忽略返回码；r48 producer 允许多笔在途，Java注释的一发一回不是实现事实；Java callback 异常会自动以 false 完成同轮后续事件，而其直接 send 及 CANCEL 清 batch 路径都不接 mFinishQueue，反向 WOULD_BLOCK 可漏回执；同轮多个 FINISHED 共用一次 now；有 future 的插值只有 2ms 下限而无 20ms 上限，2—20ms 与 8ms cap 只约束历史外推；ANR 从成功 publish 开始，2秒 slow log、5秒默认 timeout、unresponsive/BROKEN/ZOMBIE 分属不同语义；WAIT_FOR_FINISHED 可因异常清理归零且不等 handled 或 present。
- 第 175 章已完成：由 50 个二级标题、993 行收束为 16 个二级标题、737 行，以“一笔事件何时才算真正完成”为主线，拆清 ViewRoot pending/阶段账、pre/post-IME 入口、IME defer、Window.Callback 回环、TouchTarget、intercept、split、View 消费顺序与 click 延后。
- 技术修正：明确 root removed 会连终止事件一起丢，而 focus/stopped 等分支会把终止事件 cancel 后继续；IME 2500ms 超时以 false 放行且迟到回执被忽略，不重置外层 Dispatcher deadline；async stage 只按 deviceId 防越序；父中途拦截的当前 Motion 只以 CANCEL 发给旧 child，不在同轮再交父；新 DOWN 会清 disallow-intercept；多指找不到新 child 时归最早 target；r48 obscured 默认过滤只查完全遮挡 flag；disabled clickable 仍可消费且 TouchDelegate 位于 onTouchEvent 内；正常 click 的 post 可晚于 FINISHED。
- 第 176 章已完成：由 50 个二级标题、1088 行收束为 16 个二级标题、639 行，以“publish 前后的两类输入 ANR 怎样计时、归因与恢复”为主线，拆清 no-focus pending、触摸剪枝、500ms Key 门、connection deadline、AnrTracker、WMS/AMS policy、InputState 取消与迟到 FINISHED。
- 技术修正：明确 paused focused window 是无独立 ANR deadline 的 publish 前门；剪枝不会直接清 no-focus timer且每笔 blocked drop 会按类别取消所有 connection；500ms Key 门实际查看全局 responsive tracker而非仅早期 Motion；同轮 publish 可共享 currentTime；正常 timeout 到 onAnr 之间无 FINISHED 竞态；InputState 在 entry 入 outbound 时已更新，CANCEL_ALL 不清旧队列或断通道；positive extension 与 late FINISHED 都可能令 surviving wait entry 未重新入 tracker；responsive 比较与 ANR 触发还有 `<`/`>=` 边界；r48 frozen 外层仍会调用 processAnrsLocked。
- 第 177 章已完成：由 50 个二级标题、928 行收束为 16 个二级标题、585 行，以“inject 返回值究竟证明到哪里”为主线，拆清三种同步模式、Binder 身份保存、目标 ownerUid 权限、policy/filter、Motion history 展开、InjectionState 引用与 foreground 完成账。
- 技术修正：明确“自己的窗口”按 ownerUid 而非进程或包判定，shared UID 可局部放行但不会获得 TRUSTED；r48 带 history 的 Motion 只有最后一项持有 InjectionState，早期 sample 不逐项携带 UID 检查和同步结账；WAIT_FOR_RESULT 的 success 早于 publish 且 policy consume 也可产生；WAIT_FOR_FINISH 不计 monitor/OUTSIDE/wallpaper，Key fallback 会延长计数；broken/unregister drain 可结账而 global reset 本身不 drain connection 队列；30 秒 timeout 不撤销事件，HMAC 也只证明系统签名字段而非物理来源。
- 第 178 章已完成：由 50 个二级标题、882 行收束为 16 个二级标题、713 行，以“原始输入被截走后，什么事件重新回到 Dispatcher”为主线，拆清 native boolean、Handler 所有权、root ASYNC reinjection、FILTERED、顶层 reset、per-display transformation、Key 聚合与 gesture 调度。
- 技术修正：明确 IInputFilter AIDL 本身为 void，false 只证明 IMS 已交接；JNI 异常可 fail-open 而 IMS RemoteException 仍吞事件；默认透传会把 deviceId 改成 VIRTUAL_KEYBOARD_ID；非 null replacement、feature 内部重建及 disable 竞态都不是严格原子切面；Touch/Mouse gate 未按 device/display 分账且常规 UP 不关门；Key 的 500ms 只是主 Looper 定时，零服务接受时 boolean 又被忽略，KeyboardInterceptor 的空 onDestroy 也不清旧队列；r48 Autoclick 点击与 MotionEventInjector CANCEL 均用默认 display 的 obtain 重载，副屏会丢 displayId；gesture success 不证明 host 或 App 收到。
- 第 179 章已完成：由 50 个二级标题、901 行收束为 16 个二级标题、761 行，以“一个 evdev 节点怎样成为可派发的逻辑输入设备”为主线，拆清 EventHub 打开与能力探测、descriptor/合并、Mapper 选择、配置与启停、reopen、Reader 处理和 listener flush。
- 技术修正：明确 nonce 只在 uniqueId 为空时参与唯一化，按键映射文件可由多级 basename 回退混合组成；跨 fd 读取不保证全局时间排序，合成事件列表的容量控制还有极端越界风险；首轮策略禁用会在 Mapper 配置前先关设备，复合设备启用又忽略子设备失败且无 epoll 回滚；动态 EventHub/Framework id 都会重分配，全局与单设备 generation 的增长条件不同；`SYN_DROPPED` 门属于整个逻辑 InputDevice，另一子设备的 `SYN_REPORT` 也可清门；V4L 关联只是按名字匹配的启发式。
- 第 180 章已完成：由 50 个二级标题、1061 行收束为 16 个二级标题、864 行，以“一笔 EV_KEY 怎样成为稳定的 KeyEvent”为主线，拆清一次性 HID usage、KCM/KL 映射、overlay、按下记录、旋转/display、meta/LED、policy flag、repeat、fallback 与状态查询。
- 技术修正：明确 combined KCM 先于 KL 且命中会令 KL flag 消失，overlay 以指针而非内容判变且同 keyCode behavior 整项替换；`mKeyDowns` 只按 scanCode 锁住 keyCode，不保存 displayId、policy/meta 或逐键 downTime，usage-only 的 scanCode=0 还会碰撞；stem 映射为不复位的进程级 static；FUNCTION flag 不等于 FUNCTION 键状态，外接键默认 WAKE 的媒体排除也只是硬编码子集；EventHub 无视 `handlesKeyRepeat` 仍尝试关闭内核 repeat，而该参数又令残存 raw repeat 保持 count=0；普通 policy fallback 有两层弱键缓存，显式 App synthetic fallback 不锁存且丢 displayId；DeviceReset 不单独清 Dispatcher 的全局 repeat 与 Meta replacement 表。
- 第 181 章已完成：由 50 个二级标题、972 行收束为 16 个二级标题、720 行，以“同一包 REL/BTN 怎样按 mode 形成三套坐标、按钮与路由语义”为主线，拆清 capability/配置、accumulator、位移变换、共享 PointerController、按钮时序、侧键双路、滚轮与 Pointer Capture 控制面/数据面。
- 技术修正：明确普通 mouse 的 Mapper X/Y 是显示逻辑坐标而 App 通常见窗口局部坐标；同包轴与按钮只保留最终值，SYN_DROPPED 后首枚恢复 SYN_REPORT 又会被吞；多按钮实际按较大 mask 先处理，`mDownTime` 在 UP 后仍可残留；侧键 Motion/Key 不保证同窗且 Key 可进入 repeat；Capture 回调与 Reader 切换无握手，relative 保持 displayId NONE；动态 `NotifyDeviceReset` 不调用 Mapper reset，会保留按钮、downTime、accumulator 与速度历史，并按逻辑 deviceId 影响复合设备下游状态；退出 Capture 也不主动 unfade。
- 第 182 章已完成：由 50 个二级标题、791 行收束为 16 个二级标题、640 行，以“设备配置怎样建立坐标契约，RawState 又怎样经 pending 门成为 CookedState”为主线，拆清 Single/Multi 分工、首次与热配置、deviceType/mode/source、viewport/natural surface、坐标与辅助轴校准、四层状态、外接笔融合和 mode 分流。
- 技术修正：明确 App `getRawX/Y` 也不是 evdev raw；DISABLED 早退保留非零 source却不发布 ranges，并需区分外层 port 禁用的真实 reset；`touch.displayId` 只为 touchscreen 读取且动态 port 不重算 hasAssociatedDisplay；surface geometry 只随 viewport 重建，UNSCALED/NAVIGATION 的 right/bottom 还可能未初始化；旋转反向轴存在一单位 range 越界；size scale/bias 不作用于 AXIS_SIZE，pressure 不减 min或 clamp；pending raw 的时间钳制不覆盖 stylus-only 合成；动态 NotifyDeviceReset 不清 Mapper 状态，source-only 更新又不刷新 range source/本设备 generation。
- 第 183 章已完成：由 50 个二级标题、661 行收束为 16 个二级标题、714 行，以“四类编号怎样跨协议、映射与事件打包形成 App 可用身份”为主线，拆清 Protocol A/B、slot 生命周期、tracking 直映射、距离回退、两次压紧、action index、Palm 与 reset。
- 技术修正：明确 B 对任意负 trackingId 释放，A 可带 trackingId 直映射，B reset 只查询 current slot 而不重建整表；补出异常 SYN_MT_REPORT、release 后轴复活、直映射失败会清持久 bitset 并令下一帧重建、duplicate tracking 的集合折叠；纠正 fallback 同帧复用旧 ID 后可能只发 MOVE、toolType 门不阻止 fresh ID 跨工具复用；限定 UP→可选 MOVE→DOWN 只属于非 POINTER touch，并揭示 Palm 在 POINTER 模式置 abort 后既不检查也不按空帧清除；同时明确 SYN_DROPPED 吞到且包括恢复 SYN_REPORT，后续 B 增量不能自动恢复未更新 contact。
- 第 184 章已完成：由 50 个二级标题、443 行重排为 16 个二级标题、1003 行，以“Cooked finger 集合怎样经 usage 仲裁、九态分类与事件展开形成鼠标式事件流”为主线，拆清 HOVER/TAP/BUTTON、PRESS 分类、SWIPE/FREEFORM 双轨坐标、QUIET 与 finish/cancel。
- 技术修正：明确正常 AOSP tap-drag/slop 通常为 300ms/20px；settle 不延迟分类且重建 PRESS 后同帧仍可 FREEFORM；补出 BUTTON 只认三键且受 QUIET 压制、TAP 的抬手 DOWN/downTime、SWIPE 不移动 PointerController、FREEFORM 同帧不复用刚释放 id、QUIET 无 timeout、普通拓扑的坐标快照、Palm latch 与 reconfig/reset 边界。
- 第 185 章已完成：由 50 个二级标题、404 行重排为 16 个二级标题、1052 行，以“一帧 RawPointerData 怎样在 cooking 前改道成 Virtual Key，或继续成为带校准 axis 的触摸状态”为主线，拆清 parse/resolve/surface 三层、size 双链、pressure/orientation/coverage、sysfs/KCM/KL、入场门、持键、quiet 与 reset。
- 技术修正：明确 calibration 只在首配解析、range 不负责 clamp、orientation NONE 的旋转异常、coverage 与 size 独立且 180°/270° translate 不对称；补出 Virtual Key 多指只看最低 id、active 只按 keyCode 保持、ignored 锁存、global quiet 直接覆盖、Dispatcher 可 repeat，以及动态 NotifyDeviceReset 不清 mapper key latch。
- 第 186 章已完成：由 50 个二级标题、358 行重排为 16 个二级标题、1148 行，以“一帧 MotionEntry 怎样结合旧 TouchState，生成本帧每个窗口的 InputTarget，并决定哪些窗口—pointer 关系延续到下一帧”为主线，拆清 display 级路由账、modal/Region/portal 命中、四类接收者、提交矩阵、split 投影、slippery、hover 与动态窗口更新。
- 技术修正：明确 Region 同为左上闭右下开、Case 1 与 slippery 的 mouse/health/portal 差异、monitor-only OUTSIDE/permission 边路、split count 相等仍可坐标归一、遮挡真实谓词与 flags 累积；补出失败可提交带 OUTSIDE 的 down state、DeviceReset CANCEL 不清 TouchState、portal destination global monitor 缺终帧，以及 destination 窗移除可能暂时漏清 origin-keyed 路由。
- 第 187 章已完成：由 50 个二级标题、341 行重排为 16 个二级标题、1355 行，以“多条独立 InputMessage 怎样在 App 端成为一份按帧消费、可重采样且仍逐 seq 结账的 MotionEvent”为主线，拆清 Batch 状态机、compatibility 与字段头、frame cutoff、Choreographer/unbuffered 调度、history/current、seq chain、两槽 raw history、插值外推和 rewrite。
- 技术修正：明确单 sample full-flush 没有 history、5ms cutoff 先于 action/source 资格且覆盖 HOVER/非 pointer MOVE、跨组事件可越过 Batch、displayId 不参与 Batch/TouchState key；补出 pointer CANCEL 丢批的 FINISHED 无背压重试、接收硬错误可被 WOULD_BLOCK 覆盖、JNI consumedBatch 位测试的 false positive/异常 false negative，以及静止复用会复制旧 PointerCoords 全部 axes、rewrite 清 bit 还要求时间追平。
- 第 188 章已完成：由 50 个二级标题、354 行重排为 16 个二级标题、1703 行，以“一条 Java finishInputEvent 怎样跨三套编号与两端队列，最终按 seq 关闭 Dispatcher 等待账”为主线，拆清 outbound/wait、mSeqMap、ViewRoot/compat、mFinishQueue、batch seq chain、Command、fallback、ANRTracker、InjectionState 与 teardown。
- 技术修正：明确 Java mSeq 可为 0/负数且 map 只认编号不验 identity，Motion map miss 仍可能 recycle/double-recycle；补出 batch 部分发送、direct-send/INPUT/OUTPUT/HUP 的无重试边界、重复同-seq 回执可误结算 Key fallback、普通 FINISHED 恢复不重建旧 tracker，以及 standalone BROKEN、unregister ZOMBIE、cancel、drain、reset 和 WAIT_FOR_FINISHED 的不同完成语义。
- 第 189 章已完成：由 50 个二级标题、343 行重排为 16 个二级标题、1456 行，以“一条 connection 的提前 InputState 怎样合成取消与补 DOWN，又为何不等于 App 已收尾”为主线，拆清 Key/Motion memento、筛选矩阵、合成字段、call-site 范围、pilfer、transfer 与 fallback。
- 技术修正：明确 tracking 早于 outbound push/publish，普通 Motion MOVE 只刷新 pointer 数组，synthesize 本身不 reset 且正常 wrapper 在 publish 前消账；补出 ZOMBIE stranded-entry 风险、宽取消范围、pilfer 不筛 source/hover、transfer 先改路由且 merge 不比 hovering/不查容量，以及 r48 标准 policy fallback restart 绕过 tracking、保留 original memento 并用陈旧 resolvedFlags 发布的实现断接。
- 第 190 章已完成：由 50 个二级标题、455 行重排为 16 个二级标题、1144 行，以“退出动画仍可见时输入为何已经消失”为主线，拆清五层生命周期、pair/token/fd、transfer/Parcel/dup、普通与 dead-window 分支、注册双表、SF 路由快照、handle 复用、focus/hover/touch 收尾，以及 unregister、BROKEN、ZOMBIE 与 FINISHED 的不同完成点。
- 技术修正：明确 token 不唯一标识 input window、portal 可使用无注册 channel 的独立 Binder，`id + token` 才是旧 handle 复用键；补出 multi-display 更新锁外原子可见但全局 hover 会被其他 display 清除、同-token 窗口属性反查有歧义、`releaseChannel()` 只清 handle token、unregister 必须传实际注册 fd、普通与 dead-window 的端点销毁顺序不同，以及 send fatal 保留 BROKEN、receive/HUP 立即 ZOMBIE、drain 可让 WAIT_FOR_FINISHED 保持成功、stale ANR tracker 可留下 `responsive=false` 和 FINISHED policy 重入可能向 retained ZOMBIE 追加未派发 raw entry 等 r48 边界。
- 第 191 章已完成：由 50 个二级标题、374 行重排为 16 个二级标题、1225 行，以“App 收到 CANCEL 后，gesture monitor 为什么仍未独占整条手势”为主线，拆清 global/gesture 两种选择时机、创建与 capability、注册/投递四层、portal 坐标、FINISHED/ANR、pilfer 校验与异步完成点，以及 dispose/HUP/unregister。
- 技术修正：明确 global 仅在成功 key/motion 后追加，gesture 只在 ACTION_DOWN 快照；补出无窗口 gesture-only 注入权限缺口及 ASYNC/policy-consumed 假成功、portal registration-display pilfer 失配、pilfer 只清 windows/portal 并保留 all monitors/down/split、后续 POINTER_DOWN 可重加窗口、stale TouchState、non-FG fallback 与 oneway/JNI status 丢失等 r48 边界。
- 第 192 章已完成：由 50 个二级标题、367 行重排为 16 个二级标题、1243 行，以“一次屏幕旋转为什么只投递 change bit，却能让 TouchInputMapper 重写坐标”为主线，拆清值、失效与观察三条通道，串起 DMS/Java/JNI 快照、Reader mailbox/reopen、viewport 查询、port 关联、坐标几何与 generation 完成点。
- 技术修正：明确 changes=0 的三层语义、changesToString 漏掉 POINTER_CAPTURE、excluded 名单只在 openDeviceLocked 过滤及 MUST_REOPEN 无 stock 生产者；补出 Java valid 不跨 JNI、callback 逐字段失败、port 缺失设备级禁用与 unique/type 缺失 mapper 级禁用、运行时 port 无法重算 mapper 首配参数、inactive 仍可选中、touch.displayId 实为 touchscreen uniqueId、mode-only same-viewport 可沿用旧几何、NotifyDeviceReset 不调用 mapper reset，以及 generation 不是语义 diff。
- 第 193 章已完成：由 50 个二级标题、384 行重排为 16 个二级标题、1475 行，以“一笔触摸到底在哪些线程上前进”为主线，拆清 system_server 内 Reader/Dispatcher、classifier sidecar、policy command、WMS→SF 窗口快照、InputChannel/App Looper/FINISHED、注入/filter、pointer、Watchdog 与 dump 的观察边界。
- 技术修正：明确 nativeInit、nativeStart 与初次扫描通知是三个完成点，inputflinger 的 addService 返回值未检查；补出 QueuedInputListener 无消费线程、InputFilter 分叉、各类 command 不同的解锁后协议、AnimationThread pending transaction→SF drawing tree→oneway Binder 窗口链、syncInputWindows 无变化短路与 5 秒超时、Java finish 静默失效分支、viewport 回调与三段锁顺序、EventHub 条件唤醒、非原子 dump 及不完整 standalone host 边界。
- 第 194 章已完成：由 50 个二级标题、343 行重排为 16 个二级标题、1292 行，以“一份静态输入快照能把点了没反应证明到哪个完成点”为主线，拆清 Java/native 拼接与锁域、EventHub/Reader/Mapper/Classifier、Dispatcher 路由、五种队列视图、connection ANR、Last ANR、构建差异、真机采集与三棵诊断树。
- 技术修正：明确进程级 dump-disabled、DUMP permission 与整体 native string 是三层入口；EventHub 设备记录不证明 raw，IDC 路径不证明解析成功，ignored 设备仍会进入 Reader dump；补出 Classifier 全局旁路与同步 HAL ping、无焦点活动计时器不打印、Recent 不是完成队列、Wait 只证明 send 且 unhandled Key 可回到 Outbound、user build 事件脱敏、标准 inputflinger 空 dump、dumpsys 客户端 timeout/退出码及 trace counter 非完备边界。
- 第 195 章已完成：由 50 个二级标题、361 行重排为 16 个二级标题、1327 行，以“一根 Protocol B 手指怎样从 EV_ABS / SYN_REPORT 走到 Window A 的 FINISHED”为主线，拆清两套 deviceId、slot/trackingId/pointerId、Reader deferred queue、Classifier、TouchState、DispatchEntry、App batching、View TouchTarget 与回执完成点。
- 技术修正：明确 EventHub monotonic ioctl 只请求不兜底、read batch 不等于 SYN 边界、Mapper last 早于 listener flush；补出 filter 放行实为异步重新注入、paused 首候选不会穿透、失败 DOWN 仍可能提交空目标 TouchState、Connection InputState 早于 publish、global scale 不缩 X/Y、batch 首 eventId/末 seq chain、touch 跳过 pre-IME、UP 在 publish 前清 Dispatcher TouchState，以及 FINISHED 背压、command 和 timeout 不清原 wait 的边界。
- 第 196 章已完成：由 50 个二级标题、344 行重排为 16 个二级标题、949 行，以“一枚实体 Key 怎样跨映射、repeat、两次 policy、IME、FINISHED 与 native fallback 完成两段式清账”为主线，拆清两套 deviceId、KCM/KL、Mapper 状态、两类 repeat、焦点门、每目标 seq、App InputStages 与回执。
- 技术修正：明确 KCM-first/KL-second、共享 downTime 与局部 meta；补出 InputFilter 异步重注入、repeat 只在无 pending 且 inbound 为空时合成、policy token 只是焦点快照、前序事件门看全局 ANR tracker；核准 fallback 原地复用 KeyEntry/DispatchEntry/seq、陈旧 resolvedFlags、取消 memento 缺口，以及 fallback 改写 repeat seed 的时序分叉。
- 第 197 章已完成：由 50 个二级标题、377 行重排为 16 个二级标题、897 行，以“按值、所有权与完成账寻找第一处错误输出，再选择作用域最小的根因修复层”为主线，归并坐标、Virtual Key、多指、触控板、Key、窗口、队列、View、注入与多显示十五类现场，并提供九组只读源码练习。
- 技术修正：区分持久化 TouchCalibration、IDC 与 viewport，补出 Virtual Key 同帧重判、16 指上限与 0..31 ID 域、触控板 gesture/光标双轨；收窄 repeat、DeviceReset、focus、pilfer、window teardown、ANR extension 与 injection sync 的完成语义，并把 View handled、FINISHED 与异步 click 拆账。
- 第 198 章已完成：由 50 个碎片化二级标题、341 行重构为 16 个二级标题、1562 行，以“一个设备受限输入修复怎样成为可测试、可发布、可回滚并可升级的行为契约”为主线，串起最小修改层、补丁单元、Reader/Dispatcher 测试、真机证据和升级去留，并提供九组只读源码练习。
- 技术修正：拆开 IDC/KL/KCM、sysfs virtualkeys 与 descriptor+rotation 持久校准，核准文件名和目录的嵌套搜索及 KL/KCM 分别补齐；区分三种 Reader 测试缝、设备 reset 与窗口 client-channel 取消、FINISHED 发送与 Dispatcher 清账，并揭示 quiet-time fake、Motion 负向 helper、同 UID 注入、非零错误 seq、SYNC_NONE、filtered flag、pilfer 与浅仓库升级证据的边界。
- 第 199 章已完成：由 50 个碎片化二级标题、347 行重构为 16 个二级标题、1517 行，以“一条输入探针怎样同时维护身份、时间、状态与完成账，并沿第一处分歧收敛到最小修改层”为主线，串起数据、控制快照和完成反馈三条链，并提供九组只读源码练习。
- 技术修正：把线性控制点拆成 Reader 的 C-R 与 Dispatcher 的 C-D，核准 RawEvent/EventReader 两套 device id、SYN_REPORT 边界、viewport 与窗口快照、Classifier/filter/policy、TouchState/InputState、monitor/pilfer、注入、channel seq/Java seq、FINISHED、ANR 与 present 的不同所有权和完成点。
- 第 200 章已完成：由 50 个碎片化二级标题、332 行重构为 16 个二级标题、955 行，以“前 199 章的知识怎样变成一套可验证方法”为主线，用 Launcher 冷启动到首帧场景串起 I/A/G/B 偏序检查点、完成债务、证据分级、第一处分歧与后续路线，并提供九组只读源码练习。
- 技术修正：把输入完成、启动同步返回、Zygote parent/child、attach、ClientTransaction、窗口首绘、BufferQueue/BLAST 与 present 拆成不可强行线性化的局部协议；核准 startSeq 与 pending 的移除/attach-first 竞态、Input 窗口经 SurfaceFlinger 的快照所有权、oneway 事务无通用 ACK、HWC/RenderEngine 可同帧共存、present fence 的可靠性/有效性边界，以及 Job、Alarm、WakeLock 和 reportFullyDrawn 不能替首帧结账。
- 第 201 章已完成：由 50 个碎片化二级标题、363 行重构为 16 个二级标题、698 行，以“普通同用户冷启动怎样把目标 Activity 首个可见 Buffer 推进到可归因 present”为主线，串起 ActivityStarter、进程供给、attach/bind、ClientTransaction、真实窗口、G0/G1/B0 与九组只读源码练习。
- 技术修正：拆开内部 START_ABORTED 与外部 START_SUCCESS、普通 Zygote/USAP 的 pid 顺序、pid-first/attach-first、service cache/core-settings/bind/transaction 主队列；核准 HWUI/software/SurfaceHolder 三种绘制反馈、finishDrawing 到 AnimationThread placement、starting window 异步移除、传统/BLAST、可见 OutputLayer 与 presentOrValidate/presentDisplay 两条 HWC 完成链。
- 第 202 章已完成：由 50 个碎片化二级标题、336 行重构为 16 个二级标题、604 行，以“一笔普通 Launcher 请求怎样收敛为唯一启动处置”为主线，串起客户端输入、身份与结果、resolve、安全门、系统改写、候选 ActivityRecord、Task 复用/新建、返回码与九组只读源码练习。
- 技术修正：核准 MAIN/LAUNCHER 的 NEW_TASK|RESET_TASK_IF_NEEDED 与 source/result token 分账、resolveForStart 和 Resolver/Chooser；拆清 manifest/AppOps、IFW、PermissionPolicy、SafeActivityOptions、BAL 与 app-switch，明确重定向清 grants、候选记录尚未入 Task、两阶段 LaunchParams、recycle/deliver、新 Task 二次 lock-task、mMovedToFront、复用/新建 URI grant、mDoResume 与失败清理调用域。
- 第 203 章已完成：由 50 个碎片化二级标题、336 行重构为 16 个二级标题、610 行，以“目标 ActivityRecord 已加入 Task 后，为什么仍可能不是 focused、RESUMED、visible 或 drawn 的对象”为主线，用 D0 的 A_old/B_target 与 D1 的 C_side 串起容器父子、Task identity、五类 top、六层 focus、ActivityState、可见/绘制六字段、跨 Display resume、pause/process 分流、reparent/remove/进程死亡与九组只读源码练习。
- 技术修正：明确 ActivityStack 继承 Task 且 resumed/pausing 字段声明在 Task；直接 parent/inHistory 不等于 live attach；拆开 focused stack、mFocusedApp、mCurrentFocus、mTopFocusedDisplayId 与 top-resumed；核准 Root resume 的局部 resumedOnDisplay/返回 result、不构造 ResumeActivityItem 的 attached 早退、oneway ClientTransaction 服务端返回边界、startSpecificActivity 的现存 thread/新进程两级分流，以及 mDrawn/allDrawn、finish/Task remove、removed-process 与 retained dead-window 的独立完成语义。
- 第 204 章已完成：由 50 个碎片化二级标题、326 行重构为 16 个二级标题、690 行，以“A_old 必须让位给 B_target 时，两套不共享内存的状态机怎样完成交接、每个完成点只证明什么”为主线，用五本账与七个检查点串起 Pause/Resume 两轮事务、反向回报、timeout、top-resumed 和九组只读源码练习。
- 技术修正：拆开 IApplicationThread oneway 下发与 IActivityTaskManager 同步反向回报，核准全部 preExecute 先于 H 入队、callback/final-state 补路及 r48 ActivityResult 无 post state；明确 pause timer 在发送返回后起算、正常回报与 timeout 竞争同一锁、未 attached/调度异常不直接写 PAUSED，以及 pause 与 top-resumed 回报都缺少完整代际身份；补出动态重选 next、服务端先写 RESUMED、无 Resume item 的早退、schedule/complete 两级异常、completeResume 与客户端并发、activityResumed 的弱证明、idle 与 drawn/present 的独立完成语义。
- 第 205 章已完成：由 61 个碎片化二级标题、545 行重构为 16 个二级标题、674 行，以“已通过 attach 身份校验的通用进程怎样成为可创建 B_target 的应用进程”为主线，用八本账与十一个检查点串起 oneway bind、主队列、Application/Provider 装配和九组只读源码练习。
- 技术修正：明确服务缓存由 Binder 线程直接初始化、core settings 另投 H 消息，同一 IBinder 的 oneway 只保证局部有序且可与服务端并发；核准 LoadedApk 早于 debugger/proxy 取得、两个 App Context、Application attach 先于 Provider、Provider 按 initOrder 创建并同步发布后才走 Application.onCreate；补出 publish 可见早于 Application 回调、restricted/pre-boot/isolated 分流、异常处理差异、字体尾部，以及 r48 ThreadPolicy 身份 equals 令 target≥27 的 StrictMode 恢复支路实际不成立。
- 第 206 章已完成：由 71 个碎片化二级标题、603 行重构为 16 个二级标题、684 行，以“Zygote 的 pid 结果与 child attach 乱序时怎样收敛到当前启动代”为主线，用八本账与九个检查点串起两层异步、启动参数、三类供给入口、pid-first/attach-first 和九组只读源码练习。
- 技术修正：明确 `knownToBeDead` 不覆盖已有 pid、尚未 attach 的复用，`requiredAbi` 与 `instructionSet` 使用不同回退链，private-data 与 storage-dir 是两套挂载账，USAP 是 regular 全局 Zygote 内部优化而非平级入口；核准 pid 回复可早于 child specialize、attach-first 从 pending 表补登记且不建 timeout、pending 缺项不等于取消、结果代际检查是有方向的 `>`，以及 timeout 起点、迟到清理和 pid 表删除的 startSeq 防护。
- 第 207 章已完成：由 80 个碎片化二级标题、606 行重构为 16 个二级标题、780 行，以“system_server 已读到 pid 而 attach 尚未出现时，这份 regular/USAP pid 证据最多证明什么”为主线，用八本账与九个完成点串起命令授权、fork/specialize、RuntimeInit、Binder pool 启动请求、入口类初始化和 Runnable trampoline，并提供九组只读源码练习。
- 技术修正：明确无 wrapper regular 的父侧 pid 写入与 child specialize 无全序；USAP 只保证 producer 写 pid 早于本次 specialize，客户端读取时刻不设 child 上界；wrapper 的 `usingWrapper=true` 硬证 direct child 已过 S0/D0 与报告 pid 的后代关系，却不认证 inner B0；同时核准 parser 未识别参数、pre/post-fork hooks、FD detach/reopen、完整 capability bounding set、seccomp/SELinux、Binder worker 非 ready、child-Zygote range 上界缺口及 `mIsForkChild` 异常边界。
- 第 208 章已完成：由 64 个碎片化二级标题、519 行重构为 16 个二级标题、706 行，以“同步 attach、异步 bind 与主 Looper 怎样汇合”为主线，用十个完成点拆开 Binder 驱动接纳、Stub 执行、主队列入队与 dispatch，并提供九组只读源码练习。
- 技术修正：明确 bind oneway 的驱动接纳点与发送端代理返回、目标 Stub 执行无全序，`Q_bind` 与同步 attach reply、Looper 启动也无全序；补出 Binder worker 先直接初始化 ServiceManager cache、再排 `SET_CORE_SETTINGS → BIND_APPLICATION`，并限定同一 `IBinder` oneway 局部顺序、MessageQueue when/front/barrier、pool worker ready、Runtime crash token、agent/isolated/systemMain、attach 失败族与 r48 uptime/elapsedRealtime 混合时基。
- 第 209 章已完成：由 75 个碎片化二级标题、581 行重构为 16 个二级标题、659 行，以“开发者看到 onCreate 末尾日志时，客户端创建链究竟交付到了哪个完成点”为主线，用十三个完成点串起 launch 提交、ActivityClientRecord、Activity Context、Factory 实例、attach、create commit、Start/Resume 与客户端主窗口 add，并提供九组只读源码练习。
- 技术修正：限定普通远端 App 与 system-process 本地 IApplicationThread 的不同证据边界；明确 HardwareRenderer.preload 会创建/启动 RenderThread 并异步排图形驱动预载，activity token 与 ClassLoader 都不属于 ResourcesKey；补出严格关系 A_attach＜子类 onCreate 末尾日志＜O_create＜M_commit、mWindowAdded 早于 addView、starting/snapshot Window 可先存在，以及独立 network block seq、默认/自定义 Factory、pre-destroy、persistable、final Pause、preserved Window 与 Instrumentation 异常分支。
- 第 210 章已完成：由 72 个碎片化二级标题、825 行重构为 16 个二级标题、599 行，以“Activity.setContentView(int) 正常返回时究竟交付了哪一层”为主线，用十四个完成点串起 PhoneWindow、Feature、Theme、Decor、系统骨架、content、业务树、Insets 请求、内容回调、ActionBar 与后续窗口 add，并提供九组只读源码练习。
- 技术修正：限定普通首次、非 preserved、非 freeform、无 window-decor-caption、可见且未 finish 的固定主线；明确 theme id 非零调用门、getWindowStyle 缓存与 targetSdk/forced 优先账，DecorContext 不切断 Decor→PhoneWindow→Activity 强链，标题只在 mTitle 非空时条件写；补出 preserved 清理重绑、NoDisplay 双账、首次 Scene 无动画与 pending guard、未 attach Insets 请求不持久、callback/explicit/ActionBar 异常边界，以及 ViewRoot schedule、WMS add、pre-draw、draw 和 present 的分层完成点。
- 第 211 章已完成：由 88 个碎片化二级标题、931 行重构为 16 个二级标题、614 行，以“inflate(resource, root) 正常返回时，R.layout 怎样变成带 Context、LayoutParams 与父子关系的 View 树”为主线，用十六个完成点串起 Activity Inflater、资源选择、XmlBlock/Parser、Factory、默认反射、递归、外部 add 与后续窗口显示，并提供九组只读源码练习。
- 技术修正：明确 Activity 通过 ContextThemeWrapper 缓存 PhoneLayoutInflater clone，XmlBlock 四槽缓存不等于 Parser 缓存且 native 生命周期按 open count；核准 advance 只向前找事件、根与后代 Context 传播、标签 Theme 只是 Factory 输入、blink/Factory2-or-Factory/private/default 的条件链、clone 后 setFactory/setFactory2 的不对称、public 二参构造、raw name constructor cache 与 Filter 旁路；补出 root×attach 四行矩阵、LayoutParams 生成/安装时差、顶层与 include-merge 的 onFinishInflate 差异、ViewStub/测试预编译边界，以及异常无事务回滚与已 attached root 可在返回前同步 attach。
- 第 212 章已完成：由 93 个碎片化二级标题、947 行重构为 16 个二级标题、693 行，以“普通首次 Activity 的 WindowManager.addView() 正常返回时，App 与 system_server 各自已经交付到哪一层”为主线，用十八个完成点串起 INVISIBLE Decor、本地三联账、同步 add Binder、WindowState/InputChannel、客户端 parent/input pipeline、makeVisible、首次 relayout 与首帧，并提供九组只读源码练习。
- 技术修正：明确冷路径 ViewRoot 构造前可先经历 openSession，Activity mWindowAdded 与 WMG 三联账均非成功回执；核准 receiver→assignParent→flags→stages→PendingInsets、InputChannel 服务端本地 transfer/map 与稍后 reply Parcel、WMS token-first Display 选择、early-return identity、no-error 意图线、普通主窗 Policy 后置空操作和 InputMonitor 异步快照；拆开 0x4 成功位别名、recent-layout frame hint、container-layer SurfaceControl 与 relayout buffer surface；补出负码、RemoteException、SecurityException、客户端回调/并发、fresh index=-1 不回滚、pre-draw 取消，以及客户端 immediate remove 与服务端 replacement/退出动画延迟清账的边界。
- 第 213 章已完成：由 101 个碎片化二级标题、979 行重构为 16 个二级标题、734 行，以“普通首次可见 Activity 的第一轮 `performTraversals()` 怎样把 App 尺寸提议变成 WMS frame 和可写 Surface，并为何 attach、measure、relayout、layout、draw、`finishDrawing()`、show 与 present 必须分别结账”为主线，用十八个完成点串起 attach、初测、同步 relayout、WMS frame、Surface 接线、补测、layout、draw 回报、show 与 present，并提供九组只读源码练习。
- 技术修正：限定普通首次可见、非 BLAST、硬件加速且回调稳定的固定路径；拆开 add frame hint、App measured size、WMS frame、Buffer layer 与 Java `Surface`，核准 Decor 仅实现接管接口不等于实际接管、NoDisplay 可被 `setVisible(true)` 覆盖、Dialog 三档试探及补测门；明确 `W_layout` 只结算服务端 frame/layout，Insets/control 快照归入 Binder reply，Session 转发与会话状态分账，Renderer 按 initialize/notify、接纳 frame、setup、补测排序；补出透明区域、GlobalLayout、InternalInsets 三套门，硬件/软件/SurfaceHolder draw 回报差异，`finishDrawing()`、`HAS_DRAWN`、show、latch 与 present 的独立完成语义，以及 Surface 失败、回收/自杀、重入和诊断证据边界。
- 第 214 章已完成：由 107 个碎片化二级标题、1116 行重构为 16 个二级标题、967 行，以“普通首次硬件窗口帧怎样从 UI DisplayList 录制，经 RenderThread、BufferQueue 与绘制回报，推进到可归因 present”为主线，用二十二个完成点拆开录制、同步、signal、queue、callback、show、latch 与 present，并提供九组只读源码练习。
- 技术修正：拆开 Q_post/Q_wait/T_sync、signal/UI 恢复与 RT 提交的偏序，核准 DisplayList 复用、staging/current、prepareTextures×canDrawThisFrame、r48 reserveNext no-op、GL/Vulkan 取 Buffer 与两类 fence；补出 ReliableSurface fallback、Vulkan queue 失败仍可能 callback、BufferQueue 接纳/回调/返回、r48 非 BLAST 默认与 BLAST transaction 持有、droppable 未 signal latch、SurfaceView draw debt，以及 frame-complete、show、latch、合成提交和 present 的独立完成语义。
- 第 215 章已完成：由 107 个碎片化二级标题、1041 行重构为 16 个二级标题、785 行，以“一块已 queue 的普通窗口 Buffer 怎样与 show 状态在 SurfaceFlinger 汇合，经 latch、CompositionEngine、HWC 提交到可归因 present”为主线，用二十二个完成点拆开队列通知、状态 commit、Buffer latch、Output 可见性、混合合成与 fence，并提供九组只读源码练习。
- 技术修正：核准 r48 默认非 BLAST 的 BufferQueueLayer 入口、producer 回调与返回的顺序及 INVALIDATE 前的条件偏序；限定普通 head-signaled 路径的连续 latch 上限、DEVICE/CLIENT 混合与 clear-only request；拆开 Composer command staging、validate 结果的本地 apply、present 批量 execute，以及 ready fence 的 pending/signaled/分支、present/release fence 与 callback 的不同完成语义。
- 第 216 章已完成：由 125 个碎片化二级标题、1184 行重构为 16 个二级标题、587 行，以“一个已 attach 的普通 Activity ViewRoot 发出本轮 traversal 请求后，怎样经过单次 app VSync 进入五类 callback，并留下只能覆盖到 HWUI、不能替代 present 证据的 FrameInfo”为主线，用二十四个完成点拆开 barrier、Single 请求、event 回程、五阶段与 UI/RT 分叉，并提供九组只读源码练习。
- 技术修正：核准 requestNextVsync 的 oneway 提交与 SF 置 Single 无同步回执、Single 在 BitTube 写入前已清债；拆开请求前 drain 丢旧 VSync 与 fd drain 分发最新 VSync，限定 pending Java Message 的覆盖语义和 COMMIT 局部校时；对齐 Java 9 项与 native 17 项、条件 measure/layout、逐 event input 时间、IssueDrawCommandsStart 落点、FrameMetrics raw TOTAL、JankTracker 调整、延迟 GpuCompleted 与 present fence 的不同终点。
- 第 217 章已完成：由 115 个碎片化二级标题、1016 行重构为 16 个二级标题、615 行，以“一次 am start -W 普通冷启动怎样从 ActivityMetrics 起点跨进程、Activity、窗口与首帧，并把多个完成口径分别结账”为主线，用二十六个完成点拆开命令计时、分类、Zygote/attach/bind、客户端生命周期、Splash、HWUI/WMS、WaitResult、fully-drawn 与 present，并提供九组只读源码练习。
- 技术修正：核准 cold Message 先投递但 WPC 注册受 ATMS global lock 约束，分类先于普通 Zygote 请求；明确 ActivityManagerShellCommand 与 WaitTime 位于 system_server，拆开 transition-start/pending-draw 双门、reportedDrawn 字段翻转、WaitResult 与异步日志；补出 Splash timeout reason-map 空门、默认非 BLAST callback、mLastTransitionInfo 覆写、reportFullyDrawn 一次尝试以及 WMS drawn 与 HWC present 的独立完成语义。
- 第 218 章已完成：由 105 个碎片化二级标题、725 行重构为 16 个二级标题、593 行，以“Splash、Snapshot、transfer 三条路线怎样完成选择、异步创建与真实首窗交接，并为何 WMS drawn/show 不能替代物理 present”为主线，用二十四个诊断点拆开类型、缓存、主题、两段锁、两类 surface、transition 与清理，并提供九组只读源码练习。
- 技术修正：核准 `newTask`、`taskSwitch`、process thread 与 STARTED—STOPPED 状态门，补全 Home 先清 cache、rotation-only compatibility、APP_THEME/secure/DPM 与设备禁用门的精确作用域；明确 Snapshot 早返而 NONE transfer 仍受 theme 门约束，拆开 Splash/Snapshot 相反的 queue/finish/句柄登记顺序、`Q_take` 前后取消、完整/模型 transfer、startingDisplayed/reason、条件化 remove、450 ms 延迟与 SF/HWC present 边界。
- 第 219 章已完成：由 118 个碎片化二级标题、906 行重构为 16 个二级标题、474 行，以“一次客户端绘制回报怎样从 ViewRoot 债走过 WMS 五态，并为何仍不能证明物理 present”为主线，用十七个完成点拆开请求、绘制、回报、placement、Activity 聚合、show、latch 与 present，并提供九组只读源码练习。
- 技术修正：拆清 FIRST_TIME、resized、BLAST 与显式请求四类来源，核准 reportNextDraw boolean 合并门与无 generation 计数债；明确 HWUI callback 不是 GPU/SF/HWC fence，WindowCallbacks、有效根 SurfaceHolder 与 SurfaceView 分属 latch、根债完成方式和额外计数；补出无效 Surface/软件失败直接结债、callback 漏报/重复/跨代误销、SurfaceView detach 与 Handler-null 内联边界；同时限定同步 void finishDrawing、非 BLAST post transaction、BLAST 同步账、requestTraversal 合并/延迟、Activity allDrawn、HAS/show 失败及最终物理汇合。
- 第 220 章已完成：由 119 个碎片化二级标题、876 行重构为 16 个二级标题、526 行，以“一个 Activity 的窗口怎样分别闭合成组放行、reported 边沿、启动计时与同步等待，并为何都不能证明物理 present”为主线，用十一个完成点拆开 requested/client/committed visibility、两套窗口聚合、Metrics 双门与两张 WaitResult 表，并提供九组只读源码练习。
- 技术修正：明确 mDrawnStateEvaluated 只在换父时清零、main window 预占与 starting 排除；纠正 numVisible 不检查 displayed/policy/shown、nowGone 不等于窗口消失及 drawn/visible 边沿的不对称写序；补出 closing/transfer/dead-window 直接写、transition snapshot 采用 latest 元数据且 pending remove 可无操作、全局 launched 表广播与 Component 表无有效 timeout，以及 deferred fully-drawn 不回到 ActivityRecord wrapper 的边界。
- 第 221 章已完成：由 118 个碎片化二级标题、856 行重构为 16 个二级标题、588 行，以“一个 Display 的旧版 AppTransition 怎样从 prepare/READY 跨过 opening/changing 两层门，选择与提升动画 target，提交三组参与者，并按精确 isAppTransitioning 谓词收尾”为主线，用九个观察点拆开 transit、状态、集合、readiness、可见性提交与结束，并提供九组只读源码练习。
- 技术修正：明确 repeat-hide 提前返回与 onRemoved 先 commit 后入 closing，closing 不参加 ready；核准 opening→changing 短路双检查及 TIMEOUT 空 reasons；补出 default/current Display rotation 混用、wallpaper-target 语义与 500ms 跨批次状态、spec Future 无 generation 污染新批次；纠正 skip 后 UNSET 仍可被 wallpaper 改写、changing Task 用自身 prefix 排序、remote owner 取 animLpActivity、voice 重复 opening；限定 target 提升三类阻断、finish 补偿列表双 producer，以及 isAppTransitioning 检查全动画类型与 waiting-to-start。
- 第 222 章已完成：由 118 个碎片化二级标题、819 行重构为 16 个二级标题、569 行，以“一个传统 Animation 怎样变成 leash 上的逐帧属性，并在结束、取消、转移后安全恢复 Surface 树”为主线，用十个观察点拆开 adapter 选择、资源、setup、SF VSync、finish 与 present，并提供九组只读源码练习。
- 技术修正：明确 remote wrapper、change 与 local 的严格优先级及 organized 只短路 local；核准 transit 与 override type 双状态轴、Keyguard no-animation/fallback null、restrict→initialize→scale 和多套几何；补出 sources 跨 replacement 合并、单对象/runner 两层 delay、Surface-null 未 start 仍可收到 cancel、transfer map 与两次代际校验、freezer snapshot 生命周期、remote snapshot capture-null 空指针尖角、全局 early-wakeup 及 finish 不等于 present。
- 第 223 章已完成：由 124 个碎片化二级标题、1019 行重构为 16 个二级标题、605 行，以“受信任入口怎样把一批 animation leash 交给远端逐帧控制，并在 finish/cancel 后由 WMS 收回”为主线，用十五个观察点拆开身份、setup、oneway start、Launcher RT transaction、同步 finish、pending reset 与 present，并提供九组只读源码练习。
- 技术修正：核准五类入口的权限、caller 身份与 next-start 一次性消费，拆开 Activity/Display Definition 的选择、覆盖和无代际死亡清理；补出 remote-change snapshot capture-null 空指针、主/thumbnail 部分取消空指针、after-prepare 迟到 start、pid=0 晚失败残留、正常 finish 后迟到 cancel与 callback 异常缺口；明确同步 finished、Display pending merge、SF latch/present 无法互相替代，并限定 OOM 分支、SurfaceControl 引用释放及普通 remote 与真正 Recents 控制面的边界。
- 第 224 章已完成：由 127 个碎片化二级标题、996 行重构为 16 个二级标题、700 行，以“一段 Recents 会话怎样交付 Task leash、切换输入并在 finish/cancel 后收回控制”为主线，用十八个观察点拆开 initialized/start、初始 App Task 与 wallpaper/dynamic target 的提交边界、输入四道门、三种重排、screenshot hold 与 present，并提供九组只读源码练习。
- 技术修正：核准 runner oneway 与 controller 同步 capability 的身份边界，补出无 request generation、旧 finish/new start 队列逆序、WPC 与 launch-behind 跨会话串线、初始化取消后的迟到 stack listener；区分初始 App Task setup-before-callback 与 wallpaper/dynamic target 无提交屏障，拆清 consumer 注册/启用/InputWindow/listener、动态 target 无显式 client release；明确双 finish/finish-cancel 竞争、ORIGINAL 仅 stack 级恢复、500 ms wallpaper timeout 与外触发 1 秒 failsafe，并闭合 transfer 早退、runner death、Activity-null 等 screenshot hold 无自愈边界。
- 第 225 章已完成：由 116 个碎片化二级标题、925 行重构为 16 个二级标题、543 行，以“一次上滑位移怎样在 Launcher 中变成 Task leash 的逐帧 matrix、crop、alpha 与 radius”为主线，用十五个观察点拆开手势标量、Recents 几何、target 分派、RT 参数消费、SF 接收/应用与 present，并提供九组只读源码练习。
- 技术修正：拆清 shift、fullscreen progress、TransformParams progress 与 spring scale progress 四套状态；明确 live leash 模拟不读取真实 TaskSnapshot rotation/scale，crop/radius 只逆 preview matrix；核准 Home/Assistant 的 mode 前提、SurfaceParams 的可变快照边界、invalid barrier 丢更新但放行 sequence，以及 release check 无 timeout 的永久卡门分支；修正回 Home helper 把 windowX/Y 写入成员 matrix 的 r48 非对称副作用，并把 spring 三物理动画、独立 workspace 动画、controller finish 与 present 分账。
- 第 226 章已完成：由 99 个碎片化二级标题、749 行重构为 16 个二级标题、754 行，以“抬手后怎样把输入判定、四终点与异步门闩收敛为可安全收尾的 Quickstep 事务”为主线，拆清输入入口、双 slop、deferred/continuation、多指暂停、速度与终点决策、两层状态账、截图门和四类收尾，并提供九组只读源码练习。
- 技术修正：明确 `ACTION_UP` 同步计算候选终点但目标发布与动画可等待 Recents start，`mPassedPilferInputSlop` 只是可预置的客户端门闩；核准一维/二维 slop、raw Y 参与时长计算的侧边导航轴向缺口、HOME spring 不由普通 endShift/duration 直接驱动，以及首次/已 appeared NEW_TASK 相反的 finish/reset 顺序；把 `SCREENSHOT_CAPTURED`、`CURRENT_TASK_FINISHED` 与 controller received 限定为软件放行事实，并补出 MultiStateCallback 同步重入、异常中断、取消强制 end 和 Activity 重建重新武装边界。
- 第 227 章已完成：由 97 个碎片化二级标题、714 行重构为 16 个二级标题、580 行，以“任务列表、缩略图、图标三条独立数据链怎样借 taskId 与各自代际规则在 Overview 汇合”为主线，拆清服务端过滤、顺序冻结、本地 changeId、TaskKey/LRU、视觉缓存和 live/static 交接，并提供九组只读源码练习。
- 技术修正：明确完整任务权限按 Recents appId 判断，目标 user 解锁不等于逐 profile 解锁，visible 计数早于多道结果门；限定 freeze 只抑制已有非 affiliated 任务前移，changeId 不自动拒绝旧 callback；拆开 TaskKey equals 与 LRU 版本规则、无 snapshot 与坏 buffer、thumbnail/icon 取消语义，以及 postDraw、远端截图清理和 Surface 释放三类完成点。
- 第 228 章已完成：由 111 个碎片化二级标题、902 行重构为 16 个二级标题、522 行，以“一张 TaskSnapshot 怎样从触发与模式判定走到内存可见、异步写盘和重启恢复”为主线，拆清入口、候选、REAL/APP_THEME、secure layer、缓存、队列、三文件与读取权限，并提供九组只读源码练习。
- 技术修正：明确产品形态开关并非主动截图总门，失败后仍加入 skip 可错过 closing 补抓；核准 Task 根只捕获可见后代、wrapper 实际下传 RGBA_8888、非 secure RenderArea 会黑化 secure BufferLayer；补出旧隐私状态不追溯清快照、磁盘恢复不回填 running cache、残留反向映射可误删新 entry、Store 深度淘汰与 locked-user 轮转、Delete 后旧 Store 写回、proto/JPEG 非共同事务，以及 Binder 与 screen-off listener 的完成边界。
- 第 229 章已完成：由 107 个碎片化二级标题、901 行重构为 16 个二级标题、520 行，以“一张旧 TaskSnapshot 怎样变成 snapshot starting window，并经真实首窗交接走向最终销毁”为主线，拆清 running-cache-only 选择、wrapper/WindowState/三层 Surface、add/relayout、三种尺寸分支、buffer ownership、WMS draw 状态机与移除链，并提供九组只读源码练习。
- 技术修正：纠正 child 与 parent Java Surface 被误写成重复 release、显式 position 会被 matrix translation 覆盖、450ms 被误写成物理可见下界及 session.remove 被误写成最终删除；补出 Window-frame contentInsets 坐标假设、一次性几何、三次独立提交、跨代 startingData、部分创建失败、旧 REAL snapshot 缺消费端内容复查、startingDisplayed 可重算，以及 ActivityRecord 解绑、remove request、preview exit 与 removeImmediately 的分层完成点。
- 第 230 章已完成：由 105 个碎片化二级标题、878 行重构为 16 个二级标题、548 行，以“一个系统栏窗口的几何与可见性怎样从 Provider 进入 raw InsetsState，经逐窗口过滤、Binder/Handler 与客户端三份 State 变成 View 收到的 WindowInsets”为主线，用分层完成点拆开生产、聚合、控制、过滤、交付与消费，并提供九组只读源码练习。
- 技术修正：核准 top/bottom gestures 先映射 mandatory、mandatory 再并入 system gestures及多 Source 几何 max/可见性顺序覆盖；补全三种模式、legacy compat、IME max 异常、visibleInsets 与 cutout/caption 直接生产；拆清 server/client visible、real/fake/dummy target、fixed-rotation early-return、transient 改写、requested State 累积、同进程深拷贝、Insets/resize 非原子到达、ViewGroup target-SDK 消费分叉与布局元数据的安全边界。
- 第 231 章已完成：由 103 个碎片化二级标题、832 行重构为 16 个二级标题、593 行，以“一次 Insets show/hide 怎样从服务端 Control/leash 交付进入客户端动画，并在 IME 延迟、每帧提交、finish、cancel 与 control revoke 中收敛”为主线，拆清目标选择、leash 交付、Consumer 接纳、runner 生命周期、逐帧参数与服务端回报，并提供九组只读源码练习。
- 技术修正：明确 Provider 先创建并归一化 Control 再更新目标映射，after-prepare 只越过 WMS 事务准备边界而不代表 SurfaceFlinger present；拆开 real/fake/dummy target、同步占位 Control、同进程与远程 IWindow、ControlAdapter 所有权、普通与异步 runner 的 onPrepare/onReady 次序；补出混合类型遇 IME 未就绪时候选副本仅由 GC/finalizer 回收、IME 预渲染比较缺口与 fromIme 通知时机，以及 requested visibility 提前回报、View 回调层级、RT 同步、finish/cancel/revoke/release 的不同完成点。
- 第 232 章已完成：由 104 个碎片化二级标题、1024 行重构为 16 个二级标题、573 行，以“一个 View 怎样经 IMM/IMMS 与独立 IME Service 建立编辑协议、显示/隐藏窗口，并把字符跨进程写回 Editable”为主线，拆清三进程 Binder、焦点/served、客户端校验、绑定/Session、token 来源、show/hide/Insets 与 InputConnection 回写，并提供九组只读源码练习。
- 技术修正：明确 `startInputInner()`为真、show 为真和 `RESULT_SHOWN`都不是下游完成确认；补出跨 Looper full start与 focus-only 无固定顺序、旧 wrapper退休窗口、Session callback按回调时当前 client挂接、请求 token不提供最新代际；拆清 IMMS hide flag早拒绝无回调、原 flags不传入 IME、有效请求窗口才中止 pending show、`fromIme`无 control时只保留可见意图，以及 `mWindow.hide()`触发而不完成 Surface收尾。
- 第 233 章已完成：由 107 个碎片化二级标题、986 行重构为 16 个二级标题、510 行，以“一笔 IME 编辑命令或状态反馈怎样跨越 IInputContext、App Looper 与 IInputMethodSession，并分别停在哪个完成点”为主线，拆清 oneway与 callback、本地等待、TextView编辑语义、反向 Session、CursorAnchorInfo和富内容URI权限，并提供九组只读源码练习。
- 技术修正：明确 CancellationGroup是客户端bind级且r48超时日志布尔反向，`closeConnection()`不在IInputContext AIDL；补出 completion/correction能力位错位、零宽插入POINT亲和性、delete最小包络与无selection返回差异、batch非事务、跨显示无矩阵时cursor能力降级，以及token构造、take授权、callback接受和异步加载的独立完成点。
- 第 234 章已完成：由 104 个碎片化二级标题、889 行重构为 16 个二级标题、526 行，以“一枚目标 App KeyEvent 怎样经 ViewRoot 与 IME Session Channel 完成过滤回执，并为何 App 的 2500 ms 超时不等于取消已发布事件”为主线，拆清三种 InputChannel、四套 sequence、双层 finished、按 deviceId 异步排序与反向 sendKeyEvent，并提供九组只读源码练习。
- 技术修正：明确 `mHasImeFocus`只是窗口资格，Motion可能部分publish，Receiver文档不等于实现严格single-flight；补出2500 ms只是timeout最早到期、callback与同device排序仍可延后，flush只覆盖已发布pending而不覆盖queued-unsent；拆清finishSession不排空事件、迟到finished/HANGUP边界，以及 `FLAG_DELIVER_POST_IME`与KeyEvent flag的命名空间。
- 第 235 章已完成：由 99 个碎片化二级标题、879 行重构为 16 个二级标题、555 行，以“一条按焦点寻址的输入怎样在无窗口等待与成功 publish 后的 Connection 回执等待之间分流，并由 finished 或 ANR policy 收敛”为主线，拆清双焦点、三层队列、七套计时、两类 ANR、Java 归责与恢复边界，并提供九组只读源码练习。
- 技术修正：明确 no-focused-window 没有 Connection/waitQueue，abort 后仅在重试仍无窗口时按旧 deadline 丢弃；核准 Connection deadline 从成功 publish 的尝试起算，`AnrTracker` 只存 deadline/token 索引且 extension/迟到 finished 均不保证重建全部 surviving wait 索引；区分两套 500ms、2秒慢日志、IME 2500ms、两类默认5秒与10秒 stale，并拆开 cancel mode、broken Channel、handled=false fallback 与进程处置。
- 第 236 章已完成：由 135 个碎片化二级标题、1315 行重构为 16 个二级标题、482 行，以“一个 pointer Motion 怎样从 DOWN 命中建立 TouchState 所有权，并经 outside、split、slippery、wallpaper、monitor 与 pilfer 路由到 UP/CANCEL”为主线，拆清当前目标与下一状态、pointer ID 子集、动作改写、跨 display 路径和异步取消边界，并提供九组只读源码练习。
- 技术修正：明确 InputReader 边界是 NotifyMotionArgs 而非 MotionEntry，临时 TouchState 只隔离注入权限且 failed 尾仍可能写回；限定 Case 1 的 paused/Connection/responsive 门，核准 obscured/outside、split 等基数绕过、slippery 的 sticky/mouse/portal 边界与入口 display wallpaper；拆开 global/gesture monitor、MONITOR_INPUT capability、oneway pilfer、排队 CANCEL、pilfer 后 split POINTER_DOWN 重入，以及 setInputWindows、unregister、device reset 的非对称清理。
- 第 237 章已完成：由 128 个碎片化二级标题、1194 行重构为 16 个二级标题、484 行，以“一次软件输入注入怎样跨过前置受理、目标授权、foreground 释放与 HMAC 字段认证四本账”为主线，拆清 Java/Binder/JNI 身份、Key/Motion 规范化、三种 mode、超时/ANR 与 Verified 子集，并提供九组只读源码练习。
- 技术修正：明确 ASYNC 仍同步经过类型/结构校验、权限预查与 pre-queue policy，true 只代表合法 Entry 已入队；拆开早期 TRUSTED 快照、同 UID 放行、跨 UID 后期授权、wrong-device permission denied 与 signature 级 MONITOR_INPUT 注册能力；核准 Motion history 仅当前 sample 绑定 InjectionState 的结果/权限/foreground 账，policy 消费可空成功，FINISH 只等实际计数项释放且 Key fallback 延续同一计数；区分共同注入 deadline、无焦点窗口/Connection ANR 与超时不撤销事件，并把 HMAC 限定为目标 resolved action 和受保护 flags、Verified 仅认证字段子集且不证明硬件来源或新鲜性。
- 第 238 章已完成：由 122 个碎片化二级标题、1098 行重构为 16 个二级标题、646 行，以“一次进行中的 pointer 流怎样在 TouchState 路由账与两端 Connection InputState 协议账之间完成显式交接”为主线，拆清 Java/JNI token、五类 native 出口、单指/多指/split 补事件、系统接管者与异步完成边界，并提供九组只读源码练习。
- 技术修正：明确 portal 可让入口 Display 的 TouchState 保存另一 Display 的窗口，unordered_map 只迁首个命中条目，而 Connection 层会合并/取消全部 pointer-class memento；核准 merge 仅以 device/source/display 匹配、无匹配时先改源 firstNewPointerIdx、已有分界不覆盖，且 hover 并存可产生旧端 HOVER_EXIT/新端 DOWN；拆开 TouchState 三类 target flag 与 memento flags、to split 能力不重判、inbound/outbound 切换、跨 Connection 无观察顺序、synthetic DOWN 的间接 focus、weak true 与无回滚，并记录 Drag 空 Region、IME source/host 快照校验边界。
- 第 239 章已完成：由 97 个碎片化二级标题、811 行重构为 16 个二级标题、699 行，以“一条已开始的 pointer 流怎样被系统接管、逐层筛出 DROP 接收者，并把数据、URI 能力、结果回执与资源清理闭合”为主线，拆清 Motion/DragEvent 双链、窗口通知/View 兴趣/最终命中三道门、drag Surface、跨 user URI grant 与异步结束，并提供九组只读源码练习。
- 技术修正：明确 localState 只回填发起 ViewRoot，GLOBAL 对 pre-N 源 IWindow 保留 local fallback，STARTED 是 oneway 窗口账且初始 LOCATION 主动触发；核准 POINTER 动作被忽略、CANCEL 仍尝试 DROP、synthetic DOWN 的中性 buttonState 影响 stylus 分支，窗口 EXITED 坐标无语义且 child 命中只是数组逆序；修正 URI handler 必须具备 READ/WRITE、先 collectUris 再 fixUris、grant 不随 false/ENDED 自动撤销，并补出拖影 show/reparent 直到首个 MOVE 才 apply、正确 DROP token 丢失 WindowState 会移除 timeout 后悬挂、timeout 动画期间迟到 true 可翻转 ENDED result 等 r48 边界。
- 第 240 章已完成：由 101 个碎片化二级标题、827 行重构为 16 个二级标题、578 行，以“一个长期存在的 WindowState 怎样在某次 Surface 事务中变成按 display 排列的 InputWindowInfo 值快照，并在替换时闭合失效路由”为主线，拆清三次取值边界、modal Region 显式化、scheduled/force/immediate/sync、SF 最终几何、id+token 复用与 focus/hover/touch 收尾，并提供九组只读源码练习。
- 技术修正：明确 setInputWindowInfo 在 JNI 调用时复制、force 仍异步、WMS 动画等待与 SF input sync 各有独立 5 秒超时，并限定共享 pending/listener 下只有无重叠 sync 调用才能建立完成证明；核准 token-bearing visible 忽略 buffer/alpha、Region 不套任意旋转矩阵、无 token overlay 在 SF 重写、缺失 display key 不等于空列表、NO_INPUT_CHANNEL 可绕过 Connection 校验，并补出 global hover 被其他 display 更新清除、cancellation 只在正常 Connection 有匹配 memento 时产生、releaseChannel 只清 handle token 等 r48 边界。
- 第 241 章已完成：由 103 个碎片化二级标题、1002 行重构为 16 个二级标题、593 行，以“一块 App 窗口局部 touchable Region 怎样经 WMS 与 SurfaceFlinger 成为 Dispatcher 屏幕命中区，并把幸存触点送回 App 与 child 局部空间”为主线，拆清四种 insets、modal/effective/surface 三口径、Task/tap-exclude、Layer crop/replace/clone、TouchState 命中生命周期与 raw/local 语义，并提供九组只读源码练习。
- 技术修正：明确 modal surface 分支不先消费 App Region，Task 与 root-task 不是按空结果回退；区分 Java dim-bounds、Surface crop、organized replace 与有效 clone-root 后处理，限定 Region 的轴向 transform 边界；补出不可触摸窗口仍可能成为 OUTSIDE target、异 UID 才加 zero-coords、手势中途通常复用 TouchState，以及 same-token 多指断言、global scale 的轴语义和 child View 继续变换 raw/local 的边界。
- 第 242 章已完成：由 113 个碎片化二级标题、939 行重构为 16 个二级标题、455 行，以“一组 App system gesture exclusion Rect 怎样经客户端映射、WMS 触摸/Z 序裁剪与边缘预算，成为 SystemUI 对单次返回手势的 DOWN 判定输入，并在越阈值后尝试接管触摸流”为主线，拆清 View/Window List、oneway 发布、actual/unrestricted、预算、SystemUI 门序与 pilfer/Back 完成点，并提供九组只读源码练习。
- 技术修正：明确 View/List 引用与 r48 清空后 listener 不重挂、Tracker 将旧 mapped 与新 local 比较的漏更新；限定 unrestricted 已经可见性/touchable/Z 序裁剪且 actual 相等会使既有 SystemUI 副本滞后，修正 Region 矩形高预算与部分截断后 remaining/统计可为负的实现边界；补出 App List 不变仍可由 placement、Insets、导航设置、density 与 DeviceConfig 重算，以及 oneway pilfer OK 只清路由账、不保证每个旧连接收到 CANCEL，split POINTER_DOWN 还可能加入新目标或因无目标被丢弃。
- 第 243 章已完成：由 106 个碎片化二级标题、857 行重构为 16 个二级标题、495 行，以“一次触摸已命中敏感 View 后，r48 怎样按上层输入条目的主体关系与 frame 几何生成逐目标遮挡位，并由 View 过滤与 WMS 隐藏策略分别完成防护”为主线，拆清正常命中与安全扫描、Trusted Overlay、流内 targetFlags、resolvedFlags/HMAC、消费端拒绝和特权隐藏完成点，并提供九组只读源码练习。
- 技术修正：补出无 InputChannel 的非 cursor BufferLayer 仍会生成 occlusion-only 输入条目及两类 visible 来源，限定遮挡 frame、半开含点与严格相交；核准 trusted 类型硬编码列表及 INTERNAL_SYSTEM_WINDOW 权限边界；拆开普通/split/slippery/wallpaper/transfer 的风险位生命周期、TouchState 与 Connection memento 双账、原始注入 flags 和仅 resolved DOWN/UP 的 HMAC；明确默认 View 只拒 OBSCURED，而 WMS 隐藏策略受 Session 权限、Surface shown、全局请求者列表、目标窗口类型与 owner 能力共同约束。
- 第 244 章已完成：由 84 个碎片化二级标题、699 行重构为 16 个二级标题、574 行，以“一份已选好接收者的 MotionEntry 怎样经 InputTarget 的 pointer、mode、geometry 与责任计划，派生为每条 Connection 可发布的 action、pointer 集合、坐标、flags、eventId 与 seq”为主线，拆清 InputTarget 聚合、split/combined 派生、六种 mode、InputState 协议门、四本编号账、坐标/HMAC 投影与 foreground/publish 完成点，并提供九组只读源码练习。
- 技术修正：明确同 token 聚合不 OR flags，且 pointer ID 槽位只有 16 而标识范围可到 31；核准 SPLIT 以数量不等作触发门、数量相等不验成员，正常 split 的 DispatchEntry 持有每 mode combined 而非临时 split；拆开 root/derived/resolved/seq 四类编号、显式 mode 与 AS_IS hover 修复、track 前消耗 seq 与 track 后分配 resolved ID；补出几何基准取最低 marked ID 且除法无零保护、HMAC 仅签 resolved DOWN/UP 的 Verified 投影、InputState 在 publish 前记账、注入 succeeded 可早于每目标投递、synthetic cancel 旁路目标遍历，以及 foreground 按成功入队 entry 计债、send 成功才进入 wait。
- 第 245 章已完成：由 92 个碎片化二级标题、675 行重构为 16 个二级标题、463 行，以“一笔已进入 Connection 的 DispatchEntry 怎样跨 outbound、Unix socket、App处理、反向FINISHED与wait账，在背压、乱序、fallback或断链下最终释放并只结一次foreground债”为主线，拆清 Connection 状态、双向 socket、outbound/wait、ANR 索引、Java/JNI 回执、Motion batch、Key fallback 与异常释放，并提供九组只读源码练习。
- 技术修正：明确 waitQueue 是服务端确认欠账而非额外物理缓冲，send 成功迁账且只在 responsive 时登记 ANR 索引；区分正反向 WOULD_BLOCK、send/recv 错误映射、Java map、transport seq、mFinishQueue 与 SeqChain；补出 trackerless 恢复、非队首与重复 FINISHED 对 fallback 的顺序风险、fallback 重发三分支、broken drain 不擦 tracker、ZOMBIE 通知跳过及 InjectionState 结果、pending、引用三账。
- 第 246 章已完成：由 102 个碎片化二级标题、775 行重构为 16 个二级标题、438 行，以“一笔由 Reader 解析出的事件，怎样在不携带 Reader 锁的前提下跨入 Dispatcher，随后在 wake、pending、policy、drop 与 target 选择之间得到一个明确结局”为主线，拆清 Args/Entry/DispatchEntry 三层身份、Reader 双锁区与 flush、Classifier 异步缓存、Filter 回注、入队唤醒、dispatchOnce 外壳、pending 与五种丢弃、三类控制事件，并提供九组只读源码练习。
- 技术修正：明确设备列表通知先于 flush、Motion/DeviceReset 持 Classifier 锁跨下游且 HAL 结果滞后；区分 Switch 直达 policy、Focus 非 NotifyArgs、Filter 原事件不入队与 FILTERED 回注跳过第二次 beforeQueueing；补出 needWake 三条件、frozen 不停止外层 ANR、idle 不等队列空、Key drop 前仍跑 repeat/beforeDispatch 及 repeat 残留；限定 mAnrTracker 登记账、五类 drop 优先级、state-derived CANCEL、app-switch/stale/marker 边界、target 成功仍可零 DispatchEntry，以及 Focus policy command、非 foreground target 与 native 自动 FINISHED。
- 第 247 章已完成：由 115 个碎片化二级标题、963 行重构为 16 个二级标题、424 行，以“两条输入工作线程怎样借各自等待器、组件锁与可合并门铃完成交接，并在同步回调和退出期间保住状态边界”为主线，拆清线程创建与停止、Reader 四阶段循环、EventHub epoll、Classifier/Filter 交接、Dispatcher Looper、Command 解锁续体、Condition/Watchdog 探针与九个完成点，并提供九组只读源码练习。
- 技术修正：明确 InputThread 吞掉底层 run 失败且 start 不提供 ready barrier；区分 EventHub pipe 与 Looper eventfd 的合并、并发 drain 和非抢占语义；核准 Looper poll response 为单 poller 私有而非 mLock 账，Classifier 持自身锁跨 listener 是窄例外；拆开 connection/no-focus ANR、FINISHED 两次 seq 查找、CANCEL 生成条件、注入 capability 预检与最终 ownerUid 授权，并限定 waitForIdle、monitor、Watchdog 和 stop 各自只能证明的完成点。
- 第 248 章已完成：由 118 个碎片化二级标题、961 行重构为 16 个二级标题、402 行，以“一个内核输入节点如何被识别、聚合、配置、公开，并在丢包、禁用、拔出或全量重开时恢复上下游协议”为主线，拆清 EventHub 扫描/open 门、descriptor 聚合、class 到 Mapper、synthetic 批次、Reader 双映射、configure/reset、双 generation、SYN_DROPPED 与 remove/reopen，并提供九组只读源码练习。
- 技术修正：明确 open 中 id、built-in 与 controller 选择存在非事务式失败边界；区分显式 disabled 与缺 viewport 的首次关闭顺序；核准 composite 共用一枚 drop flag、partial removal 可只推进 global generation、built-in removal 的 0/正 id 不对称，以及 synthetic 容量减到零后仍可能继续写后续阶段的 r48 边界。
- 第 249 章已完成：由 117 个碎片化二级标题、640 行重构为 16 个二级标题、394 行，以“由多条 evdev 事件组成的一帧多触点报告，怎样在同步边界后获得连续身份、显示坐标和合法 Android Action 序列”为主线，拆清 Protocol A/B、slot 增量、RawState pending、tracking/距离两套 ID、Viewport 与辅助轴、动作集合差、NotifyMotion 和交付完成点，并提供九组只读源码练习。
- 技术修正：区分 32 个 slot、16 个输出点与 0—31 pointerId 三种容量；补出 B 流中的 SYN_MT_REPORT 仍会推进 current slot、初始 palm 不必然置 aborted、重复 tracking 会折叠到同一 ID、距离回退可同帧复用离场 ID；并核准 MT tilt 固定为零、coverage 不走 affine，以及一帧展开多笔 Motion 时视频队列只归第一笔的 r48 边界。
- 第 250 章已完成：由 122 个碎片化二级标题、781 行重构为 16 个二级标题、472 行，以“TouchInputMapper 怎样用 mode、viewport、校准、虚拟键与全局外接笔状态改变触摸输出语义”为主线，拆清 configure 重入、四种 mode、viewport 短路、校准公式、屏外按键和 72/20/10 ms 融合，并提供九组只读源码练习。
- 技术修正：明确 changes==0 在 composite 扩缩时重入、mode-only 可沿用旧 surface 几何、external source 变化不推进目标 generation；核准 size/orientation/coverage 的 range/value 偏差、virtual-key parser/quiet/reset 边界，以及全局无配对 stylus 广播、重复 composite update、三只时钟与 button-release 滞留。
- 第 251 章已完成：由 129 个碎片化二级标题、896 行重构为 16 个二级标题、489 行，以“PMS 怎样把 Settings 旧账、system/APEX/data 磁盘事实与用户状态调和成可查询包世界”为主线，拆清双锁启动、备份恢复、appId/shared UID、分区扫描、updated-system 回退、分层写盘与九个完成点，并提供九组只读源码练习。
- 技术修正：明确 backup 内容损坏不会回退 main、无起始标签可能因空 VersionInfo 直接崩溃、packages.xml 的 userId 实为 appId，且 runtime-permission 主账位于 permission APEX；核准共享 mLock 与 installLock 例外、可更新/flattened APEX 分流、目录内完成序串行提交、mExpectingBetter/OTA/stub 三类分叉，以及无成功返回值的 writeLPr、PMS_READY、Binder、systemReady 和 app-data/WebView join 的非等价边界。
- 第 252 章已完成：由 135 个碎片化二级标题、824 行重构为 16 个二级标题、411 行，以“一枚 APK 候选何时从可变解析结果成为其他线程可查询的包事实”为主线，拆清启动与普通安装两种锁序、parser cache、PackageImpl 双视图、证书与身份预检、updated-system 预扫描、ScanRequest 别名、批量 reconcile、appId 预占、非事务 commit、内存发布与持久化，并提供九组只读源码练习。
- 技术修正：明确构造期双锁早于并行 parse、普通安装 appId 在整批 mLock 外且早于 reconcile；核准 parse cache 共享 basename+flags entry并存在跨目录碰撞风险、PackageSetting 仅作混合浅复制、hideAsFinal 不冻结同一 PackageImpl、r48 数据分区强制验签分支未启用、普通 prepare 已有签名预检且 reconcile 会改 shared-user 签名态；并指出 shared user 可在 scan 注册 appId、frozen 检查仅为诊断、多包逐包 writeLPr、失败清理不构成完整回滚，以及标准查询可见、磁盘写、dexopt、广播各有独立完成点。
- 第 253 章已完成：由 120 个碎片化二级标题、699 行重构为 16 个二级标题、463 行，以“同一目标包为何会因调用身份、用户、flags 与对象形态得到完整、最小、裁剪、隐藏或异常结果”为主线，拆清入口身份、名字与对象路由、静态库双门、instant 包装、AppsFilter 三类键空间、用户态投影和字段所有权，并提供九组只读源码练习。
- 技术修正：区分 PMS `null` 与 SDK `NameNotFoundException`、当前 Binder UID 与原始 filter UID、declaring APK version 与 static-lib version；核准 factory/APEX 非对称路由和固定扫描缓存、Direct Boot 只逐项裁四类普通组件、system/known/hidden-until-installed 例外；补出 AppsFilter cache hit 的权威性及 protected-broadcast、DeviceConfig、InstallSource 等失效缺口，并确认 r48 普通 APK 路径会回填共享库字段、四类组件 metadata 不完全服从 `GET_META_DATA`、顶层新对象仍含浅别名。
- 第 254 章已完成：由 120 个碎片化二级标题、717 行重构为 16 个二级标题、467 行，以“一次 Intent 解析如何依次完成注册、分流、召回、匹配、过滤、排序与单项选择”为主线，拆清显式/指定包/全局三路、六张倒排表、data/type 决策树、同组件 Filter 去重、跨 profile/domain/instant 合流与 ResolverActivity 回退，并提供九组只读源码练习。
- 技术修正：确认 `setPackage()` 走目标包 listCut 而非全局索引，selector 的外层 package 与内层条件分别影响分流、instant flags 和 package guard；核准 `allowFilterResult()` 在完整 match 前且只由已加入结果挡住后续 Filter、MIME 成功会覆盖 URI match 类别、SSP 与 authority/path 是回退分支；区分普通 `android:order`、domain `preferredOrder` 与用户 preferred，并补出默认浏览器 priority 零下限、installer 追加/动态 split 替换未必重排以及 `chooseBestActivity()` 机械读取当前前两项的边界。
- 第 255 章已完成：由 120 个碎片化二级标题、721 行重构为 16 个二级标题、447 行，以“一次 Web URL 如何依次通过语法匹配、网站证明、包级/用户策略与候选处置”为主线，拆清安装排队、全包 Web Filter 批次、StatementService 整批 AND、双层持久账、默认浏览器、跨 profile、instant 与最终选择，并提供九组只读源码练习。
- 技术修正：区分 `hasWebURI()`、`isWebIntent()`、`handlesWebUris()` 与失真的 package 快照门；补出 early-return pending 状态、无 token deadline、非零用户 verifier UID 与 user-0 广播错位、签名轮换时旧 `GET_SIGNATURES` 暴露最旧 signer、恢复/清账跨用户不对称和非法 status/generation 持久化；核准单候选、仅 parent、base/selector、ALWAYS_ASK、默认浏览器零下限与 `RESOLVE_NON_RESOLVER_ONLY` 等查询边界。
- 第 256 章已完成：由 120 个碎片化二级标题、657 行重构为 16 个二级标题、525 行，以“一份安装请求怎样经身份与参数净化、受控写入、永久封印、内容验证、用户确认和 PMS/staged 两条后半程得到分层完成点”为主线，拆清 Session 七本账、FULL/INHERIT/APEX、multi-package、ActiveInstallSession、跨重启恢复与九组只读源码练习。
- 技术修正：明确 `openWrite()` 无统一截断保证、writer sentinel 登记早于文件名检查且存在 seal 锁间隙；区分 Session committed、PMS 提交与 staged applied，补出 multi child pending 子集提交、parent 最终回调不清账、ready 非终态、staged APK 重入 PMS 四阶段，以及 `updatedMillis`、确认位和 original transfer owner 未完整恢复等 r48 边界；同时核准 `.dm` 的松散配对/后置校验、remove marker sealed 窄窗、same-UID transfer、active 引用缺口与 APEX 单文件分支。
- 第 257 章已完成：由 120 个碎片化二级标题、689 行重构为 16 个二级标题、497 行，以“一份只声明文件合同的安装请求怎样在 Streaming 普通文件与 Incremental 缺块文件两条路上获得供数，并把 Loader ready、本地可安装、全量装载与长期可用分开”为主线，拆清 Manager绑定、Java/native桥、IncFS块填充、V4、健康状态、永久 bind与九组只读源码练习。
- 技术修正：确认显式组件不检查 ACTION_LOAD_DATA filter、仅 Streaming ID等于 sessionId；补出已有连接不重放 BOUND导致的 recommit缺口、JNI丢 removedFiles/signature、FileSystemConnector不重验状态或 size、cmd仅授权 per-file fill、可空 V4和 idsig/tree拆分、startLoading与 IMAGE_READY非完成保证、range查询固定 false、构造期 system-app门传 null、字符串 `==`双缺口，以及 health卸载、multi异步失败和 permanent bind早于 PMS commit等 r48边界。
- 第 258 章已完成：由 120 个碎片化二级标题、863 行重构为 16 个二级标题、423 行，以“已有 Session、content快照与 package已有包恢复怎样穿过来源授权、本次确认和结果回传三本账”为主线，拆清 InstallStart身份整理、未知来源限制/AppOp、Settings回程、确认能力、直接安装 UI和九类完成证据，并提供九组只读源码练习。
- 技术修正：区分两类外部入口与三条执行支路；补出 `setPermissionsResult()`只凭特权权限与 sealed、recommit覆盖 receiver但受快照/relinquished约束、Settings回程不重查策略、`package:`忽略 int失败码而假成功、失败 UI丢 public status、pending代启动缺 NEW_TASK、Cancel/Back清理不对称，以及 multi子集提交、staged伪终局和 committed落盘竞态等 r48边界。
- 第 259 章已完成：由 120 个碎片化二级标题、832 行重构为 16 个二级标题、464 行，以“一份已获用户同意的 APK怎样让 ordinary State投票、Integrity规则与 rollback完成门汇合，又为何 timeout可绕开 State聚合直接结束 InstallParams等待”为主线，拆清 required/sufficient身份、投票真值表、两本完成账、规则元数据、三门汇合、staged/multi与九类完成证据，并提供九组只读源码练习。
- 技术修正：明确 required缺失时主流水线不等待但 pending可残留、null Component会清掉显式目标；补出 sufficient副用户 appId/full UID错配及 shared UID折叠、file URI不建立 grant、非成员消息仍可重复发 raw结果、timeout Handler取 system UID且不查聚合结果、任意持权者可抢唯一延期并消灭后备计时、code 2先封装和白名单不续期；同时核准 Integrity timeout的 -1/0常量差异、rule-provider `matches()`参数方向、副用户 installer证书空列表、UTF-16长度规范化，以及 staged双阶段、staged+DataLoader抽取失败、Incremental+V4事后 root hash通知等 r48边界。
- 第 260 章已完成：由 120 个碎片化二级标题、812 行重构为 16 个二级标题、402 行，以“APK字节进入受控stage后，怎样经copy、rename、prepare/scan/reconcile/commit和post-commit走到可查询、资源可用与结果交付”为主线，拆清OriginInfo正交状态、File/Move InstallArgs、四处native触点、两层锁、AppData/profile/dexopt及九类完成证据，并提供九组只读源码练习。
- 技术修正：确认原始跨重启staged Session不直接进入InstallParams，而由`isStaged=false + INSTALL_STAGED`的合成APK Session接续；补出`fromUntrustedFile()`无生产调用、PRIMARY_PHYSICAL走非staged File复制、raw目标FD未关闭且无O_TRUNC、copy失败跳过pre/post清理、restorecon字段写序与IncFS运行时异常缺口、multi受控失败码分化、Move按CE源存在决定逐用户复制，以及post-commit仍占`mInstallLock`、Incremental native等待无超时且commit后异常可截断install observer等r48边界。
- 第 261 章已完成：由 120 个碎片化二级标题、672 行重构为 16 个二级标题、449 行，以“一个合法更新怎样在不同完成点交接身份、进程、活动模型、用户数据和旧代码，并在system/static/no-kill边界暴露非原子缺口”为主线，拆清三次签名复核、freezer所有权、两层锁、KEEP_DATA删除计划、system/static分支、广播与旧代码清理，并提供九组只读源码练习。
- 技术修正：确认PackageFreezer返回只证明冻结与尝试请求AMS，persistent可不被kill，且Set并非引用计数；补出`DELETE_DONT_KILL_APP`在replace直调删除链未被读取、KEEP_DATA仍撤下permission定义并解除受影响用户内所有包的distraction限制、单用户更新会请求清全包profile却只为目标用户准备，以及commit中途异常没有undo。进一步区分external资源广播、`args==null`仅GC、kill/no-kill的Incremental异常窗口，并记录no-kill install observer按包名单Pair导致重叠更新错配或丢回调。
- 第 262 章已完成：由 120 个碎片化二级标题、613 行重构为 16 个二级标题、398 行，以“更新怎样把旧用户决定投影到解析后的新请求，并让权限state、持久化与旧进程分别收敛”为主线，拆清definition发布、普通包copy-reset-rebuild、grant lattice、restricted policy、shared UID并集裁剪、split兼容、两类写盘、GID与进程，以及同步和异步撤销，并提供九组只读源码练习。
- 技术修正：明确重建依据是parser最终requested集合，`GRANT_UPGRADE`不等于单纯targetSdk跨M；确认显式revoke同步写packages.xml却异步写runtime文件，shared unused runtime状态裁剪才走定向同步writer；纠正成功regrant的差异记账、GID长度比较和group转null三个伪bug。进一步补出shared并集不按用户安装态过滤、missing才取成员最小targetSdk、unknown残留不由helper清理，以及PackageFreezer只按更新包异步请求kill，shared B-only进程和owner全量重评的第三方进程都可能暂留旧supplementary GID。
- 第 263 章已完成：由 120 个碎片化二级标题、602 行重构为 16 个二级标题、453 行，以“运行时权限请求怎样跨UI、grant/flags、AppOps、会话与后台扫描多个完成点收敛”为主线，拆清Activity/Fragment入口、调用身份、原始与affected集合、Java模型立即持久化、PMS校验、shared UID AppOps、one-time状态机和长期不用自动撤销，并提供九组只读源码练习。
- 技术修正：确认平台Fragment可绕过Activity实例闸门，PermissionController而非原App是PMS Binder caller，且请求UI不走KotlinUtils；补出ONE_TIME先写flag再grant的多轮中间态、resultCode被权限专用分发丢弃、PMS返回早于PPS两次FgThread投递、raw AppOp仍需UidState求值及appops.xml延迟写。进一步记录one-time的UID复用、outer/inner反向锁序与未同步gone回调，以及auto-revoke记录早于importance检查、GlobalScope子任务脱离Job取消和shared UID豁免/变更粒度错位。
- 第 264 章已完成：由 120 个碎片化二级标题、607 行重构为 16 个二级标题、397 行，以“本机 runtime 快照、版本迁移、选择性备份与延迟恢复怎样分别提交和失败”为主线，拆清普通包/shared user归属、APEX DE文件、两层写调度、锁与AtomicFile、legacy读取、升级握手、BackupManager传输、筛选/恢复和按包延迟消费，并提供九组只读源码练习。
- 技术修正：明确10秒、200毫秒和2秒都不是落盘截止时间，async旧快照与sync新快照可并发写同一无文件锁AtomicFile，坏的新格式不会回退legacy，pre-created user又以错误监视器重读；补出fingerprint不清当前进程升级门、version=8仍重放预装受限权限whitelist、user-sensitive与AppOps各有完成点。进一步核准RemoteStream 30秒与两只60秒等待、空数组/NULL Blob差异、ONE_TIME和ROLE来源坍缩、Q=29的未来split扩展、整组USER_FIXED与AUTO_REVOKED副作用，以及旧false回调跨代屏蔽新delayed集合。
- 第 265 章已完成：由 120 个碎片化二级标题、587 行重构为 16 个二级标题、486 行，以“两套自动授权器如何跨 permission、flags、AppOps、holder 与多份持久化状态收敛”为主线，拆清 Default 三阶段、产品 exceptions、Role 资格与 grant/revoke、来源叠加和多重完成点，并提供九组只读源码练习。
- 技术修正：确认 Default 的 factory PackageInfo 查询缺 `MATCH_FACTORY_ONLY`，Telecom SMS/Dialer 与 SIM 积压回放存在跨用户错位；补出 Default 死排序、OR-only 清位、exemption 串位、XML/XSD/缓存边界，以及 Default/Role 来源无引用计数、Role split grant/revoke 不对称、角色 unavailable 清记录不撤特权、changed 漏报和 holder/权限/AppOps/preferred 多账非事务等 r48 边界。
- 第 266 章已完成：由 120 个碎片化二级标题、558 行重构为 16 个二级标题、489 行，以“一个组级按钮怎样投影并提交逐 permission、flags、AppOps 与 one-time 会话”为主线，拆清 Utils 平台分组、Java/Light 双模型、主/背景子组、request/settings/review 三条 UI 链与多层完成点，并提供九组只读源码练习。
- 技术修正：确认 r48 位置组由 Utils 而非 Manifest group 映射，Light 的 including-AppOp 实际不读 AppOps/REVIEW_REQUIRED；补出背景子组双端链接条件、Java filter 后全子组 persist/AUTO_REVOKED 清除、真实 PM→flags/AppOps 写序、R finish 后继续、Settings result 早于确认、Review handheld/Wear 差异和 UID 级副作用。进一步核准 Camera/Mic 的动态 evalMode、Kotlin 短路在当前 LOCATION 同 switch 映射下不构成漏写，以及后台 ONE_TIME、policy override、DEFAULT/ROLE、restricted 与双文件写时钟边界。
- 第 267 章已完成：由 120 个碎片化二级标题、565 行重构为 16 个二级标题、665 行，以“同一 permission 名字怎样穿过定义、group、tree、请求、grant、持久化与缓存多个完成点”为主线，拆清静态定义仲裁、动态增删、XML 恢复、split 解析与继承、三类缓存，并提供九组只读源码练习。
- 技术修正：确认 foreign tree 拒绝绑定仍可留下并持久化 skeleton，system 包可覆盖未绑定空壳；补出 addToTree 丢 leaf info、remove 后残留 grant、普通 boot early-return 导致 dynamic 未重绑及跨重启 cap 漏算、owner 更新删除 API leaf、group stale、tree 悬空删错 Map、adopt 不迁 group。进一步核准 split 小于阈值与 parser 级联、marker 不等于 grant且 denied 状态清理盲区，以及 split 实例缓存可变、permission cache 忽略 PID 而 AMS 仍有 PID 语义、package key 实为 userId 和 AutoCork bypass。
- 第 268 章已完成：由 120 个碎片化二级标题、567 行重构为 16 个二级标题、515 行，以“身份来源、进程收缩、组件边界、UID/包状态与跨用户资格如何分层裁决”为主线，拆清 Context 四入口、两张缓存、AMS global/组件双链、PMS package/UID/shared/SystemConfig 分支、Binder identity 与两套跨用户 helper，并提供九组只读源码练习。
- 技术修正：确认同步同线程 Java helper 仍继承未清除的入站 Binder identity，而代理进程远程调用 PMS 时对端看见代理自身；拆开 Context global 固定 -1/true 与真实组件 owner/exported，补出 package-name 路径不要求目标用户已安装、install grant 归一 USER_ALL、普通目标残留 state 可在定义删除后通过。进一步核准 PID/UID 不配对、cache 忽略 PID 同时遮蔽 MY_PID/process deny 且进程启停不失效、仅 INTERNET 的进程拒绝与 GID、shared 代表包/可见性、instant/fuller、token 只覆盖局部参数，以及两套跨用户 helper 的 shell 顺序与 cross-profile 空解引用边界。
- 第 269 章已完成：由 120 个碎片化二级标题、558 行重构为 16 个二级标题、511 行，以“基础授权、AppOps政策、动态UID状态、归因事件与持续生命周期怎样在不同完成点组合”为主线，拆清普通/APPOP-flag/runtime三分支、GRANTED/HARD/SOFT真值表、preflight raw与delivery noteProxy、tag解析迁移、瞬时与active账，并提供九组只读源码练习。
- 技术修正：限定r48 PermissionChecker为隐藏平台接口且尚无AttributionSource多跳链；核准APPOP-flag的DEFAULT可在目标未note时回退基础位而runtime的null op/package直接GRANTED，raw包核对失败又可回op默认mode。进一步补出raw独有package-suspend门、proxy-first/self-blame短路、服务端与客户端信任条件差异、root跳过包/tag核对、未声明tag只日志、继承只合并最近access/reject且丢弃running，以及SELF flag不等于caller自有UID、嵌套与active边沿的不同聚合层级。
- 第 270 章已完成：由 120 个碎片化二级标题、574 行重构为 16 个二级标题、492 行，以“保存政策、动态评价、最近事件、聚合历史与观察限制怎样在不同完成点组合”为主线，拆开 check/note/start、UID/package 优先级、setter/reset、四类 watcher、current XML、分层历史与 restriction token，并提供九组只读源码练习。
- 技术修正：明确 suspend 仅在 check，note/start 的 restriction callback 不等于 reject 账，且 UID/package 动态求值使用 code 不对称；补出 setMode no-op 仍建对象、请求写盘或同步通知、running-only prune、reset 跨 user 与通知去重漏 UID、mode watcher 索引并集和首注册黏连、current active 不可恢复、history current-only query 丢 pending 与 ABBA 锁序、DISABLED 非 add/query 总闸，以及 removeUser 不负责写盘、历史、运行态收尾等 r48 边界。
- 第 271 章已完成：由 120 个碎片化二级标题、556 行重构为 16 个二级标题、400 行，以“permission、AppOps、用户初始化与相邻 Role/one-time 状态怎样在不同完成点收敛”为主线，拆清观察入口、线程与超时、shared UID候选、raw遮蔽、两层 setter、APPOP清理和内部协作，并提供九组只读源码练习。
- 技术修正：明确包观察与 onStartUser 同步执行，r48 默认 grant 仍在 system_server而 Controller主要做 runtime数据库升级；补出名义60秒 timeout在 main-thread启动路径可被自身阻塞、fingerprint不清同 boot cached gate、initialized callback还会引出 PMS重评与二次同步。进一步核准 watcher首注册黏连和 UID_ANY限制通知、shared UID同名 permission状态共享、background名为空时应 ALLOW、条件 IGNORE的成功后去重、unsafeCheckOpRaw仍受 suspend/restriction且 package/raw遮蔽可漏写 UID mode、两个 policy setter对 REVOKED_COMPAT的边界不同，以及 Role阻止仅返回 boolean、one-time通知入队与 listener清理并非全序且 Controller会直接写 AppOps。
- 第 272 章已完成：由 120 个碎片化二级标题、556 行重构为 16 个二级标题、413 行，以“grant资格、LEGACY候选、反馈缓存、mount与compat API怎样在不同状态平面收敛”为主线，拆清两份同名策略、三项精确公式、shared UID混合输入、包更新、AppOps提交和FUSE/非FUSE生效路径，并提供九组只读源码练习。
- 技术修正：确认 PermissionController同名类只管UI显示，只有READ产生extra op且其路径不要求permission已grant、非REVOKED_COMPAT或无REVIEW_REQUIRED；核准preserve只保护raw ALLOWED、shared UID中未preserve成员可产生确定IGNORE，forced名单也可能被另一成员ALLOW压过。补出PMS直接grant对secondary user传入appId而PPS同步使用完整UID的r48输入差异、request扩权更新会跨用户撤storage permission、DeviceConfig静态未trim快照与WMS强否决顺序。进一步纠正legacy UID集合并非当前视图真相，isolated-storage与FUSE为正交开关，LEGACY在FUSE下只更新缓存、非FUSE才可能重挂载，Environment compat又是不进入mount算法的并行语义门。
- 第 273 章已完成：由 120 个碎片化二级标题、571 行重构为 16 个二级标题、389 行，以“卷挂载与session、进程视图、direct-path操作、MediaStore row授权与脱敏怎样在不同完成点收敛”为主线，拆清vold/system_server/MediaProvider三侧生命周期、FUSE与AppFuse、shared UID身份、private-path前置门、create/open/readdir/delete/rename、pending/trashed及确认授权，并提供九组只读源码练习。
- 技术修正：核准emulated与public卷建立FUSE的条件不同、20秒remote等待内嵌5秒daemon屏障且超时不取消迟到任务，补出普通unmount双重remove、user stop只清session及service death触发全局reset。进一步限定pass-through在sdcardfs下来自runtime/full、data/obb隔离的NONE与pending-bind边界、exact item grant不能扩大collection query或自动匹配FUSE重建的Files URI、FUSE/Provider pending命名差异、manager仍受其他包private-path前置拒绝，以及物理delete失败仍可能删除row、Content URI与direct path脱敏规则不互推等r48边界。
- 第 274 章已完成：由 120 个碎片化二级标题、562 行重构为 16 个二级标题、452 行，以“数据库路由、卷接入、扫描事务、对账清理与长期维护怎样在不同完成点收敛”为主线，拆清双helper共库、attached与query filter、异步attach准备、扫描编排、路径/metadata决策、batch与reconcile、pending/trash及idle维护，并提供九组只读源码练习。
- 技术修正：明确普通collection在query builder动态按volume_name过滤，只有三个audio aggregate view烘焙filter；补出Documents ready是全局位、scanner状态URI是会被重叠扫描覆盖的单槽、FINISHED不证明状态清空或扫描成功、detach取消不是持久闩锁、目录锁不覆盖reconcile。进一步核准upsert单项容错不适用于clean delete、seen不等update成功、FUSE pending发布受调用线程与提交结果约束、普通delete可删row留文件、expiry事务回滚可恢复row却不能恢复已unlink文件、idle signal不能打断在途scanner且异常可绕过jobFinished，以及generation需配合version、预采样high-water和独立删除检测。
- 第 275 章已完成：由 120 个碎片化二级标题、566 行重构为 16 个二级标题、482 行，以“一次 MediaStore 写操作怎样让路径、row、文件字节、扫描与通知在不同完成点收敛”为主线，拆清 ContentResolver 与 FUSE 两个写平面、insert 放置、受限 upsert、update movement、delete 分流、open/close、rename 事务及三阶段副作用，并提供九组只读源码练习。
- 技术修正：核准 download/owner 判定早于现代路径生成、raw DATA 只用 canonical path 做包含检查、upsert 显式 include pending/trashed，并补出 movement 在 rename 前建目录、FS→DB 权限窗口与 ENOENT 不失效 dentry。进一步区分 ROOT/database-bypass/unchecked/checked 的 unlink 与 rename、thumbnail 内部 path 权限短路、open-time create/truncate、字面 `"null"` owner 兜底、跨 helper batch 非原子性、commit 后扫描反向失败，以及显式事务外通知与内存 cache 不受回滚约束等 r48 边界。
- 第 276 章已完成：由 120 个碎片化二级标题、563 行重构为 16 个二级标题、548 行，以“Transport、URI/volume、projection contract、trusted row policy与canonical恢复怎样在不同边界组合”为主线，拆清Bundle参数、定制SQLiteQueryBuilder、owner/global与typed公式、MATCH/FUSE、Cursor探针及canonical重定位，并提供九组只读源码练习。
- 技术修正：核准target<Q thumbnail与控制Cursor绕过普通builder/metadata，structured honored只表示处理分支，self仍受projection map与strict parentheses；补出ringtone OR进入写builder、playlist-member读能力可通过静态update/delete链、audio聚合view异步收敛的卷快照。进一步确认成对r48的旧APEX关闭动态源row校验，item/collection/prefix grant账均可令allowGlobal跳过owner/capability；并闭合canonical同名参数遮蔽、Image/Video exact比较错字段、清参后exact grant失配、旧id复用，以及deletedata/requireOriginal操作语义改变等边界。
- 第 277 章已完成：由 120 个碎片化二级标题、567 行重构为 16 个二级标题、433 行，以“一次缩略图请求怎样让typed授权、生成、inode发布与缓存失效在不同完成点收敛”为主线，拆清客户端采样、Transport对象级缺口、三类生成器、并发临时文件、取消、legacy竞态、UUID与idle维护，并提供九组只读源码练习。
- 技术修正：核准r48 MediaProvider typed-thumbnail只有组件级read门，ordinary direct item缺对象级row校验，且URI match后切self，直接item cache hit不查DB；区分requested Size、固定rough target与客户端整数sample，并补出零维Size异常、synthetic external只查当前卷却落primary根、album/Files的前置查询。进一步闭合compress false仍rename、late cancel、sPending单remote与ABA、files trigger在schema write lock下被抑制、invalidate完成后在途旧生成者仍可回写、`album_art`维护遗漏，以及UUID marker和idle只认known-id的盲区。
- 第 278 章已完成：由 120 个碎片化二级标题、562 行重构为 16 个二级标题、500 行，以“一次SAF媒体分类请求怎样让authority授权、type:id投影、MediaStore委托与跨Provider URI桥接在不同完成点收敛”为主线，拆清四root能力广告、虚拟层级、MatrixCursor、recent/search、只读FD、metadata、delete、通知/revoke与双向URI转换，并提供九组只读源码练习。
- 技术修正：核准Documents EMPTY统计整个当前Files可见域而发现链只取MEDIA_TYPE_DOCUMENT，Audio EMPTY/search与`is_music=1`导航口径分叉，child排序/其他Bundle参数与取消信号被丢弃，`*/*`及OGG/x-flac MIME预筛异常；补出`document:<id>` generic alias、metadata同authority横向对象授权缺口、delete count与双层revoke非原子。进一步闭合MediaStore→ESP只传persisted grant快照、full grant提前break改变tree优先级、exact/tree对missing文件检查不同、反向payload authority与canonical containment均未绑定，以及两向转换只返回标识而不新增grant。
- 第 279 章已完成：由 120 个碎片化二级标题、573 行重构为 16 个二级标题、481 行，以“一次DocumentsUI选择怎样让Intent/State候选、root/document快照、UI选择、临时grant与显式持久化在不同完成点收敛”为主线，拆清四个公开入口、位置优先级、Provider/root/document三层加载、三类loader、选择策略、结果Intent与持久授权，并提供九组只读源码练习。
- 技术修正：明确精确root定位含initial/default旁路matching roots，initial失败仍先重试last-access，并区分State parcel字段缺失、root-document中间查询与ProvidersCache三种时间/缓存边界；补出Recents每root先截64、GlobalSearch保留目录与逐root失败、Directory CancellationSignal在r48 DocumentsProvider children/search分支被丢弃。进一步核准CREATE raw MIME与三提交点、virtual/openable/replace规则、TREE blocked与hidden双门、GET外部forward、ClipData null MIME失败，以及collectGrants return/throw、exported/path-permission/grant-uri-permission差异、ActivityRecord owner、same-URI prefix take、重复take touch但不写盘、512与10秒合并写盘等边界。
- 第 280 章已完成：由 120 个碎片化二级标题、564 行重构为 16 个二级标题、425 行，以“一份URI能力怎样经过证明、Needed计划、owner或global落账、生命周期回收与持久恢复五个完成点”为主线，拆清四本账、两阶段检查、组件owner、撤销匹配、包用户清理与XML恢复，并提供九组只读源码练习。
- 技术修正：核准r48实际访问按getStrength检查persistable offer而modeFlags不含offer，READ/WRITE不能跨账拼接；补出special cross-user受限探测先于Provider policy、Needed不带epoch且逐项落账、targetUid跨项重解析、Context显式grant丢source user，以及prefix fallback漏比source user。进一步区分ActivityRecord移出history、普通非persistent started Service重投递与sticky完成保留、broadcast无通用hook、ExternalToken无死亡回收、定向revoke不清owner、target-user偏置包清理、same-URI exact/prefix双take、512仅为take修剪目标、10秒非trailing合并、带mLock写盘/恢复及sourcePkg首次创建黏住等边界。
- 第 281 章已完成：由 120 个碎片化二级标题、578 行重构为 16 个二级标题、422 行，以“一次authority获取怎样跨客户端缓存、AMS launching、宿主安装/publish和引用/死亡协议交付或收回Provider Binder能力”为主线，拆清两端索引、三只时钟、租约转移、OOM、external handle与ANR，并提供九组只读源码练习。
- 技术修正：明确同key小锁只串行AMS往返而不覆盖安装或二次查缓存，20秒ready超时不回滚Connection/Map/launching/隐式可见性；核准Provider先于Application.onCreate发布、部分publish会移除进程级监督、首次服务端引用与客户端0↔1通知的不同完成点。进一步纠正stable/unstable并非两档OOM强度，区分普通崩溃重试与启动/发布超时终局，补出跨user singleton客户端key、旧Cancellation transport、本地/no-release引用、external残留账及主Looper watchdog盲点等r48边界。
- 第 282 章已完成：由 120 个碎片化二级标题、557 行重构为 16 个二级标题、450 行，以“一次Transport请求怎样让URI形状、permission/grant、AppOps、caller身份、接口拒绝与资源完成点依序收敛”为主线，逐项覆盖CRUD/Bulk、open、call/type、Cursor/FD及batch，并提供九组只读源码练习。
- 技术修正：明确authority-only入口仅去前缀比较却原样传业务、PathPermission只在对应方向且无中途AppOps异常时呈OR语义、MODE_ERRORED在noteProxyOp内硬抛，补出singleUser读写grant与callerToken/AppOps代理身份不对称。进一步核准远程空projection塌缩为null并执行query、原Cursor未关且descriptor已离开caller Pair、Cancellation仅还原同进程transport、死亡重试丢取消、refresh返回不解绑、FD成功写Parcel后关闭服务端wrapper，以及batch别名不必等于outer、本地改写调用方列表、TYPE_CALL null触发NPE与默认循环不承诺事务等r48边界。
- 第 283 章已完成：由 120 个碎片化二级标题、559 行重构为 16 个二级标题、438 行，以“一次notifyChange怎样把客户端user分组、观察者树匹配、回调派发、cache失效与sync筛选拆成不同完成点”为主线，拆清public/hidden入口、跨用户准入、树真值表、Collector、生命周期、派发、兼容与后半副作用，并提供九组只读源码练习。
- 技术修正：明确public Collection按user拆成顺序RPC且null项迟至服务端失败，单RPC验证与跨组部分完成必须分账；核准ObserverNode的ancestor/exact/注册后代三态、SKIP/self/user过滤，以及Collector按IContentObserver包装对象而非Binder聚合。进一步闭合同节点重复注册与unregister非排空、本地death no-op、远端死亡暂留空骨架、remove-before-add、逐URI树锁、oneway与本地异常、10秒/NO_DELAY、R旧int、cache误清漏清、USER_ALL不展开、sync calling user/30秒筛选及pre-O Uri.EMPTY全树/全authority放大等r48边界。
- 当前从第 284 章继续。
