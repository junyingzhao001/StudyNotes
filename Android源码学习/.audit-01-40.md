# 01—40 章逐篇源码审计记录

> 审计基线：本地 AOSP Android 11 / API 30 / `android-11.0.0_r48`。每一行只在完整读完该章并核对关键源码后填写。文档按 macOS 只读学习设计，不把未执行的编译/设备命令宣称为实测。

| 章 | 结果 | 核对证据与修改摘要 |
|---:|---|---|
| 01 | 已核源并修订 | 对照 `frameworks/base/core`、`services`、AIDL/Binder 与 Zygote 本地结构；补 r48 边界，纠正“分层图=调用栈”、跨进程对象引用、AIDL 必然跨进程、Native 等于内核和 COW 永久共享等易混点；新增进程/线程/边界标注法、Mermaid 与 macOS `rg` 练习。 |
| 02 | 已核源并修订 | 实查 `version_defaults.mk`、`.repo`、AMS/ATMS/SystemServer 实际路径、SystemUI/SurfaceFlinger 目录；区分平台版本与 manifest tag 证据，补“源码路径→模块→产物→进程”导航、ATMS `wm` 路径、HAL 版本演进、路径漂移查法与只读练习。 |
| 03 | 已核源并修订 | 实查 `init/main.cpp`、`init.cpp`、`init.zygote*.rc`、`ZygoteInit.main()`、`SystemServer.run()` 和 `bootanim.rc`；纠正 init 首次直入 second stage、Zygote 统一注册 socket、父子 fork 同路执行等表述，补启动并发分支、五种控制流、boot-complete 多信号与手算练习。 |
| 04 | 已核源并修订 | 核对 `FirstStageMain`、`SetupSelinux`、`SubcontextMain`、`SecondStageMain`、`LoadBootScripts`、rc Parser 和 `Service::Start()`；补遗漏 subcontext 分支、`ro.boot.init_rc` 特例，明确 service 是 fork 子进程后 exec，init 创建的 socket fd 通过约定环境传递。 |
| 05 | 已核源并修订 | 核对 `app_main.cpp`、`AndroidRuntime.cpp`、`ZygoteInit`、`Zygote`、`com_android_internal_os_Zygote.cpp`、`ZygoteServer`；补 lazy-preload 边界、`ForkCommon()` 中间层、fork 窗口不等于 Zygote 永久单线程，收紧“最小权限”与 Runnable 的过度概括，补 App `attach()`/ClientTransaction 汇合说明；又补双 Zygote 设备的关键时序：fork 后的 system_server 子进程先 `waitForSecondaryZygote()`，再关闭继承 socket/进入 SystemServer，不是父 Zygote 等待，也不是副 Zygote 创建第二个 system_server。 |
| 06 | 已核源并修订 | 核对 `SystemServer.run()`/三组启动、`SystemServiceManager`、boot phase 常量、PMS Watchdog 包裹和 AMS `finishBooting()`；明确 PMS 只是启动期临时 pause Watchdog，SystemUI 不是第三方 App，phase 600 只是允许 start/bind，phase 1000 在后续 finishBooting 发出；进一步限定 phase 1000 只是 SystemService 回调阶段，不等于 `sys.boot_completed` 已写、某用户 boot 广播收完或 Launcher 首帧已 present。 |
| 07 | 已核源并修订 | 逐节核对 AIDL 生成模式、`BinderProxy`→JNI→`BpBinder`→`IPCThreadState`→驱动→`execTransact/onTransact`、servicemanager context manager、calling identity 与 death notification；修正 Parcelable 重建方向、补 `oneway` 的返回/out 限制、`clearCallingIdentity` 不是 setuid/root、DeathRecipient 语义和回调线程，删除重复句。 |
| 08 | 已核源并修订 | 核对 `Activity.startActivityForResult`、`Instrumentation.execStartActivity/checkStartActivityResult`、`ContextImpl.startActivity`、`IActivityTaskManager.aidl` 与 ATMS 入口；补 requestCode 只是请求异步结果且仍需有效 source，纠正非 Activity Context 的 N—O_MR1/launchTaskId 兼容边界，说明仅 fatal start result 被转成异常、`START_ABORTED` 不能用“没抛异常”判成功。 |
| 09 | 已核源并修订 | 核对 ATMS 入口、`ActivityStarter.execute/executeRequest/startActivityInner`、ActivityRecord/ActivityStack/Task 类继承、Task 复用与 abort 收口；补 identity 必须 finally restore，收紧 singleTask/singleInstance 的“全局唯一”过度表述，明确 r48 内部 `START_ABORTED` 在对外返回前转为 `START_SUCCESS`。 |
| 10 | 已核源并修订 | 核对 `startSpecificActivity/realStartActivityLocked`、ProcessList→Zygote、`attachApplicationLocked`、IApplicationThread oneway、ClientTransaction 与 `performLaunchActivity`；主要修正冷启动时序图：AMS 发出 `bindApplication()` 后不等 Application.onCreate，便在同一系统端 attach 流程调 ATMS 并发 launch；App 主线程依投递顺序先 BIND_APPLICATION 后 EXECUTE_TRANSACTION。 |
| 11 | 已核源并修订 | 核对 `Activity.attach/handleResumeActivity`、PhoneWindow decor、WindowManagerGlobal/ViewRootImpl `setView`、IWindowSession/WMS 与 traversal 主线；纠正 InputChannel 并非 App `new` 完就单方可用，而是 Binder out 容器配合 WMS/InputDispatcher 建立配对通道；将“addView 必须主线程”精确为 ViewRootImpl owner-thread 约束，Activity 主窗口的 owner 恰为主线程。 |
| 12 | 已核源并修订 | 核对 HWUI/RenderThread、`Surface`、BufferQueue slot 状态、r48 BLASTBufferQueue、SurfaceFlinger Layer/CompositionEngine/HWComposer 与 fence 语义；原章已准确区分经典 BQ 与 BLAST、非固定三缓冲、App/SF VSync 和 GPU/HWC 混合合成；新增关键版本边界：r48 `mUseBLAST` 的 DeviceConfig 默认值是 false，实际路径需同时满足 WMS 开关和窗口 `PRIVATE_FLAG_USE_BLAST`，不是 Android 11 所有 App 窗口必经；并将 present fence 收紧为 HWC present retire/完成同步，避免等同人眼可见回调。 |
| 13 | 已核源并修订 | 核对 PMS main/开机 scan、Settings 持久化、PackageParser2/ParallelPackageParser、PackageInstallerSession→installPackagesLI、scan/reconcile/commit、PermissionManager 与 installd；原章主线与 r48 一致，修正 Session stage 为受安装管线授权的写入，明确 Java `Installer` 是 SystemService 包装层，经 `IInstalld` Binder 跨到 init 启动的 Native installd daemon。 |
| 14 | 已核源并重构 | 核对 r48 `ActivityThread.performLaunchActivity()` 的 component 别名解析、`newActivity/attach/callActivityOnCreate/mCalled/mActivities` 精确顺序，并核对 `framework-minus-apex` 的 `installable: true`/`stem: "framework"` 与 `framework` 的 build-only 定义；将全章改成 macOS 只读推演，当前只要求精确插入锚点、未应用概念 diff 和证据设计，将编译、镜像、adb、故障注入与回滚全部标成未来 Linux/userdebug 方案，不再暗示本轮需真编译/动设备。 |
| 15 | 已全文核对，无需改文 | 实查 `ProcessList` 的 startSeq/pending-start、OOM adj 常量、`LMK_PROCPRIO` 四整数消息，`ActivityManager` 的 procState 顺序，`OomAdjuster` 的 capability/`BIND_INCLUDE_CAPABILITIES` 与 `lmkd.cpp` 命令入口；本章已正确区分 ProcessRecord/Linux 进程、procState/adj/schedGroup/capability、Framework 主动 kill/LMKD/crash，且对 PSI、freezer 和 cached process 均保留了 r48/设备配置边界。 |
| 16 | 已核源并修订 | 核对 `BroadcastReceiver.PendingResult.finish()`、`LoadedApk.ReceiverDispatcher.Args`、`ReceiverRestrictedContext`、fg/bg/offload timeout 常量与 `BroadcastQueue.broadcastTimeoutLocked()`；纠正原文把“无序动态 Receiver”误写为 `TYPE_UNREGISTERED`——正常注册对象仍是 `TYPE_REGISTERED`，不回报 AMS 是因 `orderedHint=false`；补充 `TYPE_UNREGISTERED` 的 resultReceiver 语义、Restricted Context 精确限制，并删去 r48 该路径中不存在的“CPU 时间判断”概括，改成实际 timeout-message 复用/重置逻辑。 |
| 17 | 已核源并修订 | 核对 `ActiveServices` 的 create/start/bind/unbind/destroy、20/200s service timeout、10s foreground-transition timeout，`ActivityThread` 各 Service 回调，以及 AMS Provider wait/publish/ref/death 路径；纠正 `onUnbind(false)` 后必然重调 `onBind()` 的误解，只要 Service 实例仍存活可继续复用已 publish Binder；补准 Provider 超时起点：10s publish timeout 是进程 attach/准备 bindApplication 后安排，20s ready timeout 从本次 get-provider 等待起算，两者不是同起点两级倒计时。 |
| 18 | 已核源并修订 | 核对 r48 InputDispatcher `waitQueue/AnrTracker/onAnr/notifyAnr/timeout extension`、Broadcast/Service timeout、`AnrHelper` 1 分钟过期缩减和 tombstoned ANR trace 路径；重点修正 Provider 分类：10s publish timeout 会按初始化失败清理宿主进程，20s ready timeout 让 get-provider 返回失败，两者都不直接走 `AnrHelper`；只有已发布 Provider 的业务调用被系统/特权调用方显式设置不响应检测（或 Framework 专用检测）到期，才上报 `ContentProvider not responding` ANR。 |
| 19 | 已核源并修订 | 核对 `ActivityRecord extends WindowToken`、`WindowState/DisplayContent.mTokenMap/WMS.mWindowMap`、ViewRootImpl add/relayout/draw-finished、InputChannel pair 与 `findFocusedWindow/canReceiveKeys`；原章对 add→relayout→draw→show→focus、四类焦点与 Android 11 DisplayArea 边界已较准确。补齐一条关键跨层路径：InputMonitor 将 `InputWindowHandle` 写进 `SurfaceControl.Transaction.setInputWindowInfo()`，SurfaceFlinger 再按实际 Layer Z-order 汇总并经 InputFlinger 交给 InputDispatcher；`setFocusedApplication` 则是独立直达链，不能说 WMS 直接传整张窗口表。 |
| 20 | 已核源并修订 | 核对 InputManager 启动顺序、EventHub/Reader/Mapper/Dispatcher、inbound/outbound/wait 队列、`startDispatchCycleLocked()` 在 publish 尝试前刷新 delivery/timeout 且成功后才进 waitQueue、InputTransport publish/finish 与 App InputStage/batching；补清 server/client channel 本身是端点，`InputPublisher`/`InputConsumer` 是两端各自的包装者；将 WMS→input 窗口表细化为 SurfaceControl input-info→SurfaceFlinger layer 汇总→InputFlinger→Dispatcher；补 r48 InputClassifier 的有界队列/异步 HAL/缓存分类语义，避免误会每个 MOVE 同步等 HAL。 |
| 21 | 已核源并修订 | 标注主图是硬件加速 View 路径；补充 r48 对 `physicalDisplayId` 的边界；按 `DrawFrameTask` 实现澄清 UI 通常只等待 RT 同步/准备阶段，并非等待 GPU、SF、HWC 完成物理显示。 |
| 22 | 已核源并修订 | 核对 fs_mgr/vold/installd、user 0 CE bind mount、共享存储 session、Context 目录、SharedPreferences 与 SQLite pool/JNI；将后续版本才拆出的 `AppDataHelper` 改回 r48 `PackageManagerService.prepareAppData...`，并澄清普通 Cursor 并非整个生命周期必然独占 connection，避免把资源泄漏和 pool 等待错误地画成一一对应。 |
| 23 | 已核源并修订 | 核对 r48 PermissionManager/PermissionChecker/AppOps、Binder calling identity、组件与 URI grant、SELinux label/policy；修正“漏 restore 会永久污染线程池后续请求”的过度表述：危险窗口是当前方法剩余代码及嵌套调用，native Binder 在 transaction 分发退栈时仍会恢复分发前身份，但这绝不能代替业务层 `finally restore`。 |
| 24 | 已全文核对，无需改文 | 核对 ConnectivityService 的异步 NetworkMonitor 注册、NAI/netId/netd/DnsResolver 初始化、request/rematch/linger、IpClient、socket→JNI→fwmark 与按网络 DNS；本章已准确区分控制面/数据面、INTERNET/VALIDATED、Network/netId/handle、系统默认/进程默认/单 socket 绑定，并对 VPN、策略路由和版本边界保持了必要限定。 |
| 25 | 已核源并修订 | 核对 PowerManagerService dirty-bit 收敛、WakeLock record/disabled/summary、SuspendBlocker/SystemSuspend、deep/light DeviceIdle 及 Alarm/Job 协作；纠正 `ACQUIRE_CAUSES_WAKEUP` 的精确行为：API 合同禁止与 PARTIAL 搭配，但 r48 构造校验不会拒绝该组合，服务端只对 screen lock 应用 wake-up flag，不能以“没抛异常”推断组合有效。 |
| 26 | 已核源并修订 | 核对 ClassLoader→DexFile JNI→ClassLinker、ArtMethod 入口、JIT/OSR/profile、PackageDexOptimizer→installd→dex2oat、OAT/VDEX 校验及 CC/read barrier/GC；修正两处不存在的 `art/runtime/mterp/` 为 r48 的 `art/runtime/interpreter/mterp/`，并补清 `interpreter.cc` 在 switch/mterp 间选择及 mterp 回落边界，避免把同目录中的 nterp 文件误当作所有设备必走路径。 |
| 27 | 已全文核对，无需改文 | 核对 notify→NMS Enqueue/Post Runnable、Channel 用户锁定字段、Record/ranking/alert、listener→SystemUI Entry/RemoteViews/Row、heads-up、PendingIntent、FGS 与 timeout alarm；本章已准确标明 Android 11 无 POST_NOTIFICATIONS、SystemUI 双 pipeline 迁移期、r48 只有 `FLAG_IMMUTABLE` 无显式 `FLAG_MUTABLE`，且展示、提醒、点击和权威记录边界清楚。 |
| 28 | 已核源并修订 | 核对 AudioTrack Java/JNI/native、共享 cblk/proxy、AudioFlinger PlaybackThread/Mixer/Fast/Direct/Offload、AudioPolicy 路由/patch、AudioService focus/volume 与 HAL；纠正 Track 创建图：r48 客户端先调用 `IAudioFlinger::createTrack()`，由 AudioFlinger 服务端再经 `AudioSystem::getOutputForAttr()` 询问策略并选择 PlaybackThread，不是客户端先直接向 APS 取 output。 |
| 29 | 已核源并修订 | 核对 CameraManager→CameraService→CameraDeviceClient/Camera3Device→HIDL `.hal`、session/stream、request/result/inflight、ImageReader 与 BufferQueue/fence；补出 r48 已支持的 HAL 3.5 buffer-management 分支：传统路径由 Framework 在提交前预取 buffer，新路径允许 HAL 稍后以 `requestStreamBuffers()` 请求，避免把“每个 request 必先携带实际 output buffer”写成绝对规则；并确认文中 HAL 接口均引用真实 `.hal`，未把生成的 `I*.h` 当源码入口。 |
| 30 | 已全文核对，无需改文 | 核对 MediaExtractor、MediaCodec Java/JNI/native 状态机、CCodec/Codec2 C2Work、ACodec/OMX、buffer ownership、Surface render timestamp、flush/EOS/seek 与 NuPlayer 时钟；本章已准确保留 Android 11 CCodec/ACodec 并存边界，区分媒体 PTS 与 monotonic render time，并正确说明 output release、Surface 显示、EOS drain 和 backpressure。 |
| 31 | 已全文核对，无需改文 | 核对 BluetoothService/BluetoothManagerService→独立 Bluetooth App Adapter/Profile Service→JNI→btif/BTA/HCI，Classic discovery、BLE scan、bond、A2DP、GATT 与 BluetoothSocket；本章已准确保留 API 30 的 normal `BLUETOOTH(_ADMIN)` 加位置约束、API 31 Nearby Devices 边界、BLE-only/GD 渐进迁移及请求/最终 callback 语义。 |
| 32 | 已全文核对，无需改文 | 核对 SystemServer JNI 启动 native SensorService、SystemSensorManager→Java/native EventQueue→SensorEventConnection、BitTube、SensorDevice 多客户端 batch 仲裁、wake-event ack、HAL 1.0 poll/2.x FMQ、虚拟/direct/dynamic sensor 与 UID/privacy policy；文中 HAL 入口均为 r48 真实 `ISensors.hal`，未把生成 `I*.h` 误作源码文件，采样/上报/回调时间及 us/ns 边界也准确。 |
| 33 | 已全文核对，无需改文 | 核对 r48 LMS 内部 `LocationProviderManager`、Receiver/UpdateRecord/ProviderRequest 聚合与分发、ListenerTransport 自动完成回执、LocationFudger、Network/Fused proxy、GnssLocationProvider→JNI→HIDL `.hal` 及专项 GNSS 数据流；确认全部 GNSS 接口引用真实 `.hal`，未把生成 `I*.h` 当源码入口，fine/coarse、wall/elapsed time 与请求/结果边界准确。 |
| 34 | 已全文核对，无需改文 | 核对 PhoneGlobals/PhoneFactory、phoneId/slotId/subId、UICC/subscription、SST poll/reconcile、RIL request serial/response/indication、CS/IMS、DcTracker→DataServiceManager→CellularDataService→RIL→Radio HAL 与 DataConnection NetworkAgent；确认 Radio 接口均引用 r48 真实 `.hal`，未把生成 `I*.h` 当源码入口，且旧 rild socket 与当前 HIDL 主线、Telephony/Telecom/Connectivity 边界准确。 |
| 35 | 已全文核对，无需改文 | 核对 Telecomm Manifest 的 system process、TelecomServiceImpl/CallsManager、PhoneAccount、ConnectionServiceWrapper、TelephonyConnectionService、InCallController/ParcelableCall/InCallAdapter、音频状态机及 Binder death 清理；本章已准确区分 Telecom Call、Framework Connection、Telephony Connection 与 UI Call，且外拨、来电、接听、状态回传和音频路由的完成点清楚。 |
| 36 | 已核源并修订 | 核对 SmsManager→SmsController→per-Phone IccSmsInterfaceManager→Dispatcher、IMS/RIL 发送、InboundSmsHandler raw 持久化/重组/ACK、两阶段广播、SmsBroadcastUndelivered 与 SmsProvider；明确 r48 TelephonyProvider 随应用运行于 `com.android.phone`；纠正 CarrierMessagingService 入站过滤能力描述：返回的是丢弃/锁定存储不可用时跳过通知等 `RECEIVE_OPTIONS_*` 位，并不能任意指定“下载到某应用”，最终投递对象仍由 Framework 决定。 |
| 37 | 已核源并修订 | 核对 WifiServiceImpl→ActiveModeWarden→ClientModeManager、扫描/wificond、网络选择、Supplicant `.hal`→WifiMonitor→ClientModeImpl、IpClient/NetworkAgent/NetworkMonitor；确认 HAL 入口引用真实 `ISupplicantStaIface.hal`，未把生成头文件当源码。修正总图把 `WifiConnectivityManager` 误标成通用 ConnectivityManager，并补准 r48 IPv6 主线为 RA/SLAAC/RDNSS，避免让读者误以为存在与 IPv4 DhcpClient 对称的 DHCPv6 地址配置路径。 |
| 38 | 已全文核对，无需改文 | 核对 VpnService prepare/Builder/establish、Vpn owner 与授权切换、JNI `/dev/tun` 创建、LinkProperties/NetworkAgent、UID ranges→netd 路由、protect/fwmark、underlyingNetworks、always-on/lockdown 及 IKEv2/Legacy 分支；本章已准确区分虚拟网与底层网、内层包与外层包、protect 与 bind、Framework 建网完成与远端隧道真正可用。 |
| 39 | 已核源并修订 | 核对 Tethering 模块进程、Tethering/IpServer、PrivateAddress/DHCP/RA、UpstreamNetworkMonitor、netd `tetherAddForward`/`ipfwdAddInterfaceForward`/DNS forwarding、OffloadController 与 BpfCoordinator；补准上游选择并不统一以 VALIDATED 为硬门槛，应把它作为公网可用性的后续证据；并明确 r48 BpfCoordinator 重点是 IPv6 邻居转发 offload、配额和统计，不能泛化成 IPv4 NAT 等全部数据面。 |
| 40 | 已核源并修订 | 核对 EthernetServiceImpl/Tracker 的接口发现与 client/server mode、EthernetNetworkFactory 每接口对象、IpClient 异步创建但同步等待、provisioning→NetworkAgent、link down 清理及稳态内核数据面；修正 availability 的精确含义为已跟踪 client 接口 link up/down 回调，并补准 r48 link-up 会直接启动 IpClient，无需先等待新的 NetworkRequest，同时说明 server mode 转交 tethering 而不走 client provisioning。 |
