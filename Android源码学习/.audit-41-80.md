# 第 41—80 章源码审计记录

> 审计基线：本地 `android-11.0.0_r48`；仅使用本地源码和只读命令。  
> 状态说明：`已复核` 表示正文已完整阅读，并对关键路径、符号、线程/进程、数值和版本边界做过反查；本表 41—80 均已完成该级别复核。

| 章 | 状态 | 证据与修订摘要 |
|---:|---|---|
| 41 | 已复核并修订 | 对照 `Inet6AddressImpl.lookupHostByName()`、JNI `android_getaddrinfofornet()`、`DnsResolver.query()`、`res_cache.cpp`；补清进程内 `AddressCache` 与 resolver per-net cache 两层边界，并精确解释 NO_CACHE_LOOKUP/STORE。 |
| 42 | 已复核并修订 | 对照 NPMS、NMS、`BandwidthController`、`FirewallController`/`TrafficController`；纠正“所有 UID 策略都笼统落为 BPF”表述，区分 Data Saver iptables naughty/nice 与 owner firewall 的 eBPF/iptables 选择。 |
| 43 | 已复核并修订 | 对照 `enforceValidJobRequest()` 与 `JobServiceContext.onServiceConnected()`；纠正 JobService `exported=true` 被写成固定要求的误导，补清 Job wakelock 从 service connected 到 unbind 的边界。 |
| 44 | 已复核 | 对照 Alarm 类型、elapsed/RTC 换算、批处理、trigger/delivery、Doze 与 TIME_TICK 主链；现有文字已明确 Android 11 精确闹钟权限边界及 non-wakeup 语义，未发现需改事实。 |
| 45 | 已复核 | 对照 SyncManager、SyncJobService、SyncStorageEngine 与 AbstractThreadedSyncAdapter；重点确认 JobScheduler 桥、adapter 后台线程、backoff/delayUntil 与多用户边界，未发现需改事实。 |
| 46 | 已复核 | 对照 AccountManagerService Session、Authenticator Binder、token cache/visibility、DE/CE AccountsDb；现有内容已避免把账号访问授权、token scope 与远端登录混为一谈。 |
| 47 | 已复核并修订 | 对照 Android 11 legacy keystore daemon、KeyStoreService、Keymaster HIDL、operation/HAT/SID；版本边界与 Keystore 2.0 后续实现已分开。正文指向 r48 真实 `IKeymasterDevice.hal`/`types.hal`，并明确调用侧 `.h` 是 HIDL 构建生成头文件，不是契约源码。 |
| 48 | 已复核并修订 | 对照 LSS、SyntheticPasswordManager、GateKeeper/Weaver、vold CE unlock；重点核验 password handle、SP、HAT 与用户解锁不是同一对象。正文指向 r48 真实 `IGatekeeper.hal`/`types.hal`，并明确生成 `.h` 与 HIDL 契约源码的边界。 |
| 49 | 已复核 | 对照 UMS/UserController 状态机、UID 计算、DE/CE 与用户广播；空 token 尝试解锁和真正凭据裁决边界已有明确限定。 |
| 50 | 已复核 | 对照 DPMS DevicePolicyData/ActiveAdmin/Owners、provisioning 与策略下沉；角色、持久层和执行者分离清楚，未发现需改事实。 |
| 51 | 已复核 | 对照 AccessibilityManagerService、service connection、event/node query、gesture injection；事件过滤、节点快照、同步外观与跨进程调用边界清楚。 |
| 52 | 已复核 | 对照 IMMS/IMM/InputMethodService、served view、InputConnection 与 show/hide；焦点、绑定、输入连接、可见性四态已分开。 |
| 53 | 已复核 | 对照 ClipboardService、AppOps、URI owner/grant/revoke、cross-profile copy；ClipData 与 URI 内容权限两层解释准确。 |
| 54 | 已复核 | 对照 Autofill Session/ViewState、AssistStructure、Fill/Save 请求与认证；结构快照、迟到 requestId 和最终 View 写入边界清楚。 |
| 55 | 已复核 | 对照 TextClassifier API、TextClassificationManagerService、SelectionActionModeHelper 与 TextLinks apply；生成、应用、动作执行三步未混淆。 |
| 56 | 已复核 | 对照 ContentCapture client session、master/per-user service、direct manager 与事件 flush；控制面和绕过 system_server 的事件数据面边界准确。 |
| 57 | 已复核 | 对照 AppPrediction session、RemoteAppPredictionService、callbacks 与 sortTargets；预测结果、反馈事件和启动权限分离准确。 |
| 58 | 已复核 | 对照 ContentSuggestions task snapshot/Bitmap 两入口、两阶段 request、remote 2s timeout 与 `isEnabled()` 5s 同步等待；正文数值和超时含义准确。 |
| 59 | 已复核并修订 | 对照 PeopleService/DataManager/PeopleServiceInternal；修正 `PeopleServiceInternal.java` 实际位于 `services/core`，其余 people 实现位于 `services/people`。 |
| 60 | 已复核并修订 | 对照 VIMS/Impl/SessionConnection、AssistDataRequester、VoiceInteractor 与 SoundTrigger；修正 AssistStructure/AssistContent 的 `android/app/assist` 路径。 |
| 61 | 已复核 | 对照 SpeechRecognizer/RecognitionService 双向 Binder、Handler 回调、busy 与 stop/cancel/destroy；版本常量和单活动请求模型解释清楚。 |
| 62 | 已复核 | 对照 TextToSpeech client setup、TTS service SynthHandler、PlaybackSynthesisCallback/AudioPlaybackHandler；入队、合成、播放、终态回调边界清楚。 |
| 63 | 已复核并修订 | 对照 MediaSessionRecord、controller callbacks、media-button routing 与 AudioPlaybackConfiguration；声明状态和真实播放没有混写，并将关联 SystemUI 导航修正为 r48 完整路径 `frameworks/base/packages/SystemUI/`。 |
| 64 | 已复核 | 对照 MediaBrowser/MediaBrowserService connection、subscription、detach/result 与 pagination；平台 API 和 AndroidX Compat 版本边界明确。 |
| 65 | 已复核 | 对照 `MediaRouter2`、`MediaRouter2Manager`、`MediaRouter2ServiceImpl`和 provider service；确认 30s transfer timeout、500 条 request-id 记忆上限及 provider/session 归属，现有边界准确。 |
| 66 | 已复核 | 对照 MPMS、`MediaProjection`、`VirtualDisplayAdapter`与 FGS 校验；确认 target Q+ FGS 门槛、projection stop 与 VirtualDisplay release 不是同一步。 |
| 67 | 已复核 | 对照 DMS adapter/device/logical display/traversal；确认 mirror 选择、displayId/layerStack 的 r48 实现与 logical/device 分层。 |
| 68 | 已复核并修订 | 对照 DPC `updatePowerState()` 实际赋值顺序；纠正纯“优先级”表的误导，特别说明 r48 temporary 可覆写前值；补 Mermaid 与可执行 `rg`。 |
| 69 | 已复核并修订 | 对照 DreamManagerService/DreamController/DreamService/DozeMachine；纠正通用 `cleanupDreamLocked()` 被误写成显式清 PMS override，补 Doze WakeLock 语义、Mermaid 与 `rg`。 |
| 70 | 已复核并修订 | 对照 HAL wrapper 回退、temperature/status listener、TemperatureWatcher 与 shutdown；补清底层降频和 Framework 协作两条链、Mermaid 与 `rg`。 |
| 71 | 已复核并修订 | 对照 BatteryService/HealthServiceWrapper/HealthHalCallback；补清 default/backup 是初始选择而非动态抢占，修正“不读 `/sys`”的过度绝对表述，补 Mermaid/`rg`。 |
| 72 | 已复核并修订 | 对照 BSS/BatteryStatsImpl/ExternalStatsWorker/PowerCalculator；恢复 `setBatteryState()` 外层 worker 排队这一关键代码语义，补 Mermaid，练习命令已明确为只读。 |
| 73 | 已复核并修订 | 对照 atoms/config proto、statsd main/socket/processor、Java companion/manager；核验 2000 队列上限、Binder pool 9、pull 1s/2s 默认，并修正 Java 服务精确路径、展开全部 MetricProducer 导航和统一标题。 |
| 74 | 已复核并修订 | 对照 UsageStatsService/UserUsageStatsService/AppStandbyController/AppIdleHistory；核验 elapsed→wall time、DE pending→CE、bucket/reason/minBucket 与 Restricted 开关，展开 6 个源码地图完整路径并统一标题。 |
| 75 | 已复核并修订 | 对照 AppTimeLimitController/UsageStatsService/API/测试；纠正 `getAppUsageLimit()` 是选“剩余时间最少”而非“总额度最小”，修正精确测试路径、补分层 Mermaid 并统一标题。 |
| 76 | 已复核并修订 | 对照 DeviceConfig/Settings NameValueCache/SettingsProvider/SettingsState/mapper；核验 setProperties 全量原子替换、listener 变化集与 generation，修正 Provider 与 `ActivityManagerConstants.java` 精确路径并统一标题。 |
| 77 | 已复核并修订 | 对照 bionic 属性区、init property_service/persistent properties、property_contexts 与 trigger/control 链；纠正“普通读取每次向 init RPC”和“任意写属性会自动回调所有 Java 进程”的误解，补清 mmap/serial wait、显式 `reportSyspropChanged()` 与进程内 callback 边界。 |
| 78 | 已复核并修订 | 对照 Watchdog/WatchdogDiagnostics、PackageWatchdog 与 RescueParty；纠正 diagnostics 被写成 overdue 后无条件执行，补清一次 system_server 卡死与跨重启重复崩溃自愈的不同时间尺度，并修正 `SettingsToPropertiesMapper.java` 精确路径。 |
| 79 | 已复核并修订 | 对照 RollbackManagerServiceImpl/Rollback/Store、AppDataRollbackHelper、PackageWatchdog 与 health observer；纠正 COMMITTED 内存状态被误写为提交时已落盘，区分普通 APK broadcast 与 staged `notifyStagedSession()` 两条 enable 入口，补齐线程、commit 专有 AppOps 检查、普通失败默认 1 分钟 5 次阈值和 Mermaid。 |
| 80 | 已复核并修订 | 对照 StagingManager/PackageInstallerSession、apexd session/激活/revert、vold Checkpoint 与 init 时序；纠正 r48 运行时清单入口为 `apex_manifest.pb`、pre-reboot 顺序、checkpoint 计数、DE_sys/DE_user ready 边界、retry 耗尽与 activation failure 分支，并补完整路径、跨进程线程表和 Mermaid。 |
