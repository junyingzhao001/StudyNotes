# C++ 系统学习路线：大纲评审版

> 当前文件只确定课程范围、章节顺序和写法。确认大纲后再创建正文、demo、练习和项目。

## 课程最终目标

这套资料面向已经有其他语言基础、但刚开始系统学习 C++ 的开发者。完成后应能：

1. 看懂常见 C++ 项目的目录、头文件、实现文件、构建目标和依赖关系。
2. 判断对象由谁拥有、何时销毁，以及引用、指针和视图何时失效。
3. 使用类、RAII、智能指针、STL、文件、异常和测试完成普通业务功能。
4. 正确创建和关闭线程，使用锁、条件变量、future、atomic 和任务队列。
5. 使用 Asio 编写基础 TCP/UDP 程序，处理拆包、超时、取消和连接关闭。
6. 使用 CMake、调试器和 Sanitizer 构建、测试、定位并修复问题。
7. 在代码评审和现有测试保护下参与中小型 C++ 项目开发。

课程主体建议使用 **C++20**，因为它提供 `std::span`、`std::jthread`、`std::stop_token`、concepts 和 ranges。涉及常见存量项目时，会同时标出 C++17 的替代写法。网络部分使用 standalone Asio；标准 C++20 本身没有通用 socket API。

## 每一章固定怎么写

每章使用同一套结构，避免只罗列语法：

1. **先说结论**：本章解决什么问题，读完能做什么。
2. **真实场景**：这个知识在项目的什么位置出现，不学会导致什么问题。
3. **本章目标**：列出可验证的能力，不写“了解、熟悉”这类模糊目标。
4. **Why**：为什么需要这套机制，它解决了哪种具体风险。
5. **How**：只解释完成本章场景所需的规则。
6. **API 作用表**：API、参数、返回值、所有权、失败方式、适用场景和常见误用。
7. **一个主例子**：正文只放关键代码，完整代码放在 demo 中。
8. **Java 对照**：说明相似点和关键差别，不做机械的一一对应。
9. **问题 → 方案 → 验证**：给出运行命令、预期结果和失败实验。
10. **踩坑与边界**：明确未定义行为、失效条件、平台差异和方案限制。
11. **马上动手**：预测题、修改题、独立实现题各一个。
12. **完成标准**：说明做到什么才可以进入下一章。

代码片段原则上不超过 25 行。一个概念只保留一个主例子，架构关系优先使用流程图、生命周期图或对比表。

## 第一阶段：先建立 C++ 的运行与对象模型

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的 API / 规则 | 本章产出 |
| --- | --- | --- | --- | --- |
| 00 | 学习方式与开发环境 | 知道课程边界，能独立运行示例 | `c++ --version`、编译参数、退出码、终端工作目录 | 运行第一个程序并读懂验证结果 |
| 01 | 从源码到程序 | 区分预处理、编译、链接和运行，看到报错时知道先查哪里 | `#include`、`main`、`.o`、静态库/动态库概念、`-std`、`-Wall`、`-g` | 分别制造并定位编译错误和链接错误 |
| 02 | 类型、初始化与表达式 | 避免未初始化、窄化转换、整数除法和隐式类型错误 | 基本类型、`{}`、`const`、`constexpr`、`auto`、`static_cast`、`enum class` | 实现安全的成绩统计 |
| 03 | 控制流与数据建模 | 用清楚的数据结构表达业务状态 | `if`、`switch`、循环、`struct`、结构化绑定 | 实现一组订单状态处理逻辑 |
| 04 | 函数、重载与 Lambda | 看懂参数怎样传入、结果怎样返回、回调怎样表达 | 传值、`T&`、`const T&`、默认参数、重载、`auto` 返回、Lambda 捕获 | 写出不会意外复制或修改输入的函数 |
| 05 | 作用域、namespace、头文件与 cpp | 正确拆分多文件，区分名字可见与实现可链接 | 块/类/namespace 作用域、声明/定义、include guard、`#pragma once`、匿名 namespace | 把单文件程序拆成 `.h + .cpp + main.cpp` |
| 06 | 宏与条件编译 | 知道宏适合做什么，以及为什么普通业务逻辑不应依赖宏 | `#define`、`#if/#ifdef`、构建宏；用 `constexpr`、函数、模板、`enum class` 替代宏 | 编译普通版和带日志版程序 |
| 07 | 指针、引用与生命周期 | 判断拿到的是对象、别名还是地址，避免空指针与悬空 | `&`、`*`、`nullptr`、指针算术边界、storage duration、引用失效 | 为一段代码画出对象和借用的生命周期图 |

## 第二阶段：类、对象生命周期与所有权

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的 API / 规则 | 本章产出 |
| --- | --- | --- | --- | --- |
| 08 | 类与封装 | 把状态和约束放进同一个类型，防止对象处于非法状态 | `class/struct`、`public/private/protected`、成员函数、尾部 `const`、`this`、`explicit` | 实现始终保持有效余额的账户类 |
| 09 | 构造、析构与对象生命周期 | 明确对象何时创建、何时销毁、成员按什么顺序处理 | 构造函数、成员初始化列表、析构函数、成员初始化/析构顺序、静态与局部对象 | 通过日志验证完整生命周期 |
| 10 | RAII 与资源管理 | 保证文件、锁、内存等资源在所有返回和异常路径上都被释放 | RAII、栈展开、`std::lock_guard`、文件流、Rule of Zero | 用真实文件资源验证自动清理 |
| 11 | 拷贝、移动与特殊成员函数 | 判断赋值是在复制对象、转移资源还是建立别名 | 拷贝/移动构造、拷贝/移动赋值、`std::move`、Rule of Five、`= default`、`= delete` | 实现并观察一个资源对象的移动过程 |
| 12 | 动态内存与智能指针 | 清楚表达谁负责销毁动态对象，避免泄漏、重复释放和循环引用 | `std::make_unique`、`unique_ptr`、`shared_ptr`、`weak_ptr`、`get/reset/release` | 为对象关系画出所有权图 |
| 13 | 继承与运行时多态 | 通过稳定接口替换不同实现，并正确销毁派生对象 | `virtual`、纯虚函数、`override`、虚析构、对象切片、组合优先原则 | 用统一接口实现两种存储后端 |
| 14 | 模板、concepts 与编译期多态 | 在类型安全的前提下复用算法，读懂 STL 和项目模板代码 | 函数/类模板、模板实参推导、`requires`、concept、`if constexpr` | 编写有明确类型约束的通用函数 |

## 第三阶段：标准库容器、算法与错误模型

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的 API / 规则 | 本章产出 |
| --- | --- | --- | --- | --- |
| 15 | string、string_view 与 span | 区分拥有数据和只读视图，安全处理文本及连续内存 | `std::string`、`string_view`、`span`、`data/size/c_str`、失效规则、UTF-8 字节边界 | 实现不复制输入的解析函数 |
| 16 | 顺序容器 | 根据访问、插入和地址稳定性选择容器 | `array`、`vector`、`deque`、`list`、`push_back/emplace_back/reserve/resize/erase` | 实现一个内存中的任务列表 |
| 17 | 关联容器与容器适配器 | 根据有序性、查找方式和重复键选择结构 | `map/unordered_map`、`set/unordered_set`、`stack/queue/priority_queue`、`find/contains` | 实现词频、去重和优先任务处理 |
| 18 | 迭代器、算法、Lambda 与 ranges | 用标准算法表达查找、过滤、排序和转换，并处理迭代器失效 | `find_if`、`sort`、`transform`、`remove_if + erase`、`accumulate`、ranges/view | 完成过滤、排序、统计流水线 |
| 19 | 错误模型 | 根据失败是否可预期、是否可恢复选择表达方式 | 返回值、错误码、异常、`optional`、`variant`、断言；补充 `expected` 的 C++23 用法 | 为文件加载设计成功与失败合同 |

## 第四阶段：流、文件、配置与系统边界

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的 API / 规则 | 本章产出 |
| --- | --- | --- | --- | --- |
| 20 | 输入输出流与格式化 | 正确读取整行、解析字段、检查流状态并控制输出格式 | `cin/cout/cerr`、`getline`、`istringstream/ostringstream`、`iomanip`、`std::format` | 实现可验证的命令解析器 |
| 21 | 文本与二进制文件 | 正确打开、读写、刷新和关闭文件，处理部分失败 | `ifstream/ofstream/fstream`、open mode、`read/write`、`good/fail/eof`、缓冲与关闭错误 | 保存并恢复任务数据 |
| 22 | filesystem 与可靠写入 | 安全处理路径、目录、文件覆盖和跨平台差异 | `std::filesystem::path`、`exists`、`create_directories`、`rename`、`remove`、`error_code` | 使用临时文件加 rename 避免写坏原文件 |
| 23 | 配置、JSON 与序列化边界 | 让程序的数据格式可升级、可校验，并与外部系统交换 | JSON 库的 parse/dump、字段校验、版本字段、未知字段策略；不手写完整 JSON 解析器 | 实现带版本的配置加载与错误报告 |
| 24 | C API、ABI 与回调 | 安全对接系统库、JNI/NDK 或纯 C 接口 | `extern "C"`、指针+长度、函数指针、回调 context、所有权合同、字节序、结构体布局基础 | 封装一个安全的 C 风格接口 |

## 第五阶段：真实项目的构建、测试与排错

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的 API / 工具 | 本章产出 |
| --- | --- | --- | --- | --- |
| 25 | 项目目录与 CMake target | 让库、程序、测试和依赖使用同一份构建关系 | `add_library`、`add_executable`、`target_sources`、`target_include_directories`、`target_link_libraries`、`target_compile_features` | 把前面的程序整理为库、CLI 和测试目标 |
| 26 | 测试与可测试设计 | 把需求变成可重复验证，避免只能手工点程序 | 单元测试框架、fixture、参数化测试、fake、依赖注入、CTest | 为业务、文件失败和边界输入补测试 |
| 27 | 调试器、警告与 Sanitizer | 根据错误类型选择工具，找到错误第一次出现的位置 | LLDB/GDB、调用栈、断点、watch、ASan、UBSan、TSan、静态检查 | 修复越界、悬空和逻辑边界错误 |
| 28 | 依赖、日志与配置组织 | 让项目可复现构建，并保留足够诊断信息 | CMake FetchContent/包管理器概念、日志级别、结构化字段、配置覆盖顺序 | 为项目接入一个依赖并记录可定位日志 |

## 第六阶段：线程、同步与异步任务

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的 API / 规则 | 本章产出 |
| --- | --- | --- | --- | --- |
| 29 | thread、jthread 与线程生命周期 | 确保线程结束前数据仍有效，并由明确位置负责等待和停止 | `std::thread`、`join/joinable`、`std::jthread`、`stop_token`、参数按值/引用传递 | 创建可停止且不会悬空的后台任务 |
| 30 | mutex 与锁范围 | 保护共享状态和跨多步操作的业务不变量 | `mutex`、`lock_guard`、`unique_lock`、`scoped_lock`、死锁顺序、不要在锁内做慢 I/O | 实现线程安全计数与任务表 |
| 31 | condition_variable 与任务队列 | 队列为空时不忙等，任务到达或关闭时可靠唤醒 | `condition_variable::wait`、谓词、虚假唤醒、`notify_one/all`、关闭和排空协议 | 实现生产者—消费者队列 |
| 32 | future、promise 与一次性异步结果 | 在线程之间传递返回值和异常 | `async`、`future`、`shared_future`、`promise`、`packaged_task`、`wait_for/get` | 实现带超时等待的异步计算 |
| 33 | atomic 与内存模型入门 | 安全处理独立计数或标志，并知道何时仍必须用锁 | `atomic`、`load/store/fetch_add`、默认顺序、acquire/release 概念、`volatile` 的真实作用 | 比较 mutex 计数与 atomic 计数 |
| 34 | 任务执行器与线程池 | 组合队列、线程、future、异常和关闭协议 | worker loop、submit、任务包装、拒绝新任务、drain、取消、背压、析构与 join | 完成可测试的固定线程数执行器 |

## 第七阶段：网络开发

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的 API / 规则 | 本章产出 |
| --- | --- | --- | --- | --- |
| 35 | 网络基础与协议边界 | 理解程序读到的是字节流而不是完整消息 | IP、端口、DNS、TCP/UDP、客户端/服务端、字节序、半关闭、粘包/拆包 | 画出一次 TCP 连接和关闭时序 |
| 36 | Asio 执行模型 | 使用跨平台库管理 socket、事件循环和异步回调 | `asio::io_context`、executor、endpoint、resolver、buffer、同步与异步 API、错误码 | 完成 DNS 解析与连接实验 |
| 37 | TCP 客户端与服务端 | 正确 accept、connect、读写并处理部分读写 | `tcp::acceptor`、`tcp::socket`、`async_accept/connect/read/write`、`read_until` | 实现带回显协议的客户端和服务端 |
| 38 | 消息边界、超时、取消与背压 | 防止半包误解析、永久等待、无限缓存和慢客户端拖垮服务 | 长度前缀/分隔符、`steady_timer`、取消、写队列、最大消息长度、连接状态机 | 实现有最大包长和超时的 TCP 协议 |
| 39 | UDP、HTTP 与 TLS 的选择 | 根据可靠性和协议需求选择现成方案，避免手写复杂协议 | UDP endpoint/send_to/receive_from；HTTP 客户端/服务端库；TLS 证书校验、SNI、握手概念 | UDP 小实验，并使用成熟库完成 HTTPS 请求 |
| 40 | 网络服务测试与可靠性 | 在断网、重连、慢连接和异常数据下保持行为可预测 | loopback 集成测试、故障注入、重连退避、幂等、限流、日志与指标 | 为 TCP 服务补断线、超时和坏包测试 |

## 第八阶段：性能、安全与综合项目

| 章 | 主题 | 本章解决的问题与作用 | 需要掌握的工具 / 规则 | 本章产出 |
| --- | --- | --- | --- | --- |
| 41 | 性能分析与内存成本 | 用数据找到瓶颈，避免凭感觉使用裸指针或无锁结构 | benchmark、profiler、分配次数、缓存局部性、复制/移动成本、复杂度 | 对一个真实热点给出测量前后数据 |
| 42 | 鲁棒性与安全边界 | 防止越界、整数溢出、资源耗尽和不可信输入破坏程序 | 输入上限、整数转换检查、超时、取消、最小权限、模糊测试概念 | 为文件和网络输入建立限制清单 |
| 43 | 综合项目：带网络同步的任务服务 | 把语言、文件、线程、网络、构建、测试和排错连成完整开发闭环 | 前面各章 API；业务层与 I/O 层分离；协议、持久化、线程池、日志、测试 | CLI 客户端 + TCP 服务端 + 文件持久化 + 自动化测试 |

## 第九阶段：常见项目补充专题

这一阶段用于补齐“基础语法会了，但进入不同项目仍经常遇到”的知识。44–49 建议所有人完成；50–55 根据项目方向选择。

| 章 | 主题 | 为什么需要 | 需要掌握的 API / 规则 | 学习级别 |
| --- | --- | --- | --- | --- |
| 44 | 类接口设计、运算符与转换 | 读懂标准库风格类型，避免隐式转换和异常破坏对象状态 | `operator==`、`<=>`、`[]`、`()`、流输出运算符、转换构造、转换运算符、`explicit`、`noexcept`、强/基本异常安全保证 | 必学 |
| 45 | 常用工具库、时间与随机数 | 处理组合返回值、回调、时间点、超时和随机数据 | `pair/tuple`、`std::function`、`reference_wrapper`、`chrono`、随机数引擎与分布、`regex` 的适用边界 | 必学 |
| 46 | Unicode、编码与本地化 | 避免把字节数当字符数，正确处理文件、网络和用户输入 | UTF-8 code unit/code point/grapheme、`char8_t/u8string`、locale 边界、成熟 Unicode 库的职责 | 必学 |
| 47 | 二进制数据、位运算与字节序 | 处理协议、文件格式、硬件数据和跨平台序列化 | `std::byte`、`bitset`、位运算、`std::endian`、对齐、`memcpy`、不要直接序列化对象内存布局 | 必学 |
| 48 | 共享库、ABI 与发布 | 让库能被其他程序稳定使用和升级 | 静态/动态库、符号可见性、ABI 风险、PImpl、CMake `install/export`、版本兼容 | 必学 |
| 49 | 代码质量与持续集成 | 在提交前自动发现格式、警告、未覆盖路径和不安全输入 | clang-format、clang-tidy、Include-What-You-Use、覆盖率、模糊测试、Doxygen、CI 构建矩阵 | 必学 |
| 50 | C++20 额外同步工具 | 在特定同步模式下避免手写复杂计数协议 | `shared_mutex`、`call_once/once_flag`、`thread_local`、`counting_semaphore`、`latch`、`barrier` | 并发方向 |
| 51 | 协程与异步网络 | 看懂现代异步库如何把回调流程写成顺序代码 | `co_await/co_return` 的语义、coroutine lifetime、Asio `awaitable/co_spawn/use_awaitable`、取消和异常 | 网络/异步方向 |
| 52 | 系统与进程接口 | 处理命令行程序、守护进程和操作系统资源；这些能力没有完全统一的标准 C++ API | 参数与环境变量、signal、进程、管道、动态加载、内存映射、文件描述符/handle、平台封装 | 系统方向 |
| 53 | 数据库访问 | 让服务安全地持久化结构化数据 | SQLite/客户端库、prepared statement、事务、连接生命周期、迁移、错误与重试边界 | 后端方向 |
| 54 | Modules 与大型构建优化 | 理解新式模块边界及大型项目如何降低构建成本 | `module/import` 概念、header unit、预编译头、增量构建、编译缓存；以工具链支持情况为准 | 大型项目方向 |
| 55 | 高级泛型、内存与无锁结构 | 为阅读基础设施代码建立地图，不在第一轮自行实现复杂组件 | type traits、完美转发、allocator/PMR、自定义 deleter、无锁结构与 ABA 问题 | 高级选修 |

## 按开发方向继续补什么

| 方向 | 主线完成后继续学习 |
| --- | --- |
| Android JNI/NDK | JNI 引用与线程附加、Gradle/CMake native 构建、ABI、Java/C++ 异常边界、字节与字符串转换 |
| 后端服务 | HTTP/RPC 框架、数据库、配置中心、指标、追踪、优雅关闭、容器化部署 |
| 嵌入式 | 交叉编译、无异常/无 RTTI 环境、固定容量容器、中断、寄存器、实时约束 |
| 游戏与图形 | 数学、资源系统、ECS、渲染 API、帧循环、数据布局、性能分析 |
| 桌面 GUI | Qt 等框架的对象模型、事件循环、UI 线程、信号槽、资源打包 |
| 音视频 | 缓冲队列、时间戳、编解码库、零拷贝边界、实时线程限制 |

## 三个递进项目

### 项目一：本地任务管理器

验证类、RAII、容器、文件、错误处理、CMake 和测试。支持添加、完成、删除、保存和恢复，并验证损坏文件与保存失败。

### 项目二：并发文件索引器

验证 filesystem、任务队列、线程池、future、取消和结果合并。扫描指定目录，在线程池中统计文件，并能安全停止。

### 项目三：TCP 任务服务

验证 Asio、消息边界、超时、连接生命周期、线程模型、持久化和集成测试。客户端发送任务命令，服务端持久化并返回结果。

## 明确不在第一轮深入的内容

第一轮会说明这些概念的用途，但不会展开成专家课程：模板元编程技巧、协程底层实现、无锁数据结构、自定义 allocator、编译器实现、完整 ABI、内核网络栈、密码学实现、分布式一致性和高频交易级性能调优。它们无法在一套入门课程中同时讲深，应该在主线完成后按项目方向学习。

## 大纲确认后再做的工作

1. 按上面结构建立章节目录，但先写 00–14，完成审阅后再继续后半部分。
2. 每章提供独立 demo、预期输出、错误实验和一键验证脚本。
3. 每完成一个阶段，运行真实项目检查覆盖是否足够，再调整后续章节。
4. 网络部分先确认使用 standalone Asio 还是 Boost.Asio；默认建议 standalone Asio，减少初学阶段的额外依赖概念。
5. 默认使用 C++20，同时在涉及 `span`、`jthread`、concepts 和 ranges 的章节给出 C++17 对应方案。
